const mongoose = require('mongoose');

const GuildConfigSchema = new mongoose.Schema({
    guildId: { type: String, required: true, unique: true }, // بصمة السيرفر
    ticketCount: { type: Number, default: 1 }, // بيبدأ من 1 (وفي MNC هنخليه 360)
    serverRatings: { type: Number, default: 0 },
    middlemanRatings: { type: Number, default: 0 },
    ticketChannelId: String, // الروم اللي صاحب السيرفر هيختارها
    staffRoleId: String      // الرتبة اللي صاحب السيرفر هيختارها
});

module.exports = mongoose.model('GuildConfig', GuildConfigSchema);
