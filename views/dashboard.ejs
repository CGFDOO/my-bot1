<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MNC System | اختيار السيرفر</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Tajawal', sans-serif; background-color: #121212; color: white; margin: 0; }
        header { background: #1e1e24; padding: 20px 50px; display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid #5865F2; }
        .user-info { display: flex; align-items: center; gap: 15px; }
        .user-info img { width: 50px; height: 50px; border-radius: 50%; border: 2px solid #5865F2; }
        .logout-btn { background: #ed4245; color: white; padding: 10px 20px; text-decoration: none; border-radius: 5px; font-weight: bold; transition: 0.3s; }
        .logout-btn:hover { background: #c9383b; }
        
        .container { padding: 50px; max-width: 1200px; margin: 0 auto; text-align: center; }
        .servers-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 20px; margin-top: 30px; }
        
        .server-card {
            background: #1e1e24;
            padding: 30px 20px;
            border-radius: 10px;
            text-decoration: none;
            color: white;
            transition: 0.3s;
            border: 1px solid #2b2b36;
        }
        .server-card:hover { transform: translateY(-5px); border-color: #5865F2; box-shadow: 0 10px 20px rgba(0,0,0,0.3); }
        .server-icon { width: 80px; height: 80px; border-radius: 50%; margin-bottom: 15px; background: #2f3136; display: inline-flex; justify-content: center; align-items: center; font-size: 24px; color: #fff; }
        .server-name { font-size: 18px; font-weight: bold; }
    </style>
</head>
<body>

    <header>
        <div class="user-info">
            <% if (user.avatar) { %>
                <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar">
            <% } else { %>
                <div class="server-icon" style="width:50px; height:50px; font-size:16px;">👤</div>
            <% } %>
            <h2>مرحباً، <%= user.username %></h2>
        </div>
        <a href="/logout" class="logout-btn">تسجيل الخروج</a>
    </header>

    <div class="container">
        <h1>اختر السيرفر</h1>
        <p style="color: #a3a6aa;">اختر السيرفر الذي تريد التحكم في إعداداته (تظهر السيرفرات التي تملك بها صلاحية الإدارة فقط).</p>

        <div class="servers-grid">
            <% guilds.forEach(guild => { %>
                <a href="/settings/<%= guild.id %>" class="server-card">
                    <% if (guild.icon) { %>
                        <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" class="server-icon" alt="<%= guild.name %>">
                    <% } else { %>
                        <div class="server-icon"><%= guild.name.charAt(0) %></div>
                    <% } %>
                    <div class="server-name"><%= guild.name %></div>
                </a>
            <% }) %>
        </div>
    </div>

</body>
</html>
