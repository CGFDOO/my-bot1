<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - اختر السيرفر</title>
    <style>
        body { background-color: #23272a; color: white; font-family: 'Segoe UI', sans-serif; padding: 20px; }
        .container { max-width: 1000px; margin: 0 auto; text-align: center; }
        h1 { color: #7289da; margin-bottom: 40px; }
        .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 20px; }
        .card { background: #2c2f33; padding: 20px; border-radius: 10px; text-align: center; transition: 0.3s; box-shadow: 0 4px 6px rgba(0,0,0,0.3); }
        .card:hover { transform: translateY(-5px); background: #35393e; }
        .icon { width: 80px; height: 80px; border-radius: 50%; margin-bottom: 15px; border: 3px solid #7289da; }
        .btn { display: inline-block; padding: 10px 20px; background: #7289da; color: white; text-decoration: none; border-radius: 5px; margin-top: 15px; font-weight: bold; }
        .btn:hover { background: #5b6eae; }
        .logout { position: absolute; top: 20px; left: 20px; color: #f04747; text-decoration: none; font-weight: bold; }
    </style>
</head>
<body>
    <a href="/logout" class="logout">تسجيل خروج 🚪</a>
    
    <div class="container">
        <h1>🎛️ لوحة التحكم</h1>
        <p>مرحباً، <%= user.username %>! اختر السيرفر للإعداد:</p>
        <br>

        <div class="grid">
            <% guilds.forEach(guild => { %>
                <div class="card">
                    <% if (guild.icon) { %>
                        <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" class="icon" alt="icon">
                    <% } else { %>
                        <img src="https://cdn.discordapp.com/embed/avatars/0.png" class="icon" alt="icon">
                    <% } %>
                    <h3><%= guild.name %></h3>
                    <a href="/settings/<%= guild.id %>" class="btn">إدارة السيرفر ⚙️</a>
                </div>
            <% }) %>
        </div>
        
        <% if (guilds.length === 0) { %>
            <p>❌ أنت لست مسؤولاً (Admin) في أي سيرفر مشترك فيه البوت.</p>
        <% } %>
    </div>
</body>
</html>
