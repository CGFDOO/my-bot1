<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>السيرفرات | لوحة التحكم</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background-color: #0f172a; color: #f8fafc; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
        .guild-card { background: rgba(30, 41, 59, 0.5); backdrop-filter: blur(10px); border: 1px solid rgba(255, 255, 255, 0.05); transition: all 0.3s ease; }
        .guild-card:hover { transform: translateY(-5px); border-color: rgba(59, 130, 246, 0.5); box-shadow: 0 10px 25px -5px rgba(59, 130, 246, 0.2); }
    </style>
</head>
<body class="min-h-screen">

    <nav class="bg-slate-900 border-b border-slate-800 sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
                <div class="flex items-center">
                    <span class="text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-blue-400 to-purple-500">Enterprise Bot</span>
                </div>
                <div class="flex items-center gap-4">
                    <div class="flex items-center gap-2">
                        <img class="h-8 w-8 rounded-full border border-slate-600" src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="User Avatar">
                        <span class="text-sm font-medium"><%= user.username %></span>
                    </div>
                    <a href="/logout" class="bg-red-500/10 text-red-500 hover:bg-red-500 hover:text-white px-3 py-1.5 rounded-md text-sm font-medium transition-colors">خروج</a>
                </div>
            </div>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
        <div class="mb-10 text-center">
            <h1 class="text-3xl font-bold mb-2">اختر السيرفر للتحكم</h1>
            <p class="text-slate-400">تظهر هنا السيرفرات التي تمتلك فيها صلاحية الإدارة (Administrator) فقط.</p>
        </div>

        <% if (guilds.length === 0) { %>
            <div class="text-center bg-slate-800/50 border border-slate-700 rounded-xl p-10 max-w-2xl mx-auto">
                <div class="text-5xl mb-4">😢</div>
                <h3 class="text-xl font-bold text-white mb-2">لا يوجد سيرفرات</h3>
                <p class="text-slate-400">يبدو أنك لا تمتلك صلاحية الإدارة في أي سيرفر يتواجد فيه البوت حالياً.</p>
            </div>
        <% } else { %>
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                <% guilds.forEach(guild => { %>
                    <div class="guild-card rounded-2xl p-6 flex flex-col items-center text-center">
                        <% if (guild.icon) { %>
                            <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png" alt="<%= guild.name %>" class="w-24 h-24 rounded-full mb-4 shadow-lg border-2 border-slate-700">
                        <% } else { %>
                            <div class="w-24 h-24 rounded-full mb-4 shadow-lg border-2 border-slate-700 bg-slate-800 flex items-center justify-center text-2xl font-bold text-slate-400">
                                <%= guild.name.charAt(0) %>
                            </div>
                        <% } %>
                        
                        <h2 class="text-xl font-bold text-white mb-2 truncate w-full"><%= guild.name %></h2>
                        
                        <div class="mt-auto pt-4 w-full">
                            <a href="/settings/<%= guild.id %>" class="block w-full py-2.5 px-4 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-colors font-medium">
                                إدارة السيرفر ⚙️
                            </a>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </main>

</body>
</html>
