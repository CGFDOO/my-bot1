<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إعدادات MNC | لوحة التحكم الشاملة</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Tajawal', sans-serif; background-color: #121212; color: #ffffff; margin: 0; display: flex; height: 100vh; overflow: hidden; }
        .mobile-header { display: none; background-color: #1e1e24; padding: 15px 20px; align-items: center; justify-content: space-between; border-bottom: 2px solid #5865F2; }
        .menu-btn { background: #5865F2; color: white; border: none; padding: 8px 15px; border-radius: 5px; cursor: pointer; }
        .sidebar { width: 260px; background-color: #1e1e24; padding: 20px; display: flex; flex-direction: column; gap: 10px; overflow-y: auto; transition: 0.3s; z-index: 1000; box-shadow: -2px 0 10px rgba(0,0,0,0.5); }
        .sidebar h2 { color: #5865F2; font-size: 22px; text-align: center; margin-bottom: 15px; }
        .close-btn { display: none; background: transparent; color: #ff4d4d; border: none; font-size: 24px; cursor: pointer; }
        .tab-btn { background: #2b2b36; color: white; border: none; padding: 15px; text-align: right; font-size: 15px; border-radius: 8px; cursor: pointer; transition: 0.3s; font-family: 'Tajawal', sans-serif; }
        .tab-btn:hover { background: #3b3b4f; }
        .tab-btn.active { background: #5865F2; font-weight: bold; }
        .content { flex: 1; padding: 30px; overflow-y: auto; background-color: #18181b; }
        .tab-content { display: none; background: #23232a; padding: 25px; border-radius: 10px; box-shadow: 0 4px 15px rgba(0,0,0,0.3); animation: fadeIn 0.4s; }
        .tab-content.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
        .form-group { margin-bottom: 20px; }
        .form-group label { display: block; margin-bottom: 8px; color: #b9bbbe; font-weight: bold; }
        .form-group input[type="text"], .form-group input[type="number"], .form-group select, .form-group textarea { width: 100%; padding: 12px; background: #2f3136; border: 1px solid #202225; color: white; border-radius: 5px; box-sizing: border-box; font-family: 'Tajawal', sans-serif; }
        .form-group textarea { resize: vertical; min-height: 80px; }
        .save-btn { background: #3ba55d; color: white; padding: 15px; border: none; border-radius: 5px; font-size: 18px; cursor: pointer; width: 100%; font-weight: bold; font-family: 'Tajawal', sans-serif; }
        .save-btn:hover { background: #2d7d46; }
        .alert { background: #3ba55d; padding: 10px; border-radius: 5px; text-align: center; margin-bottom: 20px; font-weight: bold; }
        @media (max-width: 768px) { body { flex-direction: column; overflow: auto; } .mobile-header { display: flex; } .sidebar { position: fixed; right: -300px; top: 0; height: 100%; width: 250px; } .sidebar.open { right: 0; } .close-btn { display: block; } .content { padding: 15px; } }
    </style>
</head>
<body>

    <div class="mobile-header">
        <h2 style="margin: 0; color: white;">إعدادات MNC</h2>
        <button class="menu-btn" onclick="toggleSidebar()">☰ القائمة</button>
    </div>

    <div class="sidebar" id="sidebar">
        <h2>🛠️ لوحة التحكم</h2>
        <button class="close-btn" onclick="toggleSidebar()">✖</button>
        <button class="tab-btn active" onclick="openTab(event, 'general')">⚙️ الإعدادات والحماية</button>
        <button class="tab-btn" onclick="openTab(event, 'tickets')">🎟️ التكتات الأساسية</button>
        <button class="tab-btn" onclick="openTab(event, 'roles')">👨‍⚖️ الرتب والصلاحيات</button>
        <button class="tab-btn" onclick="openTab(event, 'commands')">⌨️ الأوامر المخصصة</button>
        <button class="tab-btn" onclick="openTab(event, 'logs')">📁 السجلات والتقييمات</button>
        <button class="tab-btn" onclick="openTab(event, 'embedBuilder')" style="border: 1px solid #5865F2;">🎨 صانع الإيمبد الحر</button>
        <a href="/dashboard" style="color: #ff4d4d; text-align: center; text-decoration: none; margin-top: 20px;">⬅️ خروج</a>
    </div>

    <div class="content">
        <% if (success === 'saved') { %> <div class="alert">✅ تم حفظ الإعدادات بنجاح!</div> <% } %>
        <% if (success === 'embed_sent') { %> <div class="alert" style="background: #5865F2;">✅ تم إرسال رسالة الإيمبد إلى الروم بنجاح!</div> <% } %>
        
        <h1 style="border-bottom: 2px solid #5865F2; padding-bottom: 10px;">سيرفر: <%= guild.name %></h1>

        <form action="/settings/<%= guild.id %>" method="POST">
            
            <div id="general" class="tab-content active">
                <h3>⚙️ الإعدادات العامة والحماية</h3>
                <div class="form-group"><label>البريفكس (Prefix):</label><input type="text" name="prefix" value="<%= config.prefix %>"></div>
                <div class="form-group" style="background: #2b2b36; padding: 15px; border-radius: 8px; margin-bottom:10px;">
                    <label style="display:inline-block; margin:0;">🛡️ منع نشر الروابط (Anti-Links):</label>
                    <input type="checkbox" name="antiLinks" style="width: 20px; height: 20px; float: left;" <%= config.antiLinks ? 'checked' : '' %>>
                </div>
                <div class="form-group" style="background: #2b2b36; padding: 15px; border-radius: 8px;">
                    <label style="display:inline-block; margin:0;">🛡️ منع السبام والتكرار (Anti-Spam):</label>
                    <input type="checkbox" name="antiSpam" style="width: 20px; height: 20px; float: left;" <%= config.antiSpam ? 'checked' : '' %>>
                </div>
                <div class="form-group"><label>الرتبة التلقائية (Auto-Role):</label>
                    <select name="autoRoleId"><option value="">-- معطل --</option><% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.autoRoleId === r.id ? 'selected' : '' %>><%= r.name %></option><% }) %></select>
                </div>
                <div class="form-group"><label>روم الترحيب (Welcome):</label>
                    <select name="welcomeChannelId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c => { %><option value="<%= c.id %>" <%= config.welcomeChannelId === c.id ? 'selected' : '' %>><%= c.name %></option><% }) %></select>
                </div>
            </div>

            <div id="tickets" class="tab-content">
                <h3>🎟️ إعدادات بانر التكتات</h3>
                <div class="form-group"><label>روم لوحة التكتات الأساسية:</label><select name="panelChannelId"><option value="">-- اختر روم --</option><% channels.filter(c=>c.type===0).forEach(c => { %><option value="<%= c.id %>" <%= config.panelChannelId === c.id ? 'selected' : '' %>><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>عنوان الإيمبد:</label><input type="text" name="ticketEmbedTitle" value="<%= config.ticketEmbedTitle %>"></div>
                <div class="form-group"><label>وصف الإيمبد:</label><textarea name="ticketEmbedDesc"><%= config.ticketEmbedDesc %></textarea></div>
                <div class="form-group"><label>لون الإيمبد:</label><input type="color" name="ticketEmbedColor" value="<%= config.ticketEmbedColor %>" style="height:40px; padding:0;"></div>
                <div class="form-group"><label>رقم التكت القادم:</label><input type="number" name="ticketCount" value="<%= config.ticketCount %>"></div>
            </div>

            <div id="roles" class="tab-content">
                <h3>👨‍⚖️ الرتب والصلاحيات (4 مستويات)</h3>
                <div class="form-group"><label>👮 رتبة الإدارة الصغرى:</label><select name="adminRoleId"><option value="">-- غير محدد --</option><% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.adminRoleId === r.id ? 'selected' : '' %>><%= r.name %></option><% }) %></select></div>
                <div class="form-group"><label>👑 رتب الإدارة العليا:</label><select name="highAdminRoles" multiple style="height:80px;"><% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.highAdminRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %></select></div>
                <div class="form-group"><label>🤝 رتبة الوساطة الصغرى:</label><select name="mediatorRoleId"><option value="">-- غير محدد --</option><% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.mediatorRoleId === r.id ? 'selected' : '' %>><%= r.name %></option><% }) %></select></div>
                <div class="form-group"><label>🌟 رتب الوساطة العليا:</label><select name="highMediatorRoles" multiple style="height:80px;"><% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.highMediatorRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %></select></div>
                
                <hr style="border-color: #333; margin: 20px 0;">
                <div class="form-group" style="background: #2b2b36; padding: 15px; border-radius: 8px; border-right: 4px solid #5865F2;">
                    <label style="display:inline-block; margin:0;">🕵️‍♂️ إخفاء التكت عند الاستلام (Claim):</label>
                    <input type="checkbox" id="hideToggle" name="hideTicketOnClaim" style="width: 20px; height: 20px; float: left;" <%= config.hideTicketOnClaim ? 'checked' : '' %>>
                </div>
                <div class="form-group" style="background: #2b2b36; padding: 15px; border-radius: 8px; border-right: 4px solid #f2a658;">
                    <label style="display:inline-block; margin:0;">👀 وضع المراقبة (الإدارة ترى ولا تكتب):</label>
                    <input type="checkbox" id="readOnlyToggle" name="readOnlyStaffOnClaim" style="width: 20px; height: 20px; float: left;" <%= config.readOnlyStaffOnClaim ? 'checked' : '' %>>
                </div>
            </div>

            <div id="commands" class="tab-content">
                <h3>⌨️ أوامر الوساطة</h3>
                <div class="form-group"><label>تقييم الوسيط (Done):</label><input type="text" name="cmdDone" value="<%= config.cmdDone %>"></div>
                <div class="form-group"><label>استدعاء وساطة عليا:</label><input type="text" name="cmdReqHigh" value="<%= config.cmdReqHigh %>"></div>
                <div class="form-group"><label>تفاصيل التريد:</label><input type="text" name="cmdTrade" value="<%= config.cmdTrade %>"></div>
                
                <h3 style="margin-top: 30px;">🔨 أوامر الإدارة</h3>
                <div class="form-group"><label>مسح الشات (Clear):</label><input type="text" name="cmdClear" value="<%= config.cmdClear %>"></div>
                <div class="form-group"><label>قفل الشات (Lock):</label><input type="text" name="cmdLock" value="<%= config.cmdLock %>"></div>
                <div class="form-group"><label>سحب عضو فويس (Vmove):</label><input type="text" name="cmdVmove" value="<%= config.cmdVmove %>"></div>
            </div>

            <div id="logs" class="tab-content">
                <h3>📁 سجلات التكتات والتقييمات</h3>
                <div class="form-group"><label>الترانسكريبت (حفظ التكتات):</label><select name="transcriptChannelId"><option value="">-- غير محدد --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.transcriptChannelId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>لوج التكتات (Ticket Logs):</label><select name="ticketLogChannelId"><option value="">-- غير محدد --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.ticketLogChannelId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>روم تقييم الإدارة (تلقائي):</label><select name="staffRatingChannelId"><option value="">-- غير محدد --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.staffRatingChannelId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>روم تقييم الوسطاء:</label><select name="mediatorRatingChannelId"><option value="">-- غير محدد --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.mediatorRatingChannelId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>

                <h3 style="margin-top:30px;">🛡️ لوجات حماية السيرفر</h3>
                <div class="form-group"><label>لوج من أعطى رتبة لمين:</label><select name="logMemberRoleUpdateId"><option value="">-- غير محدد --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logMemberRoleUpdateId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>لوج تعديل الرسائل:</label><select name="logMsgUpdateId"><option value="">-- غير محدد --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logMsgUpdateId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>لوج حذف الرسائل والصور:</label><select name="logMsgDeleteId"><option value="">-- غير محدد --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logMsgDeleteId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>لوج الفويس (نقل/ديسكونكت):</label><select name="logVoiceId"><option value="">-- غير محدد --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logVoiceId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
            </div>

            <button type="submit" class="save-btn" id="mainSaveBtn">💾 حفظ الإعدادات</button>
        </form>

        <div id="embedBuilder" class="tab-content">
            <h3 style="color: #5865F2;">🎨 صانع الإيمبد الحر (مثل ProBot)</h3>
            <p style="color: #a3a6aa; font-size: 14px; margin-bottom: 20px;">أرسل رسالة إيمبد منسقة (قوانين، إعلانات، الخ) لأي روم في سيرفرك بضغطة زر.</p>
            
            <form action="/settings/<%= guild.id %>/send-embed" method="POST">
                <div class="form-group">
                    <label>الروم المراد الإرسال إليها <span style="color:red">*</span></label>
                    <select name="embedChannelId" required>
                        <option value="">-- اختر الروم --</option>
                        <% channels.filter(c=>c.type===0).forEach(c => { %>
                            <option value="<%= c.id %>"><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                <div class="form-group">
                    <label>عنوان الإيمبد (Title)</label>
                    <input type="text" name="embedTitle" placeholder="مثال: قوانين السيرفر">
                </div>
                <div class="form-group">
                    <label>وصف الإيمبد (Description)</label>
                    <textarea name="embedDesc" placeholder="اكتب النص الخاص بك هنا..."></textarea>
                </div>
                <div class="form-group">
                    <label>لون الإيمبد (Color)</label>
                    <input type="color" name="embedColor" value="#5865F2" style="height:40px; padding:0;">
                </div>
                <div class="form-group">
                    <label>رابط صورة (Image URL - اختياري)</label>
                    <input type="text" name="embedImage" placeholder="https://example.com/image.png">
                </div>
                <div class="form-group">
                    <label>النص السفلي (Footer - اختياري)</label>
                    <input type="text" name="embedFooter" placeholder="إدارة MNC">
                </div>
                <button type="submit" class="save-btn" style="background: #5865F2;">🚀 إرسال الإيمبد الآن!</button>
            </form>
        </div>

    </div>

    <script>
        function toggleSidebar() { document.getElementById("sidebar").classList.toggle("open"); }
        function openTab(evt, tabName) {
            var i, tabcontent, tablinks;
            tabcontent = document.getElementsByClassName("tab-content");
            for (i = 0; i < tabcontent.length; i++) tabcontent[i].style.display = "none";
            tablinks = document.getElementsByClassName("tab-btn");
            for (i = 0; i < tablinks.length; i++) tablinks[i].className = tablinks[i].className.replace(" active", "");
            document.getElementById(tabName).style.display = "block";
            evt.currentTarget.className += " active";
            if (window.innerWidth <= 768) toggleSidebar();

            // إخفاء زر الحفظ الأساسي لو دخلت على صانع الإيمبد
            document.getElementById("mainSaveBtn").style.display = (tabName === 'embedBuilder') ? "none" : "block";
        }

        const hideToggle = document.getElementById('hideToggle');
        const readOnlyToggle = document.getElementById('readOnlyToggle');
        function checkToggles() {
            if (hideToggle.checked) {
                readOnlyToggle.disabled = true; readOnlyToggle.checked = false; readOnlyToggle.style.opacity = '0.5';
            } else {
                readOnlyToggle.disabled = false; readOnlyToggle.style.opacity = '1';
            }
        }
        checkToggles();
        hideToggle.addEventListener('change', checkToggles);
    </script>
</body>
</html>
