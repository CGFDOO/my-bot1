<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إعدادات السيرفر | MNC System</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Tajawal', sans-serif; background-color: #121212; color: #ffffff; margin: 0; display: flex; height: 100vh; overflow: hidden; }
        
        /* 📱 القائمة الجانبية */
        .sidebar { width: 260px; background-color: #1e1e24; padding: 20px; box-shadow: -2px 0 10px rgba(0,0,0,0.5); display: flex; flex-direction: column; gap: 10px; overflow-y: auto; }
        .sidebar h2 { text-align: center; color: #5865F2; margin-bottom: 20px; font-size: 24px; border-bottom: 2px solid #333; padding-bottom: 10px; }
        .tab-btn { background: #2b2b36; color: white; border: none; padding: 15px; text-align: right; font-size: 16px; border-radius: 8px; cursor: pointer; transition: 0.3s; font-family: 'Tajawal', sans-serif; }
        .tab-btn:hover { background: #3b3b4f; }
        .tab-btn.active { background: #5865F2; font-weight: bold; }
        
        /* 📝 محتوى الصفحة */
        .content { flex: 1; padding: 30px; overflow-y: auto; background-color: #18181b; }
        .content h1 { border-bottom: 2px solid #5865F2; padding-bottom: 10px; margin-bottom: 30px; }
        .tab-content { display: none; background: #23232a; padding: 25px; border-radius: 10px; box-shadow: 0 4px 15px rgba(0,0,0,0.3); animation: fadeIn 0.4s; }
        .tab-content.active { display: block; }
        
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

        /* 🎛️ الإدخالات */
        .form-group { margin-bottom: 20px; }
        .form-group label { display: block; margin-bottom: 8px; color: #b9bbbe; font-weight: bold; }
        .form-group input, .form-group select { width: 100%; padding: 12px; background: #2f3136; border: 1px solid #202225; color: white; border-radius: 5px; font-family: 'Tajawal', sans-serif; font-size: 15px; }
        .form-group input:focus, .form-group select:focus { outline: none; border-color: #5865F2; }
        .help-text { font-size: 12px; color: #72767d; margin-top: 5px; display: block; }
        
        /* 💾 زر الحفظ */
        .save-btn { background: #3ba55d; color: white; padding: 15px 30px; border: none; border-radius: 5px; font-size: 18px; cursor: pointer; width: 100%; margin-top: 20px; transition: 0.3s; font-family: 'Tajawal', sans-serif; font-weight: bold; }
        .save-btn:hover { background: #2d7d46; }
        .back-link { display: block; text-align: center; margin-top: 15px; color: #5865F2; text-decoration: none; }
    </style>
</head>
<body>

    <div class="sidebar">
        <h2>🛠️ لوحة التحكم</h2>
        <button class="tab-btn active" onclick="openTab(event, 'tickets')">🎟️ إعدادات التكتات والإيمبد</button>
        <button class="tab-btn" onclick="openTab(event, 'roles')">👨‍⚖️ الإدارة والوساطة</button>
        <button class="tab-btn" onclick="openTab(event, 'logs')">📁 السجلات (اللوجات)</button>
        <button class="tab-btn" onclick="openTab(event, 'audit')">🛡️ حماية السيرفر</button>
        <button class="tab-btn" onclick="openTab(event, 'commands')">⌨️ الأوامر المخصصة</button>
    </div>

    <div class="content">
        <h1>إعدادات سيرفر: <%= guild.name %></h1>
        
        <form action="/settings/<%= guild.id %>" method="POST">

            <div id="tickets" class="tab-content active">
                <h3>🎟️ تخصيص التكتات والإيمبد</h3>
                <div class="form-group">
                    <label>قسم التكتات (Category):</label>
                    <select name="categoryId">
                        <option value="">-- اختر القسم --</option>
                        <% channels.filter(c => c.type === 4).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.categoryId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                <div class="form-group">
                    <label>عنوان الإيمبد الأساسي:</label>
                    <input type="text" name="ticketEmbedTitle" value="<%= config.ticketEmbedTitle || 'الدعم الفني والوساطة' %>">
                </div>
                <div class="form-group">
                    <label>وصف الإيمبد (القوانين أو الرسالة):</label>
                    <input type="text" name="ticketEmbedDesc" value="<%= config.ticketEmbedDesc || 'اضغط على الزر لفتح تذكرة جديدة.' %>">
                </div>
                <div class="form-group">
                    <label>لون الإيمبد (Hex Code):</label>
                    <input type="color" name="ticketEmbedColor" value="<%= config.ticketEmbedColor || '#0099ff' %>" style="height: 50px;">
                </div>
                <div class="form-group">
                    <label>رقم التكت القادم (Counter):</label>
                    <input type="number" name="ticketCount" value="<%= config.ticketCount %>">
                </div>
            </div>

            <div id="roles" class="tab-content">
                <h3>👨‍⚖️ نظام الإدارة والوساطة</h3>
                <div class="form-group">
                    <label>رتبة الإدارة (Staff):</label>
                    <select name="staffRoleId">
                        <option value="">-- اختر رتبة --</option>
                        <% roles.forEach(role => { %>
                            <option value="<%= role.id %>" <%= config.staffRoleId === role.id ? 'selected' : '' %>><%= role.name %></option>
                        <% }) %>
                    </select>
                    <span class="help-text">هذه الرتبة ستتمكن من رؤية التكتات.</span>
                </div>
                <div class="form-group">
                    <label>رتب الإدارة العليا (Multi-Admin) [للموافقات]:</label>
                    <select name="adminRoles" multiple style="height: 150px;">
                        <% roles.forEach(role => { %>
                            <option value="<%= role.id %>" <%= config.adminRoles && config.adminRoles.includes(role.id) ? 'selected' : '' %>><%= role.name %></option>
                        <% }) %>
                    </select>
                    <span class="help-text">اضغط CTRL لاختيار أكثر من رتبة. هؤلاء سيتم عمل منشن لهم عند طلب موافقة على تريد.</span>
                </div>
            </div>

            <div id="logs" class="tab-content">
                <h3>📁 السجلات والتقييمات (اللوجات)</h3>
                <div class="form-group">
                    <label>روم الترانسكريبت (حفظ التكتات):</label>
                    <select name="transcriptChannelId">
                        <option value="">-- غير محدد --</option>
                        <% channels.filter(c => c.type === 0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.transcriptChannelId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                <div class="form-group">
                    <label>روم تقييمات الإدارة (Staff Rating):</label>
                    <select name="staffRatingChannelId">
                        <option value="">-- غير محدد --</option>
                        <% channels.filter(c => c.type === 0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.staffRatingChannelId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                </div>

            <div id="audit" class="tab-content">
                <h3>🛡️ حماية ولوجات السيرفر (Audit Logs)</h3>
                <div class="form-group">
                    <label>لوج إنشاء/حذف الرتب:</label>
                    <select name="logRoleCreateId">
                        <option value="">-- غير محدد --</option>
                        <% channels.filter(c => c.type === 0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.logRoleCreateId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                <div class="form-group">
                    <label>لوج دخول وخروج الأعضاء:</label>
                    <select name="logJoinLeaveId">
                        <option value="">-- غير محدد --</option>
                        <% channels.filter(c => c.type === 0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.logJoinLeaveId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
            </div>

            <div id="commands" class="tab-content">
                <h3>⌨️ تخصيص الأوامر (Custom Commands)</h3>
                <div class="form-group">
                    <label>أمر إغلاق التكت (Done):</label>
                    <input type="text" name="cmdDone" value="<%= config.cmdDone || '!done' %>">
                </div>
                <div class="form-group">
                    <label>أمر طلب الموافقة (Approve):</label>
                    <input type="text" name="cmdApprove" value="<%= config.cmdApprove || '!approve' %>">
                </div>
                <div class="form-group">
                    <label>أمر سحب الإدارة (Come):</label>
                    <input type="text" name="cmdCome" value="<%= config.cmdCome || '!come' %>">
                </div>
            </div>

            <button type="submit" class="save-btn">💾 حفظ التعديلات في السيرفر</button>
            <a href="/dashboard" class="back-link">⬅️ العودة لقائمة السيرفرات</a>
        </form>
    </div>

    <script>
        function openTab(evt, tabName) {
            var i, tabcontent, tablinks;
            tabcontent = document.getElementsByClassName("tab-content");
            for (i = 0; i < tabcontent.length; i++) { tabcontent[i].style.display = "none"; }
            tablinks = document.getElementsByClassName("tab-btn");
            for (i = 0; i < tablinks.length; i++) { tablinks[i].className = tablinks[i].className.replace(" active", ""); }
            document.getElementById(tabName).style.display = "block";
            evt.currentTarget.className += " active";
        }
    </script>
</body>
</html>
