<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SYSTEM CONTROL | إعدادات الإمبراطورية</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;800;900&display=swap" rel="stylesheet">
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { 
                        sans: ['Tajawal', 'sans-serif'] 
                    },
                    colors: { 
                        brand: { DEFAULT: '#5865F2', dark: '#4752C4', light: '#7289DA' }, 
                        dark: { 900: '#0b0f19', 800: '#151b2b', 700: '#1f2937', 600: '#334155' } 
                    }
                }
            }
        }
    </script>

    <style>
        body { background-color: #0b0f19; color: #f8fafc; overflow-x: hidden; scroll-behavior: smooth; }
        ::-webkit-scrollbar { width: 8px; height: 8px; }
        ::-webkit-scrollbar-track { background: #0b0f19; }
        ::-webkit-scrollbar-thumb { background: #1f2937; border-radius: 10px; }
        ::-webkit-scrollbar-thumb:hover { background: #5865F2; }
        
        .glass-card { background: rgba(21, 27, 43, 0.8); backdrop-filter: blur(20px); border: 1px solid rgba(255, 255, 255, 0.05); box-shadow: 0 10px 40px -10px rgba(0,0,0,0.6); }
        .input-glow:focus { box-shadow: 0 0 0 3px rgba(88, 101, 242, 0.3); border-color: #5865F2; outline: none; }
        .settings-section { display: none; animation: fadeIn 0.4s ease forwards; }
        .settings-section.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(15px); } to { opacity: 1; transform: translateY(0); } }

        .switch { position: relative; display: inline-block; width: 54px; height: 28px; }
        .switch input { opacity: 0; width: 0; height: 0; }
        .slider { position: absolute; cursor: pointer; top: 0; left: 0; right: 0; bottom: 0; background-color: #374151; transition: .4s; border-radius: 34px; }
        .slider:before { position: absolute; content: ""; height: 20px; width: 20px; right: 4px; bottom: 4px; background-color: white; transition: .4s; border-radius: 50%; }
        input:checked + .slider { background-color: #10B981; }
        input:checked + .slider:before { transform: translateX(-26px); }

        input[type="radio"] { appearance: auto; width: 1.2rem; height: 1.2rem; accent-color: #5865F2; cursor: pointer; }
        @keyframes popDown { 0% { top: -100px; opacity: 0; } 100% { top: 20px; opacity: 1; } }
        .pop-down-alert { animation: popDown 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards; }
    </style>
</head>

<% 
    let textChannels = []; let voiceChannels = []; let categories = []; let serverRoles = [];
    let guildName = "Unknown Server"; let guildIconUrl = "https://cdn.discordapp.com/embed/avatars/0.png";
    if (bot && guildId) {
        const currentGuild = bot.guilds.cache.get(guildId);
        if (currentGuild) {
            guildName = currentGuild.name;
            if (currentGuild.icon) guildIconUrl = `https://cdn.discordapp.com/icons/${guildId}/${currentGuild.icon}.png`;
            textChannels = currentGuild.channels.cache.filter(c => c.type === 0 || c.type === 5 || c.type === 15).sort((a,b) => a.position - b.position).map(c => ({ id: c.id, name: c.name }));
            categories = currentGuild.channels.cache.filter(c => c.type === 4).sort((a,b) => a.position - b.position).map(c => ({ id: c.id, name: c.name }));
            serverRoles = currentGuild.roles.cache.filter(r => r.id !== guildId).sort((a,b) => b.position - a.position).map(r => ({ id: r.id, name: r.name }));
        }
    }
%>

<body class="flex flex-col md:flex-row h-screen overflow-hidden selection:bg-brand selection:text-white relative">

    <% if (typeof success !== 'undefined' && success) { %>
        <div class="fixed left-1/2 transform -translate-x-1/2 z-[200] bg-emerald-500 text-white px-8 py-4 rounded-2xl flex items-center shadow-[0_10px_40px_rgba(16,185,129,0.5)] border-2 border-emerald-400 pop-down-alert cursor-pointer" onclick="this.style.display='none'">
            <span class="text-3xl ml-4">✅</span>
            <div><h4 class="font-black text-xl" data-en="Changes Saved Successfully!">تم حفظ التغييرات بنجاح!</h4></div>
        </div>
    <% } %>

    <header class="md:hidden flex items-center justify-between bg-dark-800 border-b border-dark-700 p-4 z-40 shadow-md">
        <div class="flex items-center space-x-3 space-x-reverse">
            <img src="<%= guildIconUrl %>" class="w-10 h-10 rounded-lg border border-dark-600 object-cover">
            <h1 class="text-lg font-black text-white">SYSTEM <span class="text-brand">CONTROL</span></h1>
        </div>
        <button onclick="toggleMobileSidebar()" class="text-white bg-dark-700 p-2 rounded-lg hover:bg-dark-600 focus:outline-none transition-colors">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
        </button>
    </header>

    <aside id="sidebar" class="fixed inset-y-0 right-0 transform translate-x-full md:relative md:translate-x-0 transition duration-300 ease-in-out w-72 flex-shrink-0 border-l border-dark-700 bg-dark-800 flex flex-col z-[60] shadow-[5px_0_20px_rgba(0,0,0,0.5)]">
        <button onclick="toggleMobileSidebar()" class="md:hidden absolute top-4 left-4 text-red-400 bg-red-500/10 p-3 rounded-lg hover:bg-red-500 hover:text-white transition-colors border border-red-500/30 z-[70]">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M6 18L18 6M6 l12 12"></path></svg>
        </button>

        <div class="h-24 flex flex-col items-center justify-center border-b border-dark-700 bg-dark-900/60 relative overflow-hidden">
            <h1 class="text-2xl font-black text-white tracking-wider relative z-10" data-en="SYSTEM CONTROL">SYSTEM <span class="text-brand">CONTROL</span></h1>
        </div>
        
        <nav class="flex-1 overflow-y-auto py-6 px-4 space-y-2 custom-scrollbar" id="sidebar-nav">
            <div class="text-[11px] font-black text-slate-500 uppercase tracking-widest mb-3 px-2" data-en="Basics & AI">الأساسيات والذكاء</div>
            <button onclick="switchSection('general')" class="nav-btn active-nav w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all bg-brand text-white font-bold" data-target="general">
                <span class="text-lg">⚙️</span> <span data-en="General & Colors">الإعدادات والألوان</span>
            </button>
            <button onclick="switchSection('ai_system')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-amber-400 hover:bg-dark-700 font-bold" data-target="ai_system">
                <span class="text-lg">✨</span> <span data-en="AI System">الذكاء الاصطناعي (AI)</span>
            </button>
            <button onclick="switchSection('live_embed')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="live_embed">
                <span class="text-lg">🎨</span> <span data-en="Live Embed">صانع الإيمبد الفوري</span>
            </button>

            <div class="text-[11px] font-black text-slate-500 uppercase tracking-widest mb-3 mt-8 px-2" data-en="Tickets & Middleman">التذاكر والوساطة</div>
            <button onclick="switchSection('middleman')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="middleman">
                <span class="text-lg">⚖️</span> <span data-en="Middleman System">نظام الوساطة (المعزول)</span>
            </button>
            <button onclick="switchSection('custom_panels')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="custom_panels">
                <span class="text-lg">🎫</span> <span data-en="Dynamic Panels">البانلات الديناميكية</span>
            </button>
            <button onclick="switchSection('ticket_controls')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="ticket_controls">
                <span class="text-lg">🛠️</span> <span data-en="Ticket Controls">التحكم الداخلي للتكت</span>
            </button>

            <div class="text-[11px] font-black text-slate-500 uppercase tracking-widest mb-3 mt-8 px-2" data-en="Management">الإدارة والعقوبات</div>
            <button onclick="switchSection('system_points')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-emerald-400 hover:bg-dark-700 font-bold" data-target="system_points">
                <span class="text-lg">🎯</span> <span data-en="System Points">نقاط الإدارة (System Points)</span>
            </button>
            <button onclick="switchSection('warnings')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-orange-400 hover:bg-dark-700 font-bold" data-target="warnings">
                <span class="text-lg">⚠️</span> <span data-en="Warnings Panel">نظام التحذيرات وبانل الإدارة</span>
            </button>
            <button onclick="switchSection('commands')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="commands">
                <span class="text-lg">⌨️</span> <span data-en="Custom Commands">تخصيص الأوامر (Aliases)</span>
            </button>
            <button onclick="switchSection('auto_responders')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-fuchsia-400 hover:bg-dark-700 font-bold" data-target="auto_responders">
                <span class="text-lg">💬</span> <span data-en="Auto Responders & Line">الردود التلقائية والخط</span>
            </button>
            <button onclick="switchSection('logs')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="logs">
                <span class="text-lg">📝</span> <span data-en="Server Logs">السجلات الشاملة (Logs)</span>
            </button>
            <button onclick="switchSection('roles')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="roles">
                <span class="text-lg">👑</span> <span data-en="Roles & Permissions">الرتب والصلاحيات العليا</span>
            </button>
            <button onclick="switchSection('ratings')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="ratings">
                <span class="text-lg">⭐</span> <span data-en="Ratings">التقييمات المزدوجة</span>
            </button>

            <div class="text-[11px] font-black text-slate-500 uppercase tracking-widest mb-3 mt-8 px-2" data-en="Protection & Interaction">الحماية والتفاعل</div>
            <button onclick="switchSection('protection')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="protection">
                <span class="text-lg">🛡️</span> <span data-en="Anti-Nuke">الحماية والـ Anti-Nuke</span>
            </button>
            <button onclick="switchSection('welcome')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="welcome">
                <span class="text-lg">👋</span> <span data-en="Welcome & Leave">الترحيب والمغادرة</span>
            </button>
            <button onclick="switchSection('economy')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="economy">
                <span class="text-lg">💰</span> <span data-en="Economy & Daily">الاقتصاد والدعوات (Invites)</span>
            </button>
            <button onclick="switchSection('leveling')" class="nav-btn w-full flex items-center space-x-3 space-x-reverse px-4 py-3.5 rounded-xl transition-all text-slate-400 hover:bg-dark-700 hover:text-white font-bold" data-target="leveling">
                <span class="text-lg">📈</span> <span data-en="Leveling & Tops">نظام اللفلات والتوب</span>
            </button>
        </nav>
    </aside>

    <div id="sidebar-overlay" onclick="toggleMobileSidebar()" class="fixed inset-0 bg-black/80 z-40 hidden md:hidden backdrop-blur-sm transition-all"></div>

    <main class="flex-1 flex flex-col h-[calc(100vh-72px)] md:h-screen relative bg-dark-900">
        <header class="hidden md:flex h-24 flex-shrink-0 border-b border-dark-700 bg-dark-800/80 backdrop-blur-xl items-center px-10 z-10 shadow-md relative">
            <div class="flex items-center space-x-5 space-x-reverse z-10">
                <img src="<%= guildIconUrl %>" class="w-14 h-14 rounded-xl border border-dark-600 shadow-lg object-cover">
                <div>
                    <h2 class="text-2xl font-black text-white tracking-wide"><%= guildName %></h2>
                    <p class="text-xs text-emerald-400 mt-1 font-bold tracking-widest">SYSTEM ACTIVE 🟢 (ULTIMATE EDITION)</p>
                </div>
            </div>
        </header>

        <button type="button" onclick="submitMegaForm()" class="fixed bottom-8 left-8 z-[100] flex items-center px-8 py-5 bg-emerald-500 hover:bg-emerald-400 text-dark-900 text-xl font-black rounded-2xl shadow-[0_0_40px_rgba(16,185,129,0.4)] transition-all transform hover:-translate-y-2 hover:scale-105 border border-emerald-400/50">
            <span class="mr-3 ml-2 text-3xl">💾</span> <span id="save-btn-text" data-en="Save Settings Now">حفظ التعديلات الآن</span>
        </button>

        <div class="flex-1 overflow-y-auto p-4 md:p-10 z-10 custom-scrollbar relative" id="main-scroll-area">
            <form action="/settings/<%= guildId %>/save" method="POST" id="mega-settings-form">
                
                <div id="hidden-data-container" style="display: none;">
                    <input type="hidden" name="warn_reasonsDataAr" id="warn_reasonsDataAr">
                    <input type="hidden" name="warn_reasonsDataEn" id="warn_reasonsDataEn">
                    <input type="hidden" name="mm_modalFieldsData" id="mm_modalFieldsData">
                    <input type="hidden" name="ticketPanelsData" id="ticketPanelsData">
                    <input type="hidden" name="lvl_roleRewardsData" id="lvl_roleRewardsData">
                    <input type="hidden" name="autoRespondersData" id="autoRespondersData">
                </div>

                <div id="section-general" class="settings-section active max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-white mb-6 border-b border-dark-700 pb-4" data-en="General Settings & Colors ⚙️">الإعدادات العامة والألوان ⚙️</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-brand">
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-10">
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-3" data-en="Bot Prefix">البادئة (Prefix)</label>
                                <input type="text" name="prefix" value="<%= config.prefix || '!' %>" required class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-5 text-white input-glow font-mono text-2xl text-center font-black">
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-3" data-en="Dashboard Language">لغة النظام الأساسية للداشبورد</label>
                                <select name="language" id="system-language" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-4 px-5 text-white input-glow font-bold text-lg cursor-pointer">
                                    <option value="ar" <%= config.language === 'ar' ? 'selected' : '' %>>🇸🇦 العربية (Arabic)</option>
                                    <option value="en" <%= config.language === 'en' ? 'selected' : '' %>>🇺🇸 الإنجليزية (English)</option>
                                </select>
                            </div>
                        </div>

                        <div class="bg-dark-900/50 p-6 rounded-2xl border border-dark-700 mb-10 shadow-inner flex items-center justify-between">
                            <div>
                                <h3 class="text-lg font-bold text-brand-light mb-1" data-en="Enable Slash Commands (/)">تفعيل أوامر السلاش (/)</h3>
                                <p class="text-xs text-slate-400" data-en="Allow bot to use modern slash commands seamlessly.">تفعيل استخدام الأوامر بنظام الديسكورد الجديد لجميع الأوامر.</p>
                            </div>
                            <label class="switch">
                                <input type="checkbox" name="slashCommandsEnabled" <%= config.slashCommandsEnabled ? 'checked' : '' %>>
                                <span class="slider"></span>
                            </label>
                        </div>

                        <h3 class="text-2xl font-bold text-white mb-6" data-en="🎨 Global Embed Style">🎨 توحيد أشكال الإيمبد (Global Embeds)</h3>
                        
                        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-dark-700">
                                <label class="block text-sm font-bold text-brand-light mb-3" data-en="ℹ️ Primary Color">ℹ️ اللون الأساسي</label>
                                <div class="flex items-center space-x-3 space-x-reverse">
                                    <input type="color" id="emb_primaryColor_picker" value="<%= config.embedSetup?.primaryColor || '#5865F2' %>" class="w-14 h-12 rounded-xl bg-dark-900 border border-dark-600 p-1 cursor-pointer" onchange="document.getElementById('emb_primaryColor').value = this.value">
                                    <input type="text" id="emb_primaryColor" name="emb_primaryColor" value="<%= config.embedSetup?.primaryColor || '#5865F2' %>" class="flex-1 bg-dark-800 border border-dark-600 rounded-xl py-3 px-3 text-white font-mono uppercase text-center" oninput="document.getElementById('emb_primaryColor_picker').value = this.value">
                                </div>
                            </div>
                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-dark-700">
                                <label class="block text-sm font-bold text-emerald-400 mb-3" data-en="✅ Success Color">✅ لون النجاح</label>
                                <div class="flex items-center space-x-3 space-x-reverse">
                                    <input type="color" id="emb_successColor_picker" value="<%= config.embedSetup?.successColor || '#3ba55d' %>" class="w-14 h-12 rounded-xl bg-dark-900 border border-dark-600 p-1 cursor-pointer" onchange="document.getElementById('emb_successColor').value = this.value">
                                    <input type="text" id="emb_successColor" name="emb_successColor" value="<%= config.embedSetup?.successColor || '#3ba55d' %>" class="flex-1 bg-dark-800 border border-dark-600 rounded-xl py-3 px-3 text-white font-mono uppercase text-center" oninput="document.getElementById('emb_successColor_picker').value = this.value">
                                </div>
                            </div>
                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-dark-700">
                                <label class="block text-sm font-bold text-red-400 mb-3" data-en="❌ Error Color">❌ لون الخطأ</label>
                                <div class="flex items-center space-x-3 space-x-reverse">
                                    <input type="color" id="emb_errorColor_picker" value="<%= config.embedSetup?.errorColor || '#ed4245' %>" class="w-14 h-12 rounded-xl bg-dark-900 border border-dark-600 p-1 cursor-pointer" onchange="document.getElementById('emb_errorColor').value = this.value">
                                    <input type="text" id="emb_errorColor" name="emb_errorColor" value="<%= config.embedSetup?.errorColor || '#ed4245' %>" class="flex-1 bg-dark-800 border border-dark-600 rounded-xl py-3 px-3 text-white font-mono uppercase text-center" oninput="document.getElementById('emb_errorColor_picker').value = this.value">
                                </div>
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <div class="md:col-span-2">
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Global Footer Text">نص الفوتر الموحد (Footer Text)</label>
                                <input type="text" name="emb_footerText" value="<%= config.embedSetup?.footerText || 'System Control ©' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white input-glow font-bold">
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Footer Icon URL">رابط أيقونة الفوتر (Footer Icon)</label>
                                <input type="text" name="emb_footerIconUrl" value="<%= config.embedSetup?.footerIconUrl || '' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white input-glow text-left font-mono text-sm" dir="ltr" placeholder="https://...">
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Thumbnail URL">رابط الصورة الجانبية (Thumbnail)</label>
                                <input type="text" name="emb_thumbnailUrl" value="<%= config.embedSetup?.thumbnailUrl || '' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white input-glow text-left font-mono text-sm" dir="ltr" placeholder="https://...">
                            </div>
                        </div>
                    </div>
                </div>

                <div id="section-ai_system" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-amber-400 mb-6 border-b border-dark-700 pb-4" data-en="Advanced AI System ✨">الذكاء الاصطناعي المتقدم ✨</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-amber-400 shadow-[0_0_30px_rgba(251,191,36,0.1)]">
                        <div class="flex flex-col md:flex-row items-start md:items-center justify-between bg-dark-900/80 p-5 rounded-2xl border border-amber-500/30 mb-8 gap-4">
                            <div>
                                <h3 class="text-xl font-bold text-white mb-1" data-en="🤖 Enable AI Chat System">🤖 تفعيل نظام الذكاء الاصطناعي</h3>
                                <p class="text-sm text-slate-400" data-en="Bot will reply natively in specific channel, or instantly when mentioned/replied to.">يجعل البوت يرد في الروم المخصصة، أو فوراً عند عمل منشن (Ping) له أو ريبلاي.</p>
                            </div>
                            <label class="switch">
                                <input type="checkbox" name="ai_enabled" id="ai_enabled" <%= config.aiSystem?.enabled !== false ? 'checked' : '' %>>
                                <span class="slider"></span>
                            </label>
                        </div>

                        <div class="flex items-center justify-between p-5 bg-dark-800 rounded-2xl border border-dark-600 mb-8">
                            <div>
                                <h3 class="text-lg font-bold text-brand-light" data-en="Allow User to Choose Persona">السماح للعضو باختيار الشخصية</h3>
                            </div>
                            <label class="switch">
                                <input type="checkbox" name="ai_allowUserChoice" id="ai_allowUserChoice" <%= config.aiSystem?.allowUserChoice !== false ? 'checked' : '' %>>
                                <span class="slider"></span>
                            </label>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-dark-700">
                                <h4 class="text-blue-400 font-bold mb-4" data-en="👦 Boy Persona">👦 إعدادات شخصية الولد</h4>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Bot Name">اسم البوت</label>
                                <input type="text" name="ai_defaultBoyName" value="<%= config.aiSystem?.defaultBoyName || '' %>" placeholder="يرجى كتابة اسم للبوت..." class="w-full bg-dark-800 border border-dark-600 rounded-xl py-3 px-4 text-white input-glow font-bold">
                            </div>
                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-dark-700">
                                <h4 class="text-pink-400 font-bold mb-4" data-en="👧 Girl Persona">👧 إعدادات شخصية البنت</h4>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Bot Name">اسم البوت</label>
                                <input type="text" name="ai_defaultGirlName" value="<%= config.aiSystem?.defaultGirlName || '' %>" placeholder="يرجى كتابة اسم للبوت..." class="w-full bg-dark-800 border border-dark-600 rounded-xl py-3 px-4 text-white input-glow font-bold">
                            </div>
                        </div>

                        <div class="bg-dark-900/80 p-5 rounded-2xl border border-dark-700">
                            <label class="block text-sm font-bold text-slate-300 mb-3 text-lg" data-en="AI Chat Channel">روم الدردشة مع الذكاء الاصطناعي</label>
                            <select name="ai_chatChannelId" class="w-full bg-dark-800 border border-dark-600 rounded-xl py-4 px-4 text-amber-50 input-glow cursor-pointer font-bold text-lg">
                                <option value="" data-en="-- Select Channel --">-- اختر الروم ليتحدث فيها الأعضاء --</option>
                                <% textChannels.forEach(ch => { %> 
                                    <option value="<%= ch.id %>" <%= config.aiSystem?.chatChannelId === ch.id ? 'selected' : '' %>>💬 <%= ch.name %></option> 
                                <% }) %>
                            </select>
                        </div>
                    </div>
                </div>

                <div id="section-live_embed" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-white mb-6 border-b border-dark-700 pb-4" data-en="Live Embed Maker 🎨">صانع الإيمبد الفوري 🎨</h2>
                    <div class="glass-card rounded-3xl p-6 md:p-8 border-t-4 border-t-fuchsia-500 shadow-lg">
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-6">
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-3" data-en="Target Channel">اختر الروم للإرسال</label>
                                <select id="live_embed_channel" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3.5 px-4 text-white input-glow font-bold cursor-pointer">
                                    <option value="" data-en="-- Select Channel --">-- اختر الروم --</option>
                                    <% textChannels.forEach(ch => { %>
                                        <option value="<%= ch.id %>">💬 <%= ch.name %></option>
                                    <% }) %>
                                </select>
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-3" data-en="Embed Color">لون الإيمبد</label>
                                <div class="flex items-center space-x-3 space-x-reverse">
                                    <input type="color" id="live_embed_color_picker" value="#5865F2" class="w-14 h-12 rounded-xl cursor-pointer bg-dark-900 border border-dark-700 p-1" onchange="document.getElementById('live_embed_color').value = this.value">
                                    <input type="text" id="live_embed_color" value="#5865F2" class="flex-1 bg-dark-900 border border-dark-700 rounded-xl py-3.5 px-4 text-white input-glow font-mono uppercase text-center">
                                </div>
                            </div>
                        </div>

                        <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Title">العنوان (Title)</label>
                        <input type="text" id="live_embed_title" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white input-glow font-bold text-lg mb-6" placeholder="اكتب عنوان الإيمبد...">
                        
                        <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Description">الوصف / القوانين (يدعم الزخارف)</label>
                        <textarea id="live_embed_desc" rows="6" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-4 px-5 text-white input-glow resize-y mb-6" placeholder="اكتب محتوى الإيمبد هنا..."></textarea>
                        
                        <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Image URL (Optional)">رابط صورة (اختياري)</label>
                        <input type="text" id="live_embed_image" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white input-glow text-left font-mono mb-8" dir="ltr" placeholder="https://...">
                        
                        <div id="live_embed_status" class="hidden mb-6 px-5 py-4 rounded-xl font-bold text-center"></div>
                        
                        <button type="button" onclick="sendLiveEmbed('<%= guildId %>')" class="w-full py-4 bg-fuchsia-600 hover:bg-fuchsia-500 text-white font-black text-xl rounded-xl transition-all shadow-lg" data-en="🚀 Send Embed to Discord Now!">
                            🚀 إرسال الإيمبد للديسكورد الآن!
                        </button>
                    </div>
                </div>

                <div id="section-middleman" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-white mb-6 border-b border-dark-700 pb-4" data-en="Middleman System ⚖️">نظام الوساطة المعزول ⚖️</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-amber-500 shadow-lg">
                        <div class="flex flex-col md:flex-row items-start md:items-center justify-between bg-dark-900/50 p-6 rounded-2xl border border-dark-700 mb-8 gap-4">
                            <div>
                                <h3 class="text-xl font-bold text-white mb-1" data-en="Enable Middleman System">تفعيل نظام الوساطة</h3>
                                <p class="text-sm text-slate-400" data-en="Relies on the !done command for rating separately.">يعتمد على التقييم بأمر !done بشكل منفصل.</p>
                            </div>
                            <label class="switch">
                                <input type="checkbox" name="mm_enabled" <%= config.middlemanSystem?.enabled ? 'checked' : '' %>>
                                <span class="slider"></span>
                            </label>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Ticket Category">قسم التذاكر (Category)</label>
                                <select name="mm_categoryId" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3.5 px-4 text-white font-bold cursor-pointer input-glow">
                                    <option value="" data-en="-- Select Category --">-- اختر القسم --</option>
                                    <% categories.forEach(cat => { %> 
                                        <option value="<%= cat.id %>" <%= config.middlemanSystem?.categoryId === cat.id ? 'selected' : '' %>>📂 <%= cat.name %></option> 
                                    <% }) %>
                                </select>
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Panel Channel">روم إرسال البانل الخارجي</label>
                                <select name="mm_panelChannelId" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3.5 px-4 text-white font-bold cursor-pointer input-glow">
                                    <option value="" data-en="-- Select Channel --">-- اختر الروم --</option>
                                    <% textChannels.forEach(ch => { %> 
                                        <option value="<%= ch.id %>" <%= config.middlemanSystem?.panelChannelId === ch.id ? 'selected' : '' %>>💬 <%= ch.name %></option> 
                                    <% }) %>
                                </select>
                            </div>
                        </div>

                        <h3 class="text-2xl font-bold text-white mb-6 border-b border-dark-700 pb-2" data-en="🖼️ Outer Panel Design">🖼️ تصميم البانل الخارجي</h3>
                        
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Outer Panel Title">عنوان الإيمبد الخارجي</label>
                                <input type="text" name="mm_panelTitle" value="<%= config.middlemanSystem?.panelTitle || 'تذكرة وساطة آمنة' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white font-bold input-glow">
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Outer Panel Color">لون الإيمبد الخارجي</label>
                                <div class="flex items-center space-x-3 space-x-reverse">
                                    <input type="color" id="mm_panelColor_picker" value="<%= config.middlemanSystem?.panelColor || '#f2a658' %>" class="w-14 h-12 rounded-xl bg-dark-900 border border-dark-600 p-1 cursor-pointer" onchange="document.getElementById('mm_panelColor').value = this.value">
                                    <input type="text" id="mm_panelColor" name="mm_panelColor" value="<%= config.middlemanSystem?.panelColor || '#f2a658' %>" class="flex-1 bg-dark-900 border border-dark-600 rounded-xl py-3 px-4 text-white font-mono uppercase text-center input-glow">
                                </div>
                            </div>
                            <div class="md:col-span-2">
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Outer Panel Description">وصف الإيمبد الخارجي</label>
                                <textarea name="mm_panelDescription" rows="3" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-4 px-4 text-white resize-y input-glow"><%= config.middlemanSystem?.panelDescription || 'لطلب وسيط معتمد، يرجى فتح تذكرة من هنا.' %></textarea>
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Button Label">اسم الزر (Button Label)</label>
                                <input type="text" name="mm_buttonLabel" value="<%= config.middlemanSystem?.buttonLabel || 'طلب وسيط 🛡️' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white font-bold input-glow">
                            </div>
                        </div>
                        
                        <h3 class="text-2xl font-bold text-white mb-6 border-b border-dark-700 pb-2" data-en="📝 Modal & Inner Ticket Design">📝 النافذة (Modal) والإيمبد الداخلي</h3>
                        
                        <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Modal Title">عنوان النافذة المنبثقة</label>
                        <input type="text" name="mm_modalTitle" value="<%= config.middlemanSystem?.modalTitle || 'بيانات الوساطة' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white font-bold mb-6 input-glow">
                        
                        <div class="bg-dark-900/50 p-6 rounded-2xl border border-dark-700 mb-6 shadow-inner">
                            <h4 class="text-lg font-bold text-brand-light mb-4" data-en="Modal Questions">أسئلة النافذة</h4>
                            <div id="mm-fields-ui-container" class="space-y-4 mb-6"></div>
                            <button type="button" onclick="addMmFieldUI()" class="w-full py-4 bg-emerald-600/10 hover:bg-emerald-600/30 text-emerald-400 font-bold rounded-xl border-2 border-dashed border-emerald-500 transition-all" data-en="➕ Add Question to Modal">
                                ➕ إضافة سؤال للنافذة
                            </button>
                        </div>
                        
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Inner Ticket Title">عنوان الإيمبد الداخلي للتذكرة</label>
                                <input type="text" name="mm_insideTicketTitle" value="<%= config.middlemanSystem?.insideTicketTitle || 'تذكرة الوساطة' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white font-bold input-glow">
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Inner Ticket Color">لون الإيمبد الداخلي للتذكرة</label>
                                <div class="flex items-center space-x-3 space-x-reverse">
                                    <input type="color" id="mm_insideTicketColor_picker" value="<%= config.middlemanSystem?.insideTicketColor || '#f2a658' %>" class="w-14 h-12 rounded-xl bg-dark-900 border border-dark-600 p-1 cursor-pointer" onchange="document.getElementById('mm_insideTicketColor').value = this.value">
                                    <input type="text" id="mm_insideTicketColor" name="mm_insideTicketColor" value="<%= config.middlemanSystem?.insideTicketColor || '#f2a658' %>" class="flex-1 bg-dark-900 border border-dark-600 rounded-xl py-3 px-4 text-white font-mono uppercase text-center input-glow">
                                </div>
                            </div>
                            <div class="md:col-span-2">
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Inner Ticket Description (Supports {user})">وصف الإيمبد الداخلي (يدعم {user})</label>
                                <textarea name="mm_insideTicketDescription" rows="3" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-4 px-4 text-white resize-y input-glow"><%= config.middlemanSystem?.insideTicketDescription || 'أهلاً بك {user}، يرجى انتظار الوسيط.' %></textarea>
                            </div>
                            
                            <div class="md:col-span-2 bg-dark-800 p-6 rounded-xl border border-dark-600 shadow-lg">
                                <label class="block text-sm font-bold text-amber-400 mb-3" data-en="🎨 Modal Answers Embed Color (Code Blocks)">🎨 لون إيمبد إجابات النافذة المخصص (المربعات - Code Blocks)</label>
                                <div class="flex items-center space-x-4 space-x-reverse">
                                    <input type="color" id="mm_modalAnswersEmbedColor_picker" value="<%= config.middlemanSystem?.modalAnswersEmbedColor || '#f2a658' %>" class="w-16 h-12 rounded-xl bg-dark-900 border border-dark-600 p-1 cursor-pointer" onchange="document.getElementById('mm_modalAnswersEmbedColor').value = this.value">
                                    <input type="text" id="mm_modalAnswersEmbedColor" name="mm_modalAnswersEmbedColor" value="<%= config.middlemanSystem?.modalAnswersEmbedColor || '#f2a658' %>" class="flex-1 bg-dark-900 border border-dark-600 rounded-xl py-3 px-4 text-white font-mono uppercase text-center input-glow">
                                </div>
                                <p class="text-xs text-slate-400 mt-3" data-en="This is the specific embed color where the user's answers will be displayed in Code Blocks.">الإيمبد الذي ستظهر بداخله المربعات الخاصة بإجابات العضو.</p>
                            </div>
                        </div>
                    </div>
                </div>

<div id="section-custom_panels" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-white mb-6 border-b border-dark-700 pb-4" data-en="Dynamic Ticket Panels 🎫">البانلات الديناميكية 🎫</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-blue-500 shadow-lg">
                        <div class="bg-blue-900/20 border border-blue-500/30 p-4 rounded-xl mb-8">
                            <p class="text-sm text-blue-300 font-bold flex items-center">
                                <span class="mr-2 ml-2 text-xl">💡</span>
                                يمكنك إنشاء عدد لا نهائي من بانلات التذاكر وكل بانل يحتوي على أزرار خاصة به! (سيتم احتسابها تلقائياً في السيستم بوينت).
                            </p>
                        </div>

                        <div id="dynamic-panels-container" class="space-y-10"></div>
                        
                        <button type="button" onclick="addNewPanelUI()" class="mt-10 flex items-center justify-center w-full px-8 py-5 bg-blue-600 hover:bg-blue-500 text-white font-black text-xl rounded-2xl shadow-[0_10px_30px_rgba(37,99,235,0.4)] transition-all transform hover:-translate-y-1" data-en="➕ Add New Ticket Panel">
                            <span class="text-3xl mr-3">➕</span> إضافة بانل تذاكر جديد
                        </button>
                    </div>
                </div>

                <div id="section-ticket_controls" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-white mb-6 border-b border-dark-700 pb-4" data-en="Internal Ticket Controls 🛠️">التحكم الداخلي للتكت 🛠️</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-indigo-500 shadow-lg">
                        
                        <div class="flex flex-col md:flex-row items-start md:items-center justify-between p-6 bg-dark-900/80 rounded-2xl border border-indigo-500/30 mb-8 shadow-inner gap-4">
                            <div>
                                <h3 class="text-xl font-bold text-white mb-1" data-en="🔒 Two-Step Close System">🔒 الإغلاق بخطوتين (Two-Step Close)</h3>
                                <p class="text-sm text-slate-400" data-en="Pressing Close will show Reopen/Delete buttons instead of deleting immediately.">عند الضغط على إغلاق، لا تُحذف التذكرة فوراً، بل تظهر أزرار (إعادة فتح / حذف / حذف مع سبب).</p>
                            </div>
                            <label class="switch">
                                 <input type="checkbox" name="tc_twoStepClose" id="tc_twoStepClose" <%= config.ticketControls?.twoStepClose !== false ? 'checked' : '' %>>
                                <span class="slider"></span>
                            </label>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
                            <div class="bg-dark-900/50 p-6 rounded-2xl border border-dark-700 relative overflow-hidden shadow-sm">
                                <div class="absolute top-0 right-0 bg-brand text-[12px] font-bold px-3 py-1 rounded-bl-xl text-white">Counter</div>
                                <label class="block text-sm font-bold text-white mb-3 mt-2" data-en="Next Ticket Number">رقم التكت القادم (لإكمال العد القديم)</label>
                                <input type="number" name="tc_ticketCounter" value="<%= config.ticketControls?.ticketCounter || 1 %>" min="1" class="w-full bg-dark-800 border border-dark-600 rounded-xl py-4 px-4 text-brand-light font-mono text-3xl text-center font-black outline-none focus:border-brand">
                            </div>

                            <div class="bg-dark-900/50 p-6 rounded-2xl border border-dark-700 shadow-sm">
                                <label class="block text-sm font-bold text-white mb-3" data-en="Transcript Channel (Saved Messages)">روم حفظ الترانسكريبت (رسائل التذكرة)</label>
                                <select name="tc_transcriptChannelId" class="w-full bg-dark-800 border border-dark-600 rounded-xl py-4 px-4 text-white input-glow cursor-pointer font-bold">
                                    <option value="" data-en="-- Select Channel --">-- اختر الروم --</option>
                                    <% textChannels.forEach(ch => { %> 
                                        <option value="<%= ch.id %>" <%= config.ticketControls?.transcriptChannelId === ch.id ? 'selected' : '' %>>💬 <%= ch.name %></option> 
                                    <% }) %>
                                </select>
                            </div>

                            <div class="md:col-span-2 bg-dark-900/60 p-6 rounded-2xl border-2 border-blue-500/30 shadow-lg">
                                <label class="block text-lg font-black text-blue-400 mb-3" data-en="📝 Ticket Logs Channel (Open, Close, Claim)">📝 روم لوج التذاكر (فتح، إغلاق، استلام Claim)</label>
                                <select name="tc_ticketLogChannelId" class="w-full bg-dark-800 border border-dark-600 rounded-xl py-4 px-4 text-white input-glow cursor-pointer font-bold mb-3">
                                    <option value="" data-en="-- Select Channel --">-- اختر الروم --</option>
                                    <% textChannels.forEach(ch => { %> 
                                        <option value="<%= ch.id %>" <%= config.ticketControls?.ticketLogChannelId === ch.id ? 'selected' : '' %>>💬 <%= ch.name %></option> 
                                    <% }) %>
                                </select>
                                <p class="text-sm text-slate-400 font-bold" data-en="Detailed logs for every ticket action will be sent here.">سيتم إرسال سجلات مفصلة لكل تذكرة إلى هذا الروم (من قام بفتحها، إغلاقها، أو استلامها).</p>
                            </div>
                        </div>

                        <h3 class="text-2xl font-bold text-amber-400 mb-6 border-b border-dark-700 pb-2" data-en="🕵️ Claim Settings">🕵️ إعدادات زر الاستلام (Claim Settings)</h3>
                        <div class="space-y-4">
                            <div class="flex flex-col md:flex-row items-start md:items-center justify-between p-6 bg-dark-900/50 rounded-2xl border border-dark-700 transition-all shadow-sm gap-4" id="hide_claim_container">
                                <div>
                                    <h3 class="text-lg font-bold text-white mb-1" data-en="Hide Ticket on Claim">إخفاء التذكرة عند الاستلام</h3>
                                    <p class="text-xs text-slate-400" data-en="Hides the ticket from other staff members once claimed.">يخفي التكت عن باقي طاقم الإدارة بمجرد ضغط الإداري على استلام.</p>
                                </div>
                                <label class="switch">
                                    <input type="checkbox" name="tc_hideTicketOnClaim" id="tc_hideTicketOnClaim" <%= config.ticketControls?.hideTicketOnClaim ? 'checked' : '' %>>
                                    <span class="slider"></span>
                                </label>
                            </div>
                            
                            <div class="flex flex-col md:flex-row items-start md:items-center justify-between p-6 bg-dark-900/50 rounded-2xl border border-dark-700 transition-all shadow-sm gap-4" id="read_claim_container">
                                <div>
                                    <h3 class="text-lg font-bold text-white mb-1" data-en="Read-Only for Other Staff">منع الإداريين الآخرين من الكتابة (Read-Only)</h3>
                                    <p class="text-xs text-slate-400" data-en="Allows staff to view the ticket but prevents them from typing.">يسمح للإدارة برؤية التكت ولكن يمنعهم من الكتابة فيه بعد الاستلام.</p>
                                </div>
                                <label class="switch">
                                    <input type="checkbox" name="tc_readOnlyStaffOnClaim" id="tc_readOnlyStaffOnClaim" <%= config.ticketControls?.readOnlyStaffOnClaim ? 'checked' : '' %>>
                                    <span class="slider"></span>
                                </label>
                            </div>
                        </div>
                    </div>
                </div>

                <div id="section-system_points" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-emerald-400 mb-6 border-b border-dark-700 pb-4" data-en="System Points 🎯">نظام نقاط الإدارة (System Points) 🎯</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-emerald-500 shadow-[0_0_30px_rgba(16,185,129,0.1)]">
                        
                        <div class="bg-emerald-900/20 border border-emerald-500/30 p-4 rounded-xl mb-8">
                            <p class="text-sm text-emerald-300 font-bold flex items-center">
                                <span class="mr-2 ml-2 text-xl">💡</span>
                                هذا النظام يقوم باحتساب (التذاكر المستلمة، التحذيرات، العقوبات) لكل إداري بشكل مفصل.
                            </p>
                        </div>

                        <div class="bg-dark-900/50 p-6 rounded-2xl border border-dark-700 mb-8 shadow-inner">
                            <h3 class="text-xl font-bold text-white mb-2" data-en="System Points Log Channel">روم سجل نقاط الإدارة (للمراقبة)</h3>
                            <p class="text-sm text-slate-400 mb-4">يُرسل فيها إشعارات تلقائية عند استلام أي إداري لتكت أو إعطاء عقوبة.</p>
                            <select name="sp_logChannelId" class="w-full bg-dark-800 border border-dark-600 rounded-xl py-4 px-4 text-emerald-400 input-glow cursor-pointer font-bold text-lg">
                                <option value="">-- اختر الروم --</option>
                                <% textChannels.forEach(ch => { %> 
                                    <option value="<%= ch.id %>" <%= config.systemPoints?.logChannelId === ch.id ? 'selected' : '' %>>💬 <%= ch.name %></option> 
                                <% }) %>
                            </select>
                        </div>
                        
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Main SP Command">أمر عرض النقاط الأساسي (يدعم المتعدد)</label>
                                <input type="text" name="sp_command" value="<%= config.systemPoints?.command || 'systempoint, نقاط' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white font-bold input-glow" placeholder="مثال: systempoint, نقاط">
                                <p class="text-[11px] text-slate-500 mt-2">الأزرار (يوم، أسبوع، شهر، سنة) ستظهر تلقائياً داخل إيمبد هذا الأمر.</p>
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Reset SP Command">أمر تصفير النقاط (Reset)</label>
                                <input type="text" name="sp_resetCommand" value="<%= config.systemPoints?.resetCommand || 'resetpoints' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-3 px-4 text-white font-bold input-glow" placeholder="مثال: resetpoints">
                                <p class="text-[11px] text-slate-500 mt-2">يقوم بتصفير نقاط الإداري مع الاحتفاظ بنسخة أرشيفية لاستعراضها لاحقاً.</p>
                            </div>
                        </div>

                        <div class="bg-dark-900/60 p-6 rounded-2xl border-2 border-emerald-500/30 shadow-lg mb-8">
                            <label class="block text-lg font-black text-emerald-400 mb-3" data-en="Embed Basic Color">لون إيمبد إحصائيات الإدارة</label>
                            <div class="flex items-center space-x-3 space-x-reverse">
                                <input type="color" id="sp_embedColor_picker" value="<%= config.systemPoints?.embedColor || '#10B981' %>" class="w-14 h-12 rounded-xl bg-dark-800 border border-dark-600 p-1 cursor-pointer" onchange="document.getElementById('sp_embedColor').value = this.value">
                                <input type="text" id="sp_embedColor" name="sp_embedColor" value="<%= config.systemPoints?.embedColor || '#10B981' %>" class="flex-1 bg-dark-800 border border-dark-600 rounded-xl py-3 px-3 text-white font-mono uppercase text-center" oninput="document.getElementById('sp_embedColor_picker').value = this.value">
                            </div>
                        </div>
                    </div>
                </div>

                <div id="section-warnings" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-white mb-6 border-b border-dark-700 pb-4" data-en="Warnings & Management Panel ⚠️">نظام التحذيرات وبانل الإدارة ⚠️</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-orange-500 shadow-lg">
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Max Warnings Limit">أقصى عدد للتحذيرات (قبل العقوبة)</label>
                                <input type="number" name="warn_maxWarnings" value="<%= config.warnings?.maxWarnings || 3 %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-4 px-4 text-white text-center font-black text-2xl input-glow">
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-2" data-en="Auto Punishment Action">العقوبة التلقائية (بعد بلوغ الحد)</label>
                                <select name="warn_autoAction" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-4 px-4 text-white font-bold cursor-pointer input-glow">
                                    <option value="none" <%= config.warnings?.autoAction === 'none' ? 'selected' : '' %> data-en="❌ Disabled (No Action)">❌ معطل (بدون عقوبة تلقائية)</option>
                                    <option value="timeout" <%= config.warnings?.autoAction === 'timeout' ? 'selected' : '' %> data-en="⏳ Timeout">⏳ إسكات (Timeout)</option>
                                    <option value="kick" <%= config.warnings?.autoAction === 'kick' ? 'selected' : '' %> data-en="👢 Kick">👢 طرد (Kick)</option>
                                    <option value="ban" <%= config.warnings?.autoAction === 'ban' ? 'selected' : '' %> data-en="🔨 Ban">🔨 حظر (Ban)</option>
                                </select>
                            </div>
                        </div>

                        <div class="bg-dark-900/80 p-6 md:p-8 rounded-3xl border border-red-500/40 mb-10 shadow-inner">
                            <h3 class="text-2xl font-black text-red-400 mb-6 flex items-center border-b border-dark-700 pb-3" data-en="🔴 Staff Warnings Panel">🔴 بانل تحكم التحذيرات (يُرسل للإدارة)</h3>
                            
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-6">
                                <div>
                                    <label class="text-sm font-bold text-slate-400 block mb-2" data-en="Panel Channel">روم إرسال البانل</label>
                                    <select name="warn_panelChannelId" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3.5 text-white font-bold cursor-pointer input-glow">
                                        <option value="" data-en="-- Select Channel --">-- اختر الروم --</option>
                                        <% textChannels.forEach(ch => { %>
                                            <option value="<%= ch.id %>" <%= config.warnings?.panelChannelId === ch.id ? 'selected' : '' %>>💬 <%= ch.name %></option>
                                        <% }) %>
                                    </select>
                                </div>
                                <div>
                                    <label class="text-sm font-bold text-slate-400 block mb-2" data-en="Panel Color">لون البانل</label>
                                    <div class="flex items-center space-x-3 space-x-reverse">
                                        <input type="color" id="warn_panelColor_picker" value="<%= config.warnings?.panelColor || '#ed4245' %>" class="w-16 h-12 rounded-xl bg-dark-800 border border-dark-600 p-1 cursor-pointer" onchange="document.getElementById('warn_panelColor').value = this.value">
                                        <input type="text" id="warn_panelColor" name="warn_panelColor" value="<%= config.warnings?.panelColor || '#ed4245' %>" class="flex-1 bg-dark-800 border border-dark-600 rounded-xl py-3 px-4 text-white font-mono uppercase text-center input-glow">
                                    </div>
                                </div>
                            </div>
                            
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                                <div>
                                    <label class="text-sm font-bold text-slate-400 block mb-2" data-en="Panel Title">عنوان البانل</label>
                                    <input type="text" name="warn_panelTitle" value="<%= config.warnings?.panelTitle || 'لوحة تحكم التحذيرات' %>" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3.5 text-white font-bold input-glow">
                                </div>
                                <div>
                                    <label class="text-sm font-bold text-slate-400 block mb-2" data-en="Panel Description">وصف البانل</label>
                                    <input type="text" name="warn_panelDescription" value="<%= config.warnings?.panelDescription || 'استخدم الأزرار أدناه لإدارة تحذيرات الأعضاء بكل سهولة.' %>" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3.5 text-white input-glow">
                                </div>
                            </div>
                        </div>

                        <div class="bg-dark-900/60 p-6 md:p-8 rounded-3xl border border-dark-700 shadow-md">
                            <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-6 border-b border-dark-600 pb-4 gap-4">
                                <div>
                                    <h4 class="text-2xl font-black text-brand-light" data-en="Warning Reasons & DM Messages">أسباب التحذير ورسائل الخاص (DM)</h4>
                                    <p class="text-sm text-slate-400 mt-2" data-en="Each reason will send a specific private message to the member.">أضف القوانين هنا. كل سبب سيمتلك رسالة خاصة تُرسل للعضو في الخاص.</p>
                                </div>
                                <div class="flex flex-col sm:flex-row space-y-3 sm:space-y-0 sm:space-x-3 sm:space-x-reverse w-full md:w-auto">
                                    <button type="button" onclick="addWarnReason('ar')" class="w-full sm:w-auto px-5 py-3 bg-brand hover:bg-brand-light text-sm text-white font-black rounded-xl transition-all shadow-md whitespace-nowrap" data-en="➕ Add Reason (AR)">➕ إضافة سبب (عربي)</button>
                                    <button type="button" onclick="addWarnReason('en')" class="w-full sm:w-auto px-5 py-3 bg-indigo-600 hover:bg-indigo-500 text-sm text-white font-black rounded-xl transition-all shadow-md whitespace-nowrap" dir="ltr">➕ Add (EN)</button>
                                </div>
                            </div>
                            
                            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
                                <div id="warn-ar-container" class="space-y-4"></div>
                                <div id="warn-en-container" class="space-y-4" dir="ltr"></div>
                            </div>
                        </div>
                    </div>
                </div>

<div id="section-commands" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-cyan-400 mb-6 border-b border-dark-700 pb-4" data-en="Custom Commands & Punishments ⌨️">الأوامر المتعددة والعقوبات ⌨️</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-cyan-500 shadow-lg">
                        <div class="bg-blue-900/20 border border-blue-500/30 p-4 rounded-xl mb-8">
                            <p class="text-sm text-blue-300 font-bold flex items-center">
                                <span class="mr-2 ml-2 text-xl">💡</span>
                                يمكنك كتابة أكثر من أمر لكل خانة مفصولين بفاصلة. (مثال: ban, حظر, طيران) وسيعمل البوت بهم جميعاً!
                            </p>
                        </div>

                        <div class="flex items-center justify-between p-5 bg-dark-900/50 rounded-2xl border border-dark-700 mb-8 shadow-sm">
                            <div>
                                <h3 class="text-lg font-bold text-brand-light mb-1">نوع إيمبد العقوبات (Basic vs Custom)</h3>
                                <p class="text-xs text-slate-400">Basic: إيمبد البوت الأساسي الجاهز. Custom: إيمبد مخصص يمكنك التعديل عليه لاحقاً.</p>
                            </div>
                            <label class="switch">
                                <input type="checkbox" name="cmd_useCustomEmbeds" <%= config.commands?.useCustomEmbeds ? 'checked' : '' %>>
                                <span class="slider"></span>
                            </label>
                        </div>

                        <h3 class="text-2xl font-bold text-red-400 mb-6 border-b border-red-500/20 pb-2" data-en="🚨 Punishment Commands">🚨 أوامر العقوبات (الأساسية والعكسية)</h3>
                        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-10">
                            
                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-red-500/30 shadow-md">
                                <label class="block text-xs font-bold text-slate-400 mb-2" data-en="Ban Command">أمر الباند (Ban)</label>
                                <input type="text" name="cmd_banCmd" value="<%= config.commands?.banCmd || 'ban, حظر' %>" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                
                                <label class="block text-xs font-bold text-emerald-400 mb-2" data-en="Unban Command">أمر فك الباند (Unban)</label>
                                <input type="text" name="cmd_unbanCmd" value="<%= config.commands?.unbanCmd || 'unban, فك_حظر' %>" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                
                                <label class="block text-xs font-bold text-slate-300 mb-2" data-en="Allowed Roles">الرتب المسموحة:</label>
                                <select name="role_banAllowed" multiple class="w-full h-24 bg-dark-800 border border-dark-600 rounded-xl p-2 text-xs text-white custom-scrollbar focus:border-brand outline-none">
                                    <% serverRoles.forEach(r => { %>
                                        <option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.ban || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                                    <% }) %>
                                </select>
                            </div>
                            
                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-orange-500/30 shadow-md">
                                <label class="block text-xs font-bold text-slate-400 mb-2" data-en="Timeout Command">أمر التايم أوت (Timeout)</label>
                                <input type="text" name="cmd_timeoutCmd" value="<%= config.commands?.timeoutCmd || 'timeout, تايم' %>" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                
                                <label class="block text-xs font-bold text-emerald-400 mb-2" data-en="Untimeout Command">أمر فك التايم (Untimeout)</label>
                                <input type="text" name="cmd_untimeoutCmd" value="<%= config.commands?.untimeoutCmd || 'untimeout, فك_تايم' %>" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                
                                <label class="block text-xs font-bold text-slate-300 mb-2" data-en="Allowed Roles">الرتب المسموحة:</label>
                                <select name="role_timeoutAllowed" multiple class="w-full h-24 bg-dark-800 border border-dark-600 rounded-xl p-2 text-xs text-white custom-scrollbar focus:border-brand outline-none">
                                    <% serverRoles.forEach(r => { %>
                                        <option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.timeout || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                                    <% }) %>
                                </select>
                            </div>

                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-yellow-500/30 shadow-md">
                                <label class="block text-xs font-bold text-slate-400 mb-2" data-en="Warn Command">أمر التحذير (Warn)</label>
                                <input type="text" name="cmd_warnCmd" value="<%= config.commands?.warnCmd || 'warn, تحذير' %>" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                
                                <label class="block text-xs font-bold text-emerald-400 mb-2" data-en="Unwarn Command">أمر فك التحذير (Unwarn)</label>
                                <input type="text" name="cmd_unwarnCmd" value="<%= config.commands?.unwarnCmd || 'unwarn, فك_تحذير' %>" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                
                                <label class="block text-xs font-bold text-slate-300 mb-2" data-en="Allowed Roles">الرتب المسموحة:</label>
                                <select name="role_warnAllowed" multiple class="w-full h-24 bg-dark-800 border border-dark-600 rounded-xl p-2 text-xs text-white custom-scrollbar focus:border-brand outline-none">
                                    <% serverRoles.forEach(r => { %>
                                        <option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.warn || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                                    <% }) %>
                                </select>
                            </div>

                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-slate-500/30 shadow-md">
                                <label class="block text-xs font-bold text-slate-400 mb-2" data-en="Mute Command">أمر الميوت الكتابي (Mute)</label>
                                <input type="text" name="cmd_muteCmd" value="<%= config.commands?.muteCmd || 'mute, اسكات' %>" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                
                                <label class="block text-xs font-bold text-emerald-400 mb-2" data-en="Unmute Command">أمر فك الميوت (Unmute)</label>
                                <input type="text" name="cmd_unmuteCmd" value="<%= config.commands?.unmuteCmd || 'unmute, فك_اسكات' %>" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                
                                <label class="block text-xs font-bold text-slate-300 mb-2" data-en="Allowed Roles">الرتب المسموحة:</label>
                                <select name="role_muteAllowed" multiple class="w-full h-24 bg-dark-800 border border-dark-600 rounded-xl p-2 text-xs text-white custom-scrollbar focus:border-brand outline-none">
                                    <% serverRoles.forEach(r => { %>
                                        <option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.mute || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                                    <% }) %>
                                </select>
                            </div>
                        </div>

                        <hr class="border-dark-700 my-8">
                        <h3 class="text-2xl font-bold text-brand-light mb-6" data-en="🛠️ General Admin Commands">🛠️ الأوامر الإدارية العامة والضريبة</h3>
                        
                        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-5 mb-8">
                            <div class="bg-dark-900/50 p-4 rounded-xl border border-emerald-500/30 shadow-sm">
                                <label class="block text-xs font-bold text-emerald-400 mb-2" data-en="Tax">الضريبة (Tax)</label>
                                <input type="text" name="cmd_taxCmd" value="<%= config.commands?.taxCmd || 'tax, ضريبة' %>" class="w-full bg-dark-800 border border-dark-600 rounded-lg p-2.5 text-white font-mono text-sm mb-3 input-glow text-center">
                                <label class="block text-[10px] font-bold text-slate-400 mb-1" data-en="Roles:">الرتب:</label>
                                <select name="role_taxAllowed" multiple class="w-full h-20 bg-dark-800 border border-dark-600 rounded-lg p-1.5 text-xs text-white custom-scrollbar">
                                    <% serverRoles.forEach(r => { %><option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.tax || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %>
                                </select>
                            </div>
                            
                            <div class="bg-dark-900/50 p-4 rounded-xl border border-dark-700 shadow-sm">
                                <label class="block text-xs font-bold text-slate-300 mb-2" data-en="Clear Chat">مسح (Clear)</label>
                                <input type="text" name="cmd_clearCmd" value="<%= config.commands?.clearCmd || 'clear, مسح' %>" class="w-full bg-dark-800 border border-dark-600 rounded-lg p-2.5 text-white font-mono text-sm mb-3 input-glow text-center">
                                <label class="block text-[10px] font-bold text-slate-400 mb-1" data-en="Roles:">الرتب:</label>
                                <select name="role_clearAllowed" multiple class="w-full h-20 bg-dark-800 border border-dark-600 rounded-lg p-1.5 text-xs text-white custom-scrollbar">
                                    <% serverRoles.forEach(r => { %><option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.clear || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %>
                                </select>
                            </div>
                            
                            <div class="bg-dark-900/50 p-4 rounded-xl border border-dark-700 shadow-sm">
                                <label class="block text-xs font-bold text-slate-300 mb-2" data-en="Come">استدعاء (Come)</label>
                                <input type="text" name="cmd_comeCmd" value="<%= config.commands?.comeCmd || 'come, تعال' %>" class="w-full bg-dark-800 border border-dark-600 rounded-lg p-2.5 text-white font-mono text-sm mb-3 input-glow text-center">
                                <label class="block text-[10px] font-bold text-slate-400 mb-1" data-en="Roles:">الرتب:</label>
                                <select name="role_comeAllowed" multiple class="w-full h-20 bg-dark-800 border border-dark-600 rounded-lg p-1.5 text-xs text-white custom-scrollbar">
                                    <% serverRoles.forEach(r => { %><option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.come || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %>
                                </select>
                            </div>
                            
                            <div class="bg-dark-900/50 p-4 rounded-xl border border-blue-500/30 shadow-sm">
                                <label class="block text-xs font-bold text-blue-400 mb-2" data-en="Move">سحب صوتي (Move)</label>
                                <input type="text" name="cmd_moveCmd" value="<%= config.commands?.moveCmd || 'move, سحب' %>" class="w-full bg-dark-800 border border-dark-600 rounded-lg p-2.5 text-white font-mono text-sm mb-3 input-glow text-center">
                                <label class="block text-[10px] font-bold text-slate-400 mb-1" data-en="Roles:">الرتب:</label>
                                <select name="role_moveAllowed" multiple class="w-full h-20 bg-dark-800 border border-dark-600 rounded-lg p-1.5 text-xs text-white custom-scrollbar">
                                    <% serverRoles.forEach(r => { %><option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.move || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %>
                                </select>
                            </div>
                            
                            <div class="bg-dark-900/50 p-4 rounded-xl border border-dark-700 shadow-sm">
                                <label class="block text-[11px] font-bold text-slate-300 mb-2" data-en="Lock/Unlock">قفل الروم (Lock/Unlock)</label>
                                <input type="text" name="cmd_lockCmd" value="<%= config.commands?.lockCmd || 'lock, قفل' %>" class="w-full bg-dark-800 border border-dark-600 rounded-lg p-2 text-white font-mono text-[11px] mb-2 input-glow text-center" placeholder="قفل">
                                <input type="text" name="cmd_unlockCmd" value="<%= config.commands?.unlockCmd || 'unlock, فتح' %>" class="w-full bg-dark-800 border border-dark-600 rounded-lg p-2 text-white font-mono text-[11px] mb-2 input-glow text-center" placeholder="فتح">
                                <label class="block text-[10px] font-bold text-slate-400 mb-1" data-en="Roles:">الرتب:</label>
                                <select name="role_lockAllowed" multiple class="w-full h-12 bg-dark-800 border border-dark-600 rounded-lg p-1 text-[10px] text-white custom-scrollbar">
                                    <% serverRoles.forEach(r => { %><option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.lock || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %>
                                </select>
                            </div>
                        </div>

                        <h3 class="text-xl font-bold text-amber-500 mb-4 border-t border-dark-700 pt-6" data-en="⚖️ Middleman & Trade Commands">⚖️ أوامر الوساطة العليا (Trade, Approve, Done)</h3>
                        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                            <div class="bg-dark-800 p-5 rounded-2xl border border-dark-600 shadow-sm">
                                <label class="block text-sm font-bold text-white mb-2" data-en="Trade Command">أمر Trade (للوساطة)</label>
                                <input type="text" name="cmd_tradeCmd" value="<%= config.commands?.tradeCmd || 'trade, تريد' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                <label class="block text-xs font-bold text-slate-400 mb-2" data-en="Roles:">الرتب:</label>
                                <select name="role_tradeAllowed" multiple class="w-full h-24 bg-dark-900 border border-dark-700 rounded-xl p-2 text-xs text-white custom-scrollbar focus:border-brand outline-none">
                                    <% serverRoles.forEach(r => { %><option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.trade || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %>
                                </select>
                            </div>
                            <div class="bg-dark-800 p-5 rounded-2xl border border-dark-600 shadow-sm">
                                <label class="block text-sm font-bold text-white mb-2" data-en="Approve Command">أمر Approve (للموافقة)</label>
                                <input type="text" name="cmd_approveCmd" value="<%= config.commands?.approveCmd || 'approve, موافقة' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                <label class="block text-xs font-bold text-slate-400 mb-2" data-en="Roles:">الرتب:</label>
                                <select name="role_approveAllowed" multiple class="w-full h-24 bg-dark-900 border border-dark-700 rounded-xl p-2 text-xs text-white custom-scrollbar focus:border-brand outline-none">
                                    <% serverRoles.forEach(r => { %><option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.approve || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %>
                                </select>
                            </div>
                            <div class="bg-dark-800 p-5 rounded-2xl border border-dark-600 shadow-sm">
                                <label class="block text-sm font-bold text-white mb-2" data-en="Done Command">أمر Done (للتقييم)</label>
                                <input type="text" name="cmd_doneCmd" value="<%= config.commands?.doneCmd || 'done, تم' %>" class="w-full bg-dark-900 border border-dark-700 rounded-xl p-3 text-white font-mono text-sm mb-4 input-glow text-center">
                                <label class="block text-xs font-bold text-slate-400 mb-2" data-en="Roles:">الرتب:</label>
                                <select name="role_doneAllowed" multiple class="w-full h-24 bg-dark-900 border border-dark-700 rounded-xl p-2 text-xs text-white custom-scrollbar focus:border-brand outline-none">
                                    <% serverRoles.forEach(r => { %><option value="<%= r.id %>" <%= (config.commands?.allowedRoles?.done || []).includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %>
                                </select>
                            </div>
                        </div>
                    </div>
                </div>

                <div id="section-logs" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-teal-400 mb-6 border-b border-dark-700 pb-4" data-en="Detailed Server Logs 📝">سجلات السيرفر الشاملة (Logs) 📝</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-teal-500 shadow-lg">
                        
                        <h3 class="text-xl md:text-2xl font-bold text-red-400 mb-6 flex items-center border-b border-red-500/20 pb-3" data-en="😈 Security Logs">😈 اللوجات الأمنية (الريأكتات، الثريدات، الروابط المشبوهة)</h3>
                        
                        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-red-500/30 shadow-md">
                                <label class="block text-xs font-bold text-red-400 mb-2" data-en="Reactions & Ghost Ping Log">لوج التفاعلات و الـ Ghost Ping</label>
                                <select name="log_reactionLogId" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white text-xs mb-3 cursor-pointer input-glow">
                                    <option value="" data-en="-- Disabled --">-- معطل --</option>
                                    <% textChannels.forEach(ch => { %><option value="<%= ch.id %>" <%= config.serverLogs?.reactionLogId === ch.id ? 'selected' : '' %>>💬 <%= ch.name %></option><% }) %>
                                </select>
                                <input type="color" name="log_reactionColor" value="<%= config.serverLogs?.reactionColor || '#e74c3c' %>" class="w-full h-10 rounded-xl cursor-pointer bg-dark-800 border border-dark-600 p-1">
                            </div>
                            
                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-amber-500/30 shadow-md">
                                <label class="block text-[11px] font-bold text-amber-400 mb-2" data-en="Channel Setup & Auto Thread">لوج إنشاء/حذف الرومات والثريدات</label>
                                <select name="log_channelCreateDeleteLogId" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white text-xs mb-3 cursor-pointer input-glow">
                                    <option value="" data-en="-- Disabled --">-- معطل --</option>
                                    <% textChannels.forEach(ch => { %><option value="<%= ch.id %>" <%= config.serverLogs?.channelCreateDeleteLogId === ch.id ? 'selected' : '' %>>💬 <%= ch.name %></option><% }) %>
                                </select>
                                <input type="color" name="log_channelColor" value="<%= config.serverLogs?.channelColor || '#1abc9c' %>" class="w-full h-10 rounded-xl cursor-pointer bg-dark-800 border border-dark-600 p-1">
                            </div>
                            
                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-brand/30 shadow-md">
                                <label class="block text-[11px] font-bold text-brand-light mb-2" data-en="Suspicious Links Log">لوج الروابط الممسوحة (Anti-Link)</label>
                                <select name="log_threadCreateDeleteLogId" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white text-xs mb-3 cursor-pointer input-glow">
                                    <option value="" data-en="-- Disabled --">-- معطل --</option>
                                    <% textChannels.forEach(ch => { %><option value="<%= ch.id %>" <%= config.serverLogs?.threadCreateDeleteLogId === ch.id ? 'selected' : '' %>>💬 <%= ch.name %></option><% }) %>
                                </select>
                                <input type="color" name="log_threadColor" value="<%= config.serverLogs?.threadColor || '#34495e' %>" class="w-full h-10 rounded-xl cursor-pointer bg-dark-800 border border-dark-600 p-1">
                            </div>

                            <div class="bg-dark-900/50 p-5 rounded-2xl border border-fuchsia-500/30 shadow-md">
                                <label class="block text-[11px] font-bold text-fuchsia-400 mb-2" data-en="Role Setup Log">لوج الرتب (إنشاء/تعديل/حذف)</label>
                                <select name="log_roleCreateDeleteLogId" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white text-xs mb-3 cursor-pointer input-glow">
                                    <option value="" data-en="-- Disabled --">-- معطل --</option>
                                    <% textChannels.forEach(ch => { %><option value="<%= ch.id %>" <%= config.serverLogs?.roleCreateDeleteLogId === ch.id ? 'selected' : '' %>>💬 <%= ch.name %></option><% }) %>
                                </select>
                                <input type="color" name="log_roleCreateColor" value="<%= config.serverLogs?.roleCreateColor || '#e024ce' %>" class="w-full h-10 rounded-xl cursor-pointer bg-dark-800 border border-dark-600 p-1">
                            </div>
                        </div>

                        <hr class="border-dark-700 my-8">
                        
                        <h3 class="text-xl md:text-2xl font-bold text-blue-400 mb-6 flex items-center border-b border-blue-500/20 pb-3" data-en="🛡️ Punishments Logs">🛡️ لوجات العقوبات الشاملة</h3>
                        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                            <div class="bg-dark-800 p-6 rounded-3xl border border-dark-600 shadow-lg">
                                <label class="block text-sm font-bold text-red-500 mb-2" data-en="Ban & Kick Log">لوج الباند والطرد (Kick/Ban)</label>
                                <select name="log_banKickLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.banKickLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <input type="color" name="log_banColor" value="<%= config.serverLogs?.banColor || '#992d22' %>" class="w-full h-10 rounded-xl mb-5 cursor-pointer" title="لون الباند">
                                
                                <label class="block text-sm font-bold text-emerald-500 mb-2 pt-5 border-t border-dark-700" data-en="Unban Log">لوج فك الباند (Unban)</label>
                                <select name="log_unbanLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.unbanLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <input type="color" name="log_unbanColor" value="<%= config.serverLogs?.unbanColor || '#2ecc71' %>" class="w-full h-10 rounded-xl cursor-pointer" title="لون فك الباند">
                            </div>

                            <div class="bg-dark-800 p-6 rounded-3xl border border-dark-600 shadow-lg">
                                <label class="block text-sm font-bold text-orange-500 mb-2" data-en="Timeout Log">لوج التايم أوت والميوت الصوتي</label>
                                <select name="log_timeoutLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.timeoutLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <input type="color" name="log_timeoutColor" value="<%= config.serverLogs?.timeoutColor || '#e67e22' %>" class="w-full h-10 rounded-xl mb-5 cursor-pointer" title="لون التايم">
                                
                                <label class="block text-sm font-bold text-emerald-500 mb-2 pt-5 border-t border-dark-700" data-en="Untimeout Log">لوج فك التايم (Untimeout)</label>
                                <select name="log_untimeoutLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.untimeoutLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <input type="color" name="log_untimeoutColor" value="<%= config.serverLogs?.untimeoutColor || '#2ecc71' %>" class="w-full h-10 rounded-xl cursor-pointer" title="لون فك التايم">
                            </div>

                            <div class="bg-dark-800 p-6 rounded-3xl border border-dark-600 shadow-lg">
                                <label class="block text-sm font-bold text-yellow-400 mb-2" data-en="Warnings Log">لوج التحذيرات (Warn)</label>
                                <select name="log_warningsLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.warningsLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <input type="color" name="log_warnColor" value="<%= config.serverLogs?.warnColor || '#f1c40f' %>" class="w-full h-10 rounded-xl mb-5 cursor-pointer" title="لون التحذير">
                                
                                <label class="block text-sm font-bold text-emerald-500 mb-2 pt-5 border-t border-dark-700" data-en="Unwarn Log">لوج فك التحذير (Unwarn)</label>
                                <select name="log_unwarningsLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.unwarningsLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <input type="color" name="log_unwarnColor" value="<%= config.serverLogs?.unwarnColor || '#2ecc71' %>" class="w-full h-10 rounded-xl cursor-pointer" title="لون فك التحذير">
                            </div>
                        </div>

                        <hr class="border-dark-700 my-8">
                        <h3 class="text-xl md:text-2xl font-bold text-slate-300 mb-6 border-b border-dark-700 pb-3" data-en="💬 General Action Logs">💬 لوجات الحركة العامة والصوتية</h3>

                        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-5">
                            <div class="bg-dark-800 p-5 rounded-2xl border border-dark-600 shadow-sm">
                                <label class="text-[12px] font-bold text-slate-400 block mb-2" data-en="Message Delete">حذف الرسائل الطويلة</label>
                                <select name="log_messageDeleteLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.messageDeleteLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <input type="color" name="log_msgDelColor" value="<%= config.serverLogs?.msgDelColor || '#ed4245' %>" class="w-full h-10 rounded-xl cursor-pointer">
                            </div>
                            
                            <div class="bg-dark-800 p-5 rounded-2xl border border-dark-600 shadow-sm">
                                <label class="text-[12px] font-bold text-slate-400 block mb-2" data-en="Message Edit">تعديل الرسائل</label>
                                <select name="log_messageEditLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.messageEditLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <input type="color" name="log_msgEditColor" value="<%= config.serverLogs?.msgEditColor || '#fee75c' %>" class="w-full h-10 rounded-xl cursor-pointer">
                            </div>
                            
                            <div class="bg-dark-800 p-5 rounded-2xl border border-dark-600 shadow-sm">
                                <label class="text-[12px] font-bold text-amber-500 block mb-2" data-en="Image Delete Only">حذف صور فقط</label>
                                <select name="log_imageDeleteLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.imageDeleteLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <input type="color" name="log_imgDelColor" value="<%= config.serverLogs?.imgDelColor || '#e67e22' %>" class="w-full h-10 rounded-xl cursor-pointer">
                            </div>
                            
                            <div class="bg-dark-800 p-5 rounded-2xl border border-dark-600 shadow-sm">
                                <label class="text-[12px] font-bold text-indigo-400 block mb-2" data-en="Voice Logs">سجلات الدخول الصوتي</label>
                                <select name="log_roleGiveTakeLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.roleGiveTakeLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <input type="color" name="log_roleColor" value="<%= config.serverLogs?.roleColor || '#6366f1' %>" class="w-full h-10 rounded-xl cursor-pointer">
                            </div>
                            
                            <div class="bg-dark-800 p-5 rounded-2xl border border-dark-600 shadow-sm">
                                <label class="text-[12px] font-bold text-slate-400 block mb-2" data-en="Member Join/Leave">دخول/خروج أعضاء</label>
                                <select name="log_memberJoinLeaveLogId" class="w-full bg-dark-900 border border-dark-700 text-xs text-white rounded-xl p-3 mb-3 cursor-pointer input-glow"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.serverLogs?.memberJoinLeaveLogId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <div class="flex space-x-2 space-x-reverse">
                                    <input type="color" name="log_joinColor" value="<%= config.serverLogs?.joinColor || '#3ba55d' %>" class="w-1/2 h-10 rounded-xl cursor-pointer" title="لون الدخول">
                                    <input type="color" name="log_leaveColor" value="<%= config.serverLogs?.leaveColor || '#ed4245' %>" class="w-1/2 h-10 rounded-xl cursor-pointer" title="لون الخروج">
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div id="section-roles" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-white mb-6 border-b border-dark-700 pb-4" data-en="Staff Roles & Permissions 👑">الرتب والصلاحيات 👑</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-purple-500 shadow-lg">
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-10">
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-3" data-en="Main Admin Role">رتبة الدعم الفني الأساسية (لاستلام التكتات العادية)</label>
                                <select name="role_adminRoleId" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-4 px-4 text-white font-bold cursor-pointer input-glow">
                                    <option value="">-- اختر الرتبة --</option>
                                    <% serverRoles.forEach(role => { %> 
                                        <option value="<%= role.id %>" <%= config.roles?.adminRoleId === role.id ? 'selected' : '' %>>🛡️ <%= role.name %></option> 
                                    <% }) %>
                                </select>
                            </div>
                            <div>
                                <label class="block text-sm font-bold text-slate-300 mb-3" data-en="Main Middleman Role">رتبة الوسيط الأساسية (لاستلام تكتات الوساطة)</label>
                                <select name="role_middlemanRoleId" class="w-full bg-dark-900 border border-dark-700 rounded-xl py-4 px-4 text-white font-bold cursor-pointer input-glow">
                                    <option value="">-- اختر الرتبة --</option>
                                    <% serverRoles.forEach(role => { %> 
                                        <option value="<%= role.id %>" <%= config.roles?.middlemanRoleId === role.id ? 'selected' : '' %>>⚖️ <%= role.name %></option> 
                                    <% }) %>
                                </select>
                            </div>
                        </div>

                        <hr class="border-dark-700 my-8">
                        <h3 class="text-xl md:text-2xl font-bold text-white mb-6 border-b border-dark-700 pb-3" data-en="🌟 High Staff & Trade Roles (Multi-Select)">🌟 رتب الإدارة العليا والتريد (تحديد متعدد)</h3>

                        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                            <div class="bg-dark-900/50 p-6 rounded-2xl border border-dark-700 shadow-md">
                                <label class="block text-sm font-bold text-amber-400 mb-3" data-en="High Staff Roles">الإدارة العليا (لاستخدام أوامر التصفير والسيستم بوينت)</label>
                                <select name="role_highAdminRoles" multiple class="w-full h-48 bg-dark-800 border border-dark-600 rounded-xl py-3 px-4 text-white custom-scrollbar focus:border-brand outline-none">
                                    <% serverRoles.forEach(role => { %> 
                                        <option value="<%= role.id %>" <%= (config.roles?.highAdminRoles || []).includes(role.id) ? 'selected' : '' %>><%= role.name %></option> 
                                    <% }) %>
                                </select>
                            </div>
                            <div class="bg-dark-900/50 p-6 rounded-2xl border border-dark-700 shadow-md">
                                <label class="block text-sm font-bold text-blue-400 mb-3" data-en="Trade Ping Roles">رتب يتم عمل منشن لها عند طلب وساطة (Ping)</label>
                                <select name="role_tradePingRoleIds" multiple class="w-full h-48 bg-dark-800 border border-dark-600 rounded-xl py-3 px-4 text-white custom-scrollbar focus:border-brand outline-none">
                                    <% serverRoles.forEach(role => { %> 
                                        <option value="<%= role.id %>" <%= (config.roles?.tradePingRoleIds || []).includes(role.id) ? 'selected' : '' %>><%= role.name %></option> 
                                    <% }) %>
                                </select>
                            </div>
                            <div class="bg-dark-900/50 p-6 rounded-2xl border border-dark-700 shadow-md">
                                <label class="block text-sm font-bold text-emerald-400 mb-3" data-en="Trade Approve Roles">رتب الموافقة أو الرفض على طلبات الوساطة العليا</label>
                                <select name="role_tradeApproveRoleIds" multiple class="w-full h-48 bg-dark-800 border border-dark-600 rounded-xl py-3 px-4 text-white custom-scrollbar focus:border-brand outline-none">
                                    <% serverRoles.forEach(role => { %> 
                                        <option value="<%= role.id %>" <%= (config.roles?.tradeApproveRoleIds || []).includes(role.id) ? 'selected' : '' %>><%= role.name %></option> 
                                    <% }) %>
                                </select>
                            </div>
                        </div>
                    </div>
                </div>

                <div id="section-ratings" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-white mb-6 border-b border-dark-700 pb-4" data-en="Ratings System ⭐">نظام التقييمات المزدوجة ⭐</h2>
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-yellow-400 shadow-lg">
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                            <div class="bg-dark-900/60 p-6 rounded-2xl border border-dark-700">
                                <h3 class="text-xl font-bold text-emerald-400 mb-4 border-b border-dark-600 pb-2">⭐ تقييم الدعم الفني (Staff)</h3>
                                <p class="text-xs text-slate-400 mb-4">يُرسل للعضو في الخاص عند إغلاق التكت لتقييم الدعم.</p>
                                <label class="block text-sm font-bold text-slate-300 mb-2">روم إرسال التقييمات</label>
                                <select name="rate_staffLogChannelId" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white mb-4"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.ratings?.staffLogChannelId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <label class="block text-sm font-bold text-slate-300 mb-2">لون إيمبد التقييم</label>
                                <input type="color" name="rate_staffEmbedColor" value="<%= config.ratings?.staffEmbedColor || '#3ba55d' %>" class="w-full h-10 rounded-xl cursor-pointer">
                            </div>
                            <div class="bg-dark-900/60 p-6 rounded-2xl border border-dark-700">
                                <h3 class="text-xl font-bold text-amber-500 mb-4 border-b border-dark-600 pb-2">⚖️ تقييم الوساطة (Middleman)</h3>
                                <p class="text-xs text-slate-400 mb-4">يتم التقييم عند كتابة أمر !done بعد إنهاء الوساطة.</p>
                                <label class="block text-sm font-bold text-slate-300 mb-2">روم إرسال التقييمات</label>
                                <select name="rate_middlemanLogChannelId" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-3 text-white mb-4"><option value="">- معطل -</option><%- textChannels.map(c=>`<option value="${c.id}" ${config.ratings?.middlemanLogChannelId===c.id?'selected':''}>💬 ${c.name}</option>`).join('') %></select>
                                <label class="block text-sm font-bold text-slate-300 mb-2">لون إيمبد التقييم</label>
                                <input type="color" name="rate_middlemanEmbedColor" value="<%= config.ratings?.middlemanEmbedColor || '#f2a658' %>" class="w-full h-10 rounded-xl cursor-pointer">
                            </div>
                        </div>
                    </div>
                </div>

                ```
<div id="section-custom_texts" class="settings-section max-w-5xl mx-auto">
                    <h2 class="text-3xl md:text-4xl font-black text-white mb-6 border-b border-dark-700 pb-4" data-en="Custom Embed Texts 🎨">نصوص الإيمبدات المخصصة (Custom) 🎨</h2>
                    
                    <div class="glass-card rounded-3xl p-6 md:p-8 mb-8 border-t-4 border-t-brand shadow-lg">
                        <div class="bg-blue-900/20 border border-blue-500/30 p-5 rounded-2xl mb-8">
                            <p class="text-sm text-blue-300 font-bold leading-relaxed">
                                <span class="text-xl">💡</span>
                                هذه النصوص لن تعمل إلا إذا قمت بتفعيل خيار (Custom Embeds) من قسم الأوامر. 
                                <br><br>
                                <b>المتغيرات المتاحة للاستخدام في أي إيمبد:</b><br>
                                <code class="bg-dark-900 px-2 py-1 rounded text-amber-400 mr-1">{user}</code> = منشن العضو المٌعاقب / المستدعى.<br>
                                <code class="bg-dark-900 px-2 py-1 rounded text-amber-400 mr-1">{admin}</code> = منشن الإداري الذي نفذ الأمر.<br>
                                <code class="bg-dark-900 px-2 py-1 rounded text-amber-400 mr-1">{reason}</code> = السبب (إن وُجد).<br>
                                <code class="bg-dark-900 px-2 py-1 rounded text-amber-400 mr-1">{room}</code> = منشن الروم (لأمر الاستدعاء).
                            </p>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                            <div class="bg-dark-900/60 p-6 rounded-2xl border border-dark-700 shadow-sm">
                                <h3 class="text-lg font-black text-brand-light mb-3">رسالة الاستدعاء (Come)</h3>
                                <textarea name="customText_come" rows="3" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-4 text-white font-bold resize-y focus:border-brand outline-none" placeholder="يرجى التوجه إلى الروم الصوتي {room} بناءً على طلب الإداري {admin}"><%= config.commands?.customText_come || 'مرحباً {user}، يرجى التوجه إلى {room} بناءً على طلب الإداري {admin}.' %></textarea>
                            </div>

                            <div class="bg-dark-900/60 p-6 rounded-2xl border border-red-500/30 shadow-sm">
                                <h3 class="text-lg font-black text-red-400 mb-3">رسالة الحظر (Ban)</h3>
                                <textarea name="customText_ban" rows="3" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-4 text-white font-bold resize-y focus:border-brand outline-none"><%= config.commands?.customText_ban || 'تم حظر العضو {user} بواسطة {admin}.\nالسبب: {reason}' %></textarea>
                            </div>

                            <div class="bg-dark-900/60 p-6 rounded-2xl border border-orange-500/30 shadow-sm">
                                <h3 class="text-lg font-black text-orange-400 mb-3">رسالة التايم أوت (Timeout)</h3>
                                <textarea name="customText_timeout" rows="3" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-4 text-white font-bold resize-y focus:border-brand outline-none"><%= config.commands?.customText_timeout || 'تم إعطاء تايم أوت للعضو {user} بواسطة {admin}.\nالمدة: {time}\nالسبب: {reason}' %></textarea>
                            </div>

                            <div class="bg-dark-900/60 p-6 rounded-2xl border border-yellow-500/30 shadow-sm">
                                <h3 class="text-lg font-black text-yellow-400 mb-3">رسالة التحذير (Warn)</h3>
                                <textarea name="customText_warn" rows="3" class="w-full bg-dark-800 border border-dark-600 rounded-xl p-4 text-white font-bold resize-y focus:border-brand outline-none"><%= config.commands?.customText_warn || 'تم تحذير العضو {user} بواسطة {admin}.\nالسبب: {reason}' %></textarea>
                            </div>
                        </div>
                    </div>
                </div>

            </form>
        </div>
    </main>

    <script>
        
        // -----------------------------------------------------------
        // 1. المتغيرات السحابية الأساسية للحفظ (Variables)
        // -----------------------------------------------------------
        const safeConfig = <%- JSON.stringify(config || {}) %>;
        
        let warnDataAr = safeConfig.warnings?.reasonsDataAr || [];
        let warnDataEn = safeConfig.warnings?.reasonsDataEn || [];
        let mmFields = safeConfig.middlemanSystem?.modalFields || [];
        let panelsArray = safeConfig.ticketPanels || [];
        let roleRewards = safeConfig.leveling?.roleRewards || [];
        let autoResponders = safeConfig.autoResponders || [];
        
        const serverRolesHTML = `<%- serverRoles.map(r => `<option value="${r.id}">${r.name}</option>`).join('') %>`;
        const categoriesHTML = `<%- categories.map(cat => `<option value="${cat.id}">${cat.name}</option>`).join('') %>`;
        const channelsHTML = `<%- textChannels.map(ch => `<option value="${ch.id}">${ch.name}</option>`).join('') %>`;

        // -----------------------------------------------------------
        // 2. دالة الحفظ العملاقة التي ترسل الداتا للداتابيز
        // -----------------------------------------------------------
        function submitMegaForm() {
            // تجميع وتعبئة البيانات المعقدة (المصفوفات) في الحقول المخفية قبل الإرسال
            document.getElementById('warn_reasonsDataAr').value = JSON.stringify(warnDataAr);
            document.getElementById('warn_reasonsDataEn').value = JSON.stringify(warnDataEn);
            document.getElementById('mm_modalFieldsData').value = JSON.stringify(mmFields);
            document.getElementById('ticketPanelsData').value = JSON.stringify(panelsArray);
            document.getElementById('lvl_roleRewardsData').value = JSON.stringify(roleRewards);
            document.getElementById('autoRespondersData').value = JSON.stringify(autoResponders);
            
            // تغيير شكل زر الحفظ ليعطي تأثير التحميل
            const saveBtn = document.querySelector('button[onclick="submitMegaForm()"]');
            const isEnglish = document.getElementById('system-language')?.value === 'en';
            
            saveBtn.innerHTML = `<span class="mr-3 ml-2 text-3xl animate-spin">⏳</span> ${isEnglish ? 'Saving...' : 'جاري الحفظ وتحديث السيرفر...'}`;
            saveBtn.classList.replace('bg-emerald-500', 'bg-emerald-700');
            saveBtn.classList.replace('border-emerald-400/50', 'border-emerald-700');
            saveBtn.classList.remove('hover:bg-emerald-400', 'hover:-translate-y-2');
            
            // إرسال الفورم بعد التأكد من التعبئة بـ 100 ملي ثانية
            setTimeout(() => {
                document.getElementById('mega-settings-form').submit();
            }, 100);
        }

        // -----------------------------------------------------------
        // 3. دوال التنقل والقائمة الجانبية
        // -----------------------------------------------------------
        function toggleMobileSidebar() {
            const sidebar = document.getElementById('sidebar');
            const overlay = document.getElementById('sidebar-overlay');
            
            if (sidebar.classList.contains('translate-x-full')) {
                sidebar.classList.remove('translate-x-full');
                overlay.classList.remove('hidden');
            } else {
                sidebar.classList.add('translate-x-full');
                overlay.classList.add('hidden');
            }
        }

        function switchSection(target) {
            // إخفاء كل الأقسام
            document.querySelectorAll('.settings-section').forEach(section => {
                section.classList.remove('active');
            });
            
            // إظهار القسم المطلوب
            const targetSection = document.getElementById('section-' + target);
            if(targetSection) {
                targetSection.classList.add('active');
            }
            
            // تحديث ألوان القائمة الجانبية
            document.querySelectorAll('.nav-btn').forEach(btn => { 
                btn.classList.remove('bg-brand', 'text-white', 'active-nav'); 
                btn.classList.add('text-slate-400'); 
            });
            
            const activeBtn = document.querySelector(`[data-target="${target}"]`);
            if(activeBtn) { 
                activeBtn.classList.add('bg-brand', 'text-white', 'active-nav');
                activeBtn.classList.remove('text-slate-400'); 
            }
            
            // التمرير لأعلى الصفحة
            document.getElementById('main-scroll-area').scrollTo({ top: 0, behavior: 'smooth' });
            
            // إغلاق القائمة الجانبية في الموبايل
            if(window.innerWidth < 768) {
                toggleMobileSidebar();
            }
        }

        // -----------------------------------------------------------
        // 4. دالة إنشاء الإيمبد المباشر (Live Embed)
        // -----------------------------------------------------------
        async function sendLiveEmbed(guildId) {
            const channelId = document.getElementById('live_embed_channel').value;
            const title = document.getElementById('live_embed_title').value;
            const description = document.getElementById('live_embed_desc').value;
            const color = document.getElementById('live_embed_color').value;
            const imageUrl = document.getElementById('live_embed_image').value;
            const statusDiv = document.getElementById('live_embed_status');
            
            if(!channelId || (!title && !description)) { 
                statusDiv.className = 'mb-6 p-4 rounded-xl bg-red-500/20 text-red-400 border border-red-500 font-bold';
                statusDiv.innerText = '❌ يرجى اختيار الروم وكتابة محتوى الإيمبد.'; 
                statusDiv.classList.remove('hidden'); 
                return;
            }
            
            statusDiv.className = 'mb-6 p-4 rounded-xl bg-blue-500/20 text-blue-400 border border-blue-500 font-bold';
            statusDiv.innerText = '⏳ جاري الإرسال للديسكورد...'; 
            statusDiv.classList.remove('hidden');
            
            try {
                const res = await fetch(`/api/send-embed/${guildId}`, { 
                    method: 'POST', 
                    headers: { 'Content-Type': 'application/json' }, 
                    body: JSON.stringify({ channelId, title, description, color, imageUrl }) 
                });
                const data = await res.json();
                
                if(data.success) { 
                    statusDiv.className = 'mb-6 p-4 rounded-xl bg-emerald-500/20 text-emerald-400 border border-emerald-500 font-bold';
                    statusDiv.innerText = '✅ تم الإرسال بنجاح!'; 
                    document.getElementById('live_embed_title').value = ''; 
                    document.getElementById('live_embed_desc').value = ''; 
                    document.getElementById('live_embed_image').value = '';
                } else { 
                    statusDiv.className = 'mb-6 p-4 rounded-xl bg-red-500/20 text-red-400 border border-red-500 font-bold';
                    statusDiv.innerText = data.error || '❌ فشل الإرسال.'; 
                }
            } catch(e) { 
                statusDiv.className = 'mb-6 p-4 rounded-xl bg-red-500/20 text-red-400 border border-red-500 font-bold';
                statusDiv.innerText = '❌ حدث خطأ في الاتصال بالبوت. تأكد من عمل البوت.'; 
            }
        }

        // -----------------------------------------------------------
        // 5. محرك الردود التلقائية
        // -----------------------------------------------------------
        function renderAutoResponders() {
            const container = document.getElementById('auto-responders-container');
            if(!container) return;
            container.innerHTML = '';
            
            if(autoResponders.length === 0) {
                container.innerHTML = '<div class="text-center text-slate-500 text-base p-8 border-2 border-dashed border-dark-600 rounded-2xl font-bold">لا توجد ردود تلقائية حالياً.</div>';
                return;
            }
            
            autoResponders.forEach((resp, idx) => {
                container.innerHTML += `
                    <div class="bg-dark-800 p-6 rounded-2xl border border-dark-600 mb-4 relative shadow-md hover:border-fuchsia-500/50 transition-colors">
                        <button type="button" onclick="autoResponders.splice(${idx}, 1); renderAutoResponders();" class="absolute top-4 left-4 text-red-400 bg-red-500/10 hover:bg-red-500 hover:text-white px-4 py-2 rounded-lg text-xs font-bold transition-all">حذف 🗑️</button>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-4">
                            <div>
                                <label class="block text-xs font-bold text-slate-400 mb-2">الكلمة التي يكتبها العضو (Trigger)</label>
                                <input type="text" class="w-full bg-dark-900 border border-dark-700 rounded-xl p-3.5 text-white font-bold focus:border-brand outline-none" value="${resp.trigger || ''}" onchange="autoResponders[${idx}].trigger = this.value">
                            </div>
                            <div>
                                <label class="block text-xs font-bold text-slate-400 mb-2">رد البوت (نص أو رابط صورة)</label>
                                <input type="text" class="w-full bg-dark-900 border border-dark-700 rounded-xl p-3.5 text-white focus:border-brand outline-none" value="${resp.reply || ''}" onchange="autoResponders[${idx}].reply = this.value">
                            </div>
                        </div>
                    </div>
                `;
            });
        }
        function addAutoResponderUI() { 
            autoResponders.push({trigger: 'كلمة جديدة', reply: 'رد جديد'}); 
            renderAutoResponders(); 
        }

        // -----------------------------------------------------------
        // 6. محرك التحذيرات
        // -----------------------------------------------------------
        function renderWarns(lang) {
            const arr = lang === 'ar' ? warnDataAr : warnDataEn;
            const container = document.getElementById(`warn-${lang}-container`);
            if(!container) return;
            container.innerHTML = '';
            
            arr.forEach((item, index) => {
                container.innerHTML += `
                    <div class="bg-dark-800 p-5 rounded-2xl border border-dark-600 mb-4 transition-all shadow-md relative group hover:border-red-500/50">
                        <button type="button" onclick="${lang === 'ar' ? 'warnDataAr' : 'warnDataEn'}.splice(${index}, 1); renderWarns('${lang}');" class="absolute top-4 left-4 text-red-500 bg-red-500/10 hover:bg-red-500 hover:text-white px-4 py-2 rounded-lg text-xs font-bold transition-all opacity-100 md:opacity-0 md:group-hover:opacity-100">حذف 🗑️</button>
                        
                        <label class="block text-sm font-bold text-slate-400 mb-2">السبب (Reason):</label>
                        <input type="text" class="w-full bg-dark-900 border border-dark-700 text-white text-base rounded-xl p-3 font-bold mb-4 focus:border-brand outline-none ${lang === 'en' ? 'text-left' : ''}" dir="${lang === 'en' ? 'ltr' : 'rtl'}" value="${item.reason}" onchange="${lang === 'ar' ? 'warnDataAr' : 'warnDataEn'}[${index}].reason = this.value">
                        
                        <label class="block text-sm font-bold text-brand-light mb-2">رسالة الخاص للعضو (DM Message):</label>
                        <textarea class="w-full bg-dark-900 border border-dark-700 text-white text-sm rounded-xl p-4 resize-y focus:border-brand outline-none ${lang === 'en' ? 'text-left' : ''}" dir="${lang === 'en' ? 'ltr' : 'rtl'}" rows="3" onchange="${lang === 'ar' ? 'warnDataAr' : 'warnDataEn'}[${index}].dmMessage = this.value">${item.dmMessage}</textarea>
                    </div>
                `;
            });
        }
        function addWarnReason(lang) { 
            if(lang === 'ar') {
                warnDataAr.push({reason: 'سبب جديد', dmMessage: 'لقد تم تحذيرك بسبب مخالفتك القوانين.'}); 
            } else {
                warnDataEn.push({reason: 'New Reason', dmMessage: 'You have been warned for breaking the rules.'}); 
            }
            renderWarns(lang); 
        }

        // -----------------------------------------------------------
        // 7. محرك أسئلة نافذة الوساطة
        // -----------------------------------------------------------
        function renderMmFields() {
            const container = document.getElementById('mm-fields-ui-container'); 
            if(!container) return;
            container.innerHTML = '';
            
            if(mmFields.length === 0) {
                container.innerHTML = '<div class="text-center text-slate-500 text-base p-6 border-2 border-dashed border-dark-600 rounded-2xl font-bold">لا توجد أسئلة للنافذة حالياً. العميل سيفتح التذكرة فوراً بدون نافذة.</div>';
                return;
            }
            
            mmFields.forEach((field, index) => {
                container.innerHTML += `
                    <div class="bg-dark-800 p-6 rounded-2xl border border-dark-600 mb-5 relative shadow-md">
                        <div class="absolute top-0 right-0 bg-dark-700 text-xs px-4 py-1.5 rounded-bl-xl text-slate-300 font-bold">السؤال ${index + 1}</div>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-6">
                            <div>
                                <label class="text-sm font-bold text-slate-400 mb-2 block">نص السؤال</label>
                                <input type="text" class="w-full bg-dark-900 border border-dark-700 rounded-xl p-3.5 text-white text-base font-bold focus:border-brand outline-none" value="${field.label || ''}" onchange="mmFields[${index}].label = this.value">
                            </div>
                            <div>
                                <label class="text-sm font-bold text-slate-400 mb-2 block">مثال إرشادي (Placeholder)</label>
                                <input type="text" class="w-full bg-dark-900 border border-dark-700 rounded-xl p-3.5 text-white text-sm focus:border-brand outline-none" value="${field.placeholder || ''}" onchange="mmFields[${index}].placeholder = this.value">
                            </div>
                        </div>
                        <div class="flex flex-col md:flex-row justify-between items-start md:items-center mt-6 pt-5 border-t border-dark-700 gap-4">
                            <div class="flex flex-col space-y-3 text-sm font-bold text-brand-light bg-dark-900/50 p-4 rounded-xl border border-dark-600 w-full md:w-auto">
                                <label class="cursor-pointer flex items-center">
                                    <input type="radio" name="mm_style_${index}" value="Short" class="mr-3 ml-3 w-5 h-5 accent-brand" ${field.style !== 'Paragraph' ? 'checked' : ''} onchange="mmFields[${index}].style = this.value"> 
                                    إجابة قصيرة (سطر واحد)
                                </label>
                                <label class="cursor-pointer flex items-center">
                                    <input type="radio" name="mm_style_${index}" value="Paragraph" class="mr-3 ml-3 w-5 h-5 accent-brand" ${field.style === 'Paragraph' ? 'checked' : ''} onchange="mmFields[${index}].style = this.value"> 
                                    إجابة طويلة (فقرة كبيرة)
                                </label>
                            </div>
                            <button type="button" onclick="mmFields.splice(${index}, 1); renderMmFields();" class="w-full md:w-auto text-sm bg-red-600 hover:bg-red-500 text-white px-6 py-3 rounded-xl font-bold transition-all shadow-md">حذف السؤال 🗑️</button>
                        </div>
                    </div>
                `;
            });
        }
        function addMmFieldUI() { 
            mmFields.push({ label: 'سؤال جديد', placeholder: 'اكتب إجابتك هنا...', style: 'Short' }); 
            renderMmFields(); 
        }

        // -----------------------------------------------------------
        // 8. محرك جوائز اللفلات (Role Rewards)
        // -----------------------------------------------------------
        function renderLvlRewards() {
            const container = document.getElementById('level-rewards-container');
            if(!container) return;
            container.innerHTML = '';
            
            if(roleRewards.length === 0){ 
                container.innerHTML = '<div class="text-center text-slate-500 text-base p-8 border-2 border-dashed border-dark-600 rounded-2xl font-bold">لم تقم بإضافة أي جوائز رتب للمستويات حتى الآن.</div>';
                return; 
            }
            
            roleRewards.forEach((reward, index) => {
                container.innerHTML += `
                    <div class="flex flex-col md:flex-row items-center space-x-0 md:space-x-6 md:space-x-reverse space-y-4 md:space-y-0 bg-dark-800 p-6 rounded-2xl border border-dark-600 mb-3 shadow-lg hover:border-blue-500/50 transition-colors">
                        <div class="flex items-center w-full md:w-auto">
                            <span class="text-brand-light font-black text-base w-40">عند الوصول لـ لفل:</span>
                            <input type="number" min="1" class="w-24 bg-dark-900 border border-dark-600 rounded-xl py-3 px-3 text-white font-black text-center text-xl focus:border-brand outline-none" value="${reward.levelRequired}" onchange="roleRewards[${index}].levelRequired = parseInt(this.value)">
                        </div>
                        <div class="flex items-center w-full md:flex-1">
                            <span class="text-slate-400 font-bold text-base w-24">يأخذ رتبة:</span>
                            <select class="flex-1 bg-dark-900 border border-dark-600 rounded-xl py-3 px-4 text-white font-bold cursor-pointer focus:border-brand outline-none" onchange="roleRewards[${index}].roleId = this.value">
                                <option value="">-- يرجى اختيار الرتبة --</option>
                                ${serverRolesHTML.replace(`value="${reward.roleId}"`, `value="${reward.roleId}" selected`)}
                            </select>
                        </div>
                        <button type="button" onclick="roleRewards.splice(${index}, 1); renderLvlRewards();" class="w-full md:w-auto bg-red-600 hover:bg-red-500 text-white px-8 py-3 rounded-xl font-black transition-all shadow-md">إزالة 🗑️</button>
                    </div>
                `;
            });
        }
        function addLevelRewardUI() { 
            roleRewards.push({ levelRequired: 5, roleId: '' }); 
            renderLvlRewards(); 
        }

        // -----------------------------------------------------------
        // 9. المحرك العملاق للبانلات والنوافذ الخاصة بها
        // -----------------------------------------------------------
        function renderAllPanels() {
            const container = document.getElementById('dynamic-panels-container'); 
            if(!container) return;
            container.innerHTML = '';
            
            if(panelsArray.length === 0) { 
                container.innerHTML = `<div class="text-center p-12 bg-dark-900/50 rounded-3xl border-2 border-dashed border-dark-700 text-slate-500 font-bold text-lg">لا توجد بانلات تذاكر حالياً. ابدأ بإضافة أول بانل لك.</div>`; 
                return; 
            }

            panelsArray.forEach((panel, pIdx) => {
                let buttonsHTML = '';
                
                (panel.buttons || []).forEach((btn, bIdx) => {
                    let modalHTML = '';
                    // إذا كان الزر يحتاج نافذة منبثقة، نعرض أسئلة النافذة
                    if(btn.requireModal) {
                        modalHTML += `
                            <div class="mt-6 p-6 bg-dark-900/80 rounded-2xl border border-dark-700 shadow-inner">
                                <div class="flex flex-col md:flex-row justify-between items-start md:items-center mb-6 border-b border-dark-600 pb-4 gap-4">
                                    <span class="text-base font-bold text-brand-light">📝 أسئلة النافذة المنبثقة للزر</span>
                                    <button type="button" onclick="if(!panelsArray[${pIdx}].buttons[${bIdx}].modalFields) panelsArray[${pIdx}].buttons[${bIdx}].modalFields=[]; panelsArray[${pIdx}].buttons[${bIdx}].modalFields.push({label:'',placeholder:'',style:'Short'}); renderAllPanels();" class="text-sm bg-brand hover:bg-brand-light text-white px-5 py-2.5 rounded-xl font-bold transition-all shadow-md w-full md:w-auto">➕ إضافة سؤال</button>
                                </div>
                        `;
                        (btn.modalFields || []).forEach((mf, mIdx) => {
                            modalHTML += `
                                <div class="flex flex-col space-y-4 mb-5 bg-dark-800 p-5 rounded-2xl border border-dark-600 relative">
                                    <button type="button" onclick="panelsArray[${pIdx}].buttons[${bIdx}].modalFields.splice(${mIdx},1); renderAllPanels();" class="absolute top-4 left-4 bg-red-600 hover:bg-red-500 text-white px-4 py-2 rounded-lg font-bold transition-all shadow-sm text-xs">حذف</button>
                                    <div class="flex flex-col md:flex-row gap-4 mt-4">
                                        <input type="text" class="w-full md:w-1/2 bg-dark-900 border border-dark-700 text-sm text-white rounded-xl p-3.5 font-bold focus:border-brand outline-none" placeholder="السؤال الذي سيظهر للعميل" value="${mf.label}" onchange="panelsArray[${pIdx}].buttons[${bIdx}].modalFields[${mIdx}].label=this.value">
                                        <input type="text" class="w-full md:w-1/2 bg-dark-900 border border-dark-700 text-sm text-white rounded-xl p-3.5 focus:border-brand outline-none" placeholder="مثال إرشادي للإجابة (اختياري)" value="${mf.placeholder || ''}" onchange="panelsArray[${pIdx}].buttons[${bIdx}].modalFields[${mIdx}].placeholder=this.value">
                                    </div>
                                    <div class="flex flex-col space-y-3 bg-dark-900/50 p-4 rounded-xl border border-dark-700 w-full md:w-1/2">
                                        <label class="cursor-pointer flex items-center text-sm font-bold text-brand-light">
                                            <input type="radio" name="p_${pIdx}_b_${bIdx}_m_${mIdx}" value="Short" class="mr-3 ml-3 w-5 h-5 accent-brand" ${mf.style !== 'Paragraph' ? 'checked' : ''} onchange="panelsArray[${pIdx}].buttons[${bIdx}].modalFields[${mIdx}].style=this.value"> إجابة قصيرة (سطر واحد)
                                        </label>
                                        <label class="cursor-pointer flex items-center text-sm font-bold text-brand-light">
                                            <input type="radio" name="p_${pIdx}_b_${bIdx}_m_${mIdx}" value="Paragraph" class="mr-3 ml-3 w-5 h-5 accent-brand" ${mf.style === 'Paragraph' ? 'checked' : ''} onchange="panelsArray[${pIdx}].buttons[${bIdx}].modalFields[${mIdx}].style=this.value"> إجابة طويلة (فقرة كبيرة)
                                        </label>
                                    </div>
                                </div>
                            `;
                        });
                        modalHTML += `</div>`;
                    }

                    // تصميم كل زر في البانل
                    buttonsHTML += `
                        <div class="bg-dark-800 p-8 rounded-3xl border border-dark-600 mb-8 relative shadow-xl">
                            <button type="button" onclick="panelsArray[${pIdx}].buttons.splice(${bIdx},1); renderAllPanels();" class="absolute top-5 left-5 text-red-400 bg-red-500/10 hover:bg-red-500 hover:text-white px-5 py-2.5 rounded-xl text-sm font-bold transition-all">حذف هذا الزر 🗑️</button>
                            <h5 class="text-xl font-black text-white mb-8 border-b border-dark-700 pb-4">🔘 إعدادات الزر رقم ${bIdx + 1}</h5>
                            
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
                                <div>
                                    <label class="text-sm font-bold text-slate-400 block mb-3">اسم الزر (الذي سيظهر للعميل)</label>
                                    <input type="text" class="w-full bg-dark-900 border border-dark-700 text-base text-white font-bold rounded-xl p-4 focus:border-brand outline-none" value="${btn.label}" onchange="panelsArray[${pIdx}].buttons[${bIdx}].label=this.value">
                                </div>
                                <div>
                                    <label class="text-sm font-bold text-slate-400 block mb-3">لون الزر (ستايل ديسكورد)</label>
                                    <select class="w-full bg-dark-900 border border-dark-700 text-base font-bold text-white rounded-xl p-4 cursor-pointer outline-none" onchange="panelsArray[${pIdx}].buttons[${bIdx}].buttonStyle=this.value">
                                        <option value="Primary" ${btn.buttonStyle === 'Primary' ? 'selected' : ''}>أزرق (Primary)</option>
                                        <option value="Success" ${btn.buttonStyle === 'Success' ? 'selected' : ''}>أخضر (Success)</option>
                                        <option value="Danger" ${btn.buttonStyle === 'Danger' ? 'selected' : ''}>أحمر (Danger)</option>
                                        <option value="Secondary" ${btn.buttonStyle === 'Secondary' ? 'selected' : ''}>رمادي (Secondary)</option>
                                    </select>
                                </div>
                            </div>
                            
                            <div class="flex flex-col md:flex-row items-start md:items-center md:space-x-8 md:space-x-reverse space-y-4 md:space-y-0 text-base font-bold bg-dark-900/60 p-6 rounded-2xl border border-dark-700 mb-8 shadow-inner">
                                <label class="text-emerald-400 flex items-center cursor-pointer w-full md:w-auto">
                                    <input type="checkbox" ${btn.requireModal ? 'checked' : ''} onchange="panelsArray[${pIdx}].buttons[${bIdx}].requireModal=this.checked; if(this.checked && !panelsArray[${pIdx}].buttons[${bIdx}].modalFields) panelsArray[${pIdx}].buttons[${bIdx}].modalFields=[]; renderAllPanels();" class="mr-3 ml-3 w-6 h-6 accent-emerald-400"> 📝 طلب أسئلة منبثقة (Modal) قبل الفتح
                                </label>
                            </div>
                            
                            <h6 class="text-lg font-black text-brand-light mb-6 mt-8 border-t border-dark-700 pt-6">🎨 إيمبد التذكرة الداخلي الخاص بهذا الزر:</h6>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                                <div>
                                    <label class="text-xs font-bold text-slate-400 block mb-2">عنوان الإيمبد الداخلي (Title)</label>
                                    <input type="text" class="w-full bg-dark-900 border border-dark-700 text-sm text-white rounded-xl p-4 font-bold focus:border-brand outline-none" value="${btn.insideEmbedTitle || 'تذكرة جديدة'}" onchange="panelsArray[${pIdx}].buttons[${bIdx}].insideEmbedTitle=this.value">
                                </div>
                                <div>
                                    <label class="text-xs font-bold text-slate-400 block mb-2">وصف الإيمبد الداخلي (Description)</label>
                                    <input type="text" class="w-full bg-dark-900 border border-dark-700 text-sm text-white rounded-xl p-4 focus:border-brand outline-none" value="${btn.insideEmbedDesc || ''}" onchange="panelsArray[${pIdx}].buttons[${bIdx}].insideEmbedDesc=this.value">
                                </div>
                                <div class="md:col-span-2 bg-dark-900/40 p-6 rounded-2xl border border-dark-700 shadow-inner">
                                    <label class="text-sm font-black text-amber-400 block mb-4">🎨 لون إيمبد إجابات النافذة المخصص (المربعات - Code Blocks)</label>
                                    <div class="flex items-center space-x-6 space-x-reverse">
                                        <input type="color" class="w-16 h-14 bg-dark-900 rounded-xl p-1.5 cursor-pointer border-2 border-dark-600 shadow-sm" value="${btn.modalAnswersEmbedColor || '#2b2d31'}" onchange="panelsArray[${pIdx}].buttons[${bIdx}].modalAnswersEmbedColor=this.value">
                                        <input type="text" class="flex-1 bg-dark-900 text-lg font-mono uppercase text-center rounded-xl border border-dark-600 py-3 text-white" value="${btn.modalAnswersEmbedColor || '#2b2d31'}" readonly>
                                    </div>
                                </div>
                            </div>
                            ${modalHTML}
                        </div>
                    `;
                });

                // تجميع البانل بالكامل
                container.innerHTML += `
                    <div class="bg-dark-900/80 border-2 border-blue-500/40 rounded-[2.5rem] p-6 md:p-10 relative shadow-[0_10px_40px_rgba(0,0,0,0.5)] mb-12">
                        <button type="button" onclick="panelsArray.splice(${pIdx},1); renderAllPanels();" class="absolute top-6 left-6 md:top-8 md:left-8 bg-red-600 hover:bg-red-500 text-white px-4 md:px-6 py-2 md:py-3.5 rounded-xl text-sm md:text-base font-black shadow-lg transition-all transform hover:scale-105 border border-red-400">حذف البانل 🗑️</button>
                        
                        <div class="flex flex-col md:flex-row md:items-center space-y-4 md:space-y-0 md:space-x-6 md:space-x-reverse mb-10 border-b-2 border-dark-700 pb-8 mt-10 md:mt-0">
                            <input type="color" class="w-20 h-20 rounded-2xl bg-dark-800 p-2 cursor-pointer border-2 border-dark-600 shadow-inner hidden md:block" value="${panel.panelColor || '#5865F2'}" onchange="panelsArray[${pIdx}].panelColor=this.value" title="لون البانل الخارجي">
                            <div class="flex-1 w-full">
                                <label class="text-sm font-bold text-slate-400 block mb-2">عنوان البانل الخارجي (الذي سيظهر في الروم)</label>
                                <input type="text" class="bg-transparent text-3xl md:text-4xl font-black text-white focus:border-b-4 focus:border-brand outline-none w-full md:w-3/4 pb-2 transition-all" value="${panel.panelTitle || ''}" onchange="panelsArray[${pIdx}].panelTitle=this.value">
                            </div>
                        </div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-10">
                            <div class="bg-dark-800 p-6 rounded-2xl border border-dark-600 shadow-inner">
                                <label class="text-base font-bold text-slate-300 block mb-4">روم إرسال البانل</label>
                                <select class="w-full bg-dark-900 text-white font-bold text-lg rounded-xl p-4 cursor-pointer outline-none border border-dark-700 focus:border-brand" onchange="panelsArray[${pIdx}].channelId=this.value">
                                    <option value="">-- يرجى اختيار الروم --</option>
                                    ${channelsHTML.replace(`value="${panel.channelId}"`, `value="${panel.channelId}" selected`)}
                                </select>
                            </div>
                            <div class="bg-dark-800 p-6 rounded-2xl border border-dark-600 shadow-inner">
                                <label class="text-base font-bold text-slate-300 block mb-4">قسم فتح التذاكر (Category)</label>
                                <select class="w-full bg-dark-900 text-white font-bold text-lg rounded-xl p-4 cursor-pointer outline-none border border-dark-700 focus:border-brand" onchange="panelsArray[${pIdx}].categoryId=this.value">
                                    <option value="">-- يرجى اختيار القسم --</option>
                                    ${categoriesHTML.replace(`value="${panel.categoryId}"`, `value="${panel.categoryId}" selected`)}
                                </select>
                            </div>
                            <div class="md:col-span-2 bg-dark-800 p-6 rounded-2xl border border-dark-600 shadow-inner">
                                <label class="text-base font-bold text-slate-300 block mb-4">وصف البانل الخارجي (Description)</label>
                                <textarea class="w-full bg-dark-900 text-white text-lg rounded-xl p-5 outline-none resize-y border border-dark-700 focus:border-brand leading-relaxed font-bold" rows="3" onchange="panelsArray[${pIdx}].panelDescription=this.value">${panel.panelDescription || ''}</textarea>
                            </div>
                        </div>

                        <div class="p-6 md:p-8 bg-dark-900/60 rounded-3xl border border-dark-700 shadow-inner">
                            <h4 class="text-2xl md:text-3xl font-black text-brand-light mb-8">🔘 أزرار البانل</h4>
                            ${buttonsHTML}
                            <button type="button" onclick="panelsArray[${pIdx}].buttons.push({id:'btn_'+Math.random().toString(36).substr(2,7),label:'زر جديد',buttonStyle:'Primary',requireModal:false,insideEmbedTitle:'تذكرة جديدة',modalAnswersEmbedColor:'#2b2d31'}); renderAllPanels();" class="w-full mt-6 py-6 border-4 border-dashed border-brand/40 bg-brand/5 hover:bg-brand/20 rounded-2xl text-brand-light font-black text-lg md:text-xl transition-all shadow-md transform hover:-translate-y-1">➕ إضافة زر جديد لهذا البانل</button>
                        </div>
                    </div>
                `;
            });
        }

        function addNewPanelUI() { 
            panelsArray.push({
                panelId: 'pnl_' + Math.random().toString(36).substr(2,7), 
                panelTitle: 'بانل تذاكر جديد', 
                panelColor: '#5865F2', 
                panelDescription: 'لطلب الدعم الفني يرجى الضغط على الزر أدناه.',
                buttons: []
            });
            renderAllPanels(); 
        }

        // -----------------------------------------------------------
        // 10. محرك الترجمة الديناميكي للداشبورد (اللغة)
        // -----------------------------------------------------------
        const systemLang = document.getElementById('system-language');
        function applyDashboardLanguage() {
            const isEnglish = systemLang && systemLang.value === 'en';
            if(isEnglish) {
                document.documentElement.setAttribute('dir', 'ltr');
                document.documentElement.setAttribute('lang', 'en');
                document.body.style.fontFamily = "'Segoe UI', Tahoma, Geneva, Verdana, sans-serif";
                document.querySelectorAll('[data-en]').forEach(el => {
                    if(!el.hasAttribute('data-ar')) el.setAttribute('data-ar', el.innerText);
                    el.innerText = el.getAttribute('data-en');
                });
                document.getElementById('save-btn-text').innerText = "Save Settings Now";
            } else {
                document.documentElement.setAttribute('dir', 'rtl');
                document.documentElement.setAttribute('lang', 'ar');
                document.body.style.fontFamily = "'Tajawal', sans-serif";
                document.querySelectorAll('[data-ar]').forEach(el => {
                    el.innerText = el.getAttribute('data-ar');
                });
                document.getElementById('save-btn-text').innerText = "حفظ التعديلات الآن";
            }
            // إعادة رسم التحذيرات حسب اللغة
            renderWarns(isEnglish ? 'en' : 'ar');
        }
        
        if(systemLang) {
            systemLang.addEventListener('change', applyDashboardLanguage);
            setTimeout(applyDashboardLanguage, 100); 
        }

        // -----------------------------------------------------------
        // 11. تشغيل كل المحركات عند فتح الصفحة تلقائياً
        // -----------------------------------------------------------
        window.onload = () => { 
            renderWarns('ar');
            renderWarns('en'); 
            renderMmFields(); 
            renderAllPanels(); 
            renderLvlRewards();
            renderAutoResponders();
            
            // إخفاء إشعار (تم الحفظ بنجاح) بعد 5 ثواني
            const alert = document.querySelector('.pop-down-alert');
            if(alert) {
                setTimeout(() => {
                    alert.style.display = 'none';
                }, 5000);
            }
        };
    </script>
</body>
</html>
