<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>إعدادات <%= guild.name %></title>
</head>
<body style="background: #23272a; color: white; font-family: sans-serif; padding: 50px;">
    <h1>لوحة تحكم <%= guild.name %> 🛠️</h1>
    <form action="/settings/<%= guild.id %>" method="POST">
        
        <h3>1. إعدادات التكت (يبدأ من <%= config.ticketCount %>)</h3>
        <label>اختار روم التكتات:</label>
        <select name="ticketChannelId">
            <% channels.filter(c => c.type === 0).forEach(channel => { %>
                <option value="<%= channel.id %>" <%= config.ticketChannelId === channel.id ? 'selected' : '' %>># <%= channel.name %></option>
            <% }) %>
        </select>

        <br><br>

        <label>اختار رتبة الإدارة (Staff Role):</label>
        <select name="staffRoleId">
            <% roles.forEach(role => { %>
                <option value="<%= role.id %>" <%= config.staffRoleId === role.id ? 'selected' : '' %>>@ <%= role.name %></option>
            <% }) %>
        </select>

        <br><br>
        <button type="submit" style="background: #43b581; color: white; padding: 10px 20px; border: none; border-radius: 5px; cursor: pointer;">حفظ التغييرات ✅</button>
    </form>
</body>
</html>
