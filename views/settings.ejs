<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إعدادات الإمبراطورية | MNC System</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
    
    <style>
        body { 
            font-family: 'Tajawal', sans-serif; 
            background-color: #121212; 
            color: #ffffff; 
            margin: 0; 
            display: flex; 
            height: 100vh; 
            overflow: hidden; 
        }
        
        .mobile-header { 
            display: none; 
            background-color: #1e1e24; 
            padding: 15px 20px; 
            align-items: center; 
            justify-content: space-between; 
            border-bottom: 2px solid #5865F2; 
        }
        
        .menu-btn { 
            background: #5865F2; 
            color: white; 
            border: none; 
            padding: 8px 15px; 
            border-radius: 5px; 
            cursor: pointer; 
            font-family: 'Tajawal', sans-serif; 
            font-weight: bold;
        }
        
        .sidebar { 
            width: 320px; 
            background-color: #1e1e24; 
            padding: 20px; 
            display: flex; 
            flex-direction: column; 
            gap: 10px; 
            overflow-y: auto; 
            z-index: 1000; 
            border-left: 1px solid #333; 
        }
        
        .sidebar-header {
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            border-bottom: 1px solid #333; 
            padding-bottom: 10px; 
            margin-bottom: 15px;
        }
        
        .sidebar h2 { 
            color: #5865F2; 
            font-size: 22px; 
            margin: 0; 
        }

        .close-sidebar-btn {
            background: transparent;
            border: none;
            color: #ff4d4d;
            font-size: 20px;
            cursor: pointer;
            display: none;
        }
        
        .tab-btn { 
            background: #2b2b36; 
            color: white; 
            border: none; 
            padding: 15px; 
            text-align: right; 
            font-size: 16px; 
            border-radius: 8px; 
            cursor: pointer; 
            transition: 0.3s; 
            font-family: 'Tajawal', sans-serif; 
        }
        
        .tab-btn:hover { 
            background: #3b3b4f; 
        }
        
        .tab-btn.active { 
            background: #5865F2; 
            font-weight: bold; 
            border-right: 5px solid #ffffff; 
        }
        
        .content { 
            flex: 1; 
            padding: 30px; 
            overflow-y: auto; 
            background-color: #18181b; 
        }
        
        .tab-content { 
            display: none; 
            background: #23232a; 
            padding: 25px; 
            border-radius: 10px; 
            box-shadow: 0 4px 15px rgba(0,0,0,0.3); 
            animation: fadeIn 0.4s; 
        }
        
        .tab-content.active { 
            display: block; 
        }
        
        @keyframes fadeIn { 
            from { opacity: 0; transform: translateY(10px); } 
            to { opacity: 1; transform: translateY(0); } 
        }
        
        .form-group { 
            margin-bottom: 20px; 
        }
        
        .form-group label { 
            display: block; 
            margin-bottom: 8px; 
            color: #b9bbbe; 
            font-weight: bold; 
        }
        
        .form-group input[type="text"], 
        .form-group input[type="number"], 
        .form-group select, 
        .form-group textarea { 
            width: 100%; 
            padding: 12px; 
            background: #2f3136; 
            border: 1px solid #202225; 
            color: white; 
            border-radius: 5px; 
            box-sizing: border-box; 
            font-family: 'Tajawal', sans-serif; 
            font-size: 15px;
        }
        
        .save-btn { 
            background: #3ba55d; 
            color: white; 
            padding: 15px; 
            border: none; 
            border-radius: 5px; 
            font-size: 18px; 
            cursor: pointer; 
            width: 100%; 
            font-weight: bold; 
            font-family: 'Tajawal', sans-serif; 
            margin-top: 20px; 
            transition: 0.3s; 
            box-shadow: 0 4px 10px rgba(59, 165, 93, 0.4);
        }
        
        .save-btn:hover { 
            background: #2d7d46; 
            transform: translateY(-2px);
        }
        
        .builder-box { 
            border: 1px solid #5865F2; 
            padding: 20px; 
            border-radius: 8px; 
            margin-bottom: 15px; 
            background: #1e1e24; 
        }
        
        .cmd-box { 
            background: #1e1e24; 
            border: 1px solid #444; 
            padding: 15px; 
            border-radius: 8px; 
        }
        
        hr { 
            border-color: #333; 
            margin: 20px 0; 
        }
        
        .section-title { 
            color: #f2a658; 
            border-bottom: 2px solid #333; 
            padding-bottom: 10px; 
            margin-top: 30px; 
            margin-bottom: 20px;
        }
        
        @media (max-width: 768px) { 
            body { flex-direction: column; overflow: auto; } 
            .mobile-header { display: flex; } 
            .sidebar { display: none; width: 100%; height: auto;} 
            .sidebar.open { display: flex; } 
            .close-sidebar-btn { display: block; }
            .content { padding: 15px; } 
        }
    </style>
</head>
<body>

    <div class="mobile-header">
        <h2 style="margin: 0; color: white;">MNC Control</h2>
        <button class="menu-btn" onclick="document.getElementById('sidebar').classList.toggle('open')">☰ القائمة المتقدمة</button>
    </div>

    <div class="sidebar" id="sidebar">
        
        <div class="sidebar-header">
            <h2>👑 Control Panel</h2>
            <button class="close-sidebar-btn" onclick="document.getElementById('sidebar').classList.remove('open')">❌</button>
        </div>

        <button class="tab-btn active" onclick="openTab(event, 'general')">⚙️ الإعدادات والترحيب</button>
        <button class="tab-btn" onclick="openTab(event, 'warns_games')">⚠️ التحذيرات والألعاب</button>
        <button class="tab-btn" onclick="openTab(event, 'tickets_panel')">🎟️ التكتات الخارجية (البانر)</button>
        <button class="tab-btn" onclick="openTab(event, 'tickets_builder')" style="border-right: 3px solid #f2a658;">🎛️ صانع التكتات والنوافذ</button>
        <button class="tab-btn" onclick="openTab(event, 'roles')">👨‍⚖️ الرتب واستلام التكت</button>
        <button class="tab-btn" onclick="openTab(event, 'commands')">⌨️ الأوامر الديناميكية (والفك)</button>
        <button class="tab-btn" onclick="openTab(event, 'punishments')" style="border-right: 3px solid #e74c3c;">🔨 تحكم العقوبات (باند/تايم)</button>
        <button class="tab-btn" onclick="openTab(event, 'logs')">📁 جميع سجلات اللوج</button>
        <button class="tab-btn" onclick="openTab(event, 'embeds_control')" style="border-right: 3px solid #ed4245;">🎨 تحكم الإيمبدات والألوان</button>
        <button class="tab-btn" onclick="openTab(event, 'ratings_system')" style="border-right: 3px solid #f1c40f;">⭐ نظام التقييمات الشامل</button>
        <button class="tab-btn" onclick="openTab(event, 'embedBuilder')" style="border-right: 3px solid #9b59b6;">🚀 صانع الإيمبد الحر</button>
        
        <a href="/dashboard" style="color: #ff4d4d; text-align: center; margin-top: auto; padding-top:20px; text-decoration: none; font-weight: bold;">⬅️ العودة للسيرفرات</a>
    </div>

    <div class="content">
        
        <% if (success === 'saved') { %> 
            <div style="background: #3ba55d; padding: 15px; text-align: center; border-radius: 5px; margin-bottom: 20px; font-weight: bold; font-size: 18px; color: white;">
                ✅ تمت مزامنة التعديلات مع الديسكورد بنجاح!
            </div> 
        <% } %>
        
        <form action="/settings/<%= guild.id %>" method="POST" id="mainForm">
            
            <input type="hidden" name="customButtonsData" id="customButtonsData">
            <input type="hidden" name="warnReasonsData" id="warnReasonsData">

            <div id="general" class="tab-content active">
                <h3 class="section-title">⚙️ الإعدادات العامة والحماية</h3>
                <div class="form-group">
                    <label>البريفكس (Prefix):</label>
                    <input type="text" name="prefix" value="<%= config.prefix || '!' %>">
                </div>
                
                <div class="form-group" style="background: #2b2b36; padding: 15px; border-radius: 8px;">
                    <label style="display:inline-block; margin:0; color:#f2a658;">🛡️ الحماية (تفعيل الإيقاف التلقائي):</label><br><br>
                    <input type="checkbox" name="antiLinks" style="width:20px; height:20px;" <%= config.antiLinks ? 'checked' : '' %>> <span style="font-size: 16px;">منع نشر الروابط</span> <br>
                    <input type="checkbox" name="antiSpam" style="width:20px; height:20px; margin-top: 10px;" <%= config.antiSpam ? 'checked' : '' %>> <span style="font-size: 16px;">منع السبام والتكرار</span>
                </div>
                
                <div class="form-group">
                    <label>رتبة الدخول التلقائية (Auto-Role):</label>
                    <select name="autoRoleId">
                        <option value="">-- معطل --</option>
                        <% roles.forEach(r => { %>
                            <option value="<%= r.id %>" <%= config.autoRoleId === r.id ? 'selected' : '' %>><%= r.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <h3 class="section-title">🖼️ نظام الترحيب المتطور (بالصور)</h3>
                <div style="background:#2f3136; padding:15px; border-radius:8px; margin-bottom:15px; border-right: 4px solid #5865F2;">
                    💡 <b>المتغيرات المتاحة:</b><br>
                    <code>[user]</code> = منشن | <code>[server]</code> = السيرفر | <code>[memberCount]</code> = الرقم<br>
                </div>
                
                <div class="form-group">
                    <label>روم الترحيب:</label>
                    <select name="welcomeChannelId">
                        <option value="">-- معطل --</option>
                        <% channels.filter(c=>c.type===0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.welcomeChannelId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div class="form-group">
                    <label>رسالة الترحيب النصية (خارج الصورة):</label>
                    <textarea name="welcomeMessage" rows="3"><%= config.welcomeMessage || 'حياك الله يا [user] في [server]! أنت العضو رقم [memberCount].' %></textarea>
                </div>
                
                <div class="form-group">
                    <label>رابط صورة الخلفية (رابط مباشر):</label>
                    <input type="text" name="welcomeBgImage" value="<%= config.welcomeBgImage || '' %>" placeholder="https://example.com/bg.png">
                </div>
                
                <div class="form-group">
                    <label>لون إطار صورة العضو (Hex Color):</label>
                    <input type="color" name="welcomeAvatarBorderColor" value="<%= config.welcomeAvatarBorderColor || '#ffffff' %>" style="height: 40px; padding:0; width: 100px;">
                </div>
            </div>

            <div id="warns_games" class="tab-content">
                <h3 class="section-title">⚠️ لوحة التحكم بالتحذيرات (Warn Panel)</h3>
                <div class="form-group">
                    <label>الروم التي سترسل بها لوحة التحذيرات:</label>
                    <select name="warnPanelChannelId">
                        <option value="">-- معطل --</option>
                        <% channels.filter(c=>c.type===0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.warnPanelChannelId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div class="form-group">
                    <label>روم اللوج المخصصة لسجل التحذيرات:</label>
                    <select name="warnLogChannelId">
                        <option value="">-- اتبع اللوج العام --</option>
                        <% channels.filter(c=>c.type===0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.warnLogChannelId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div class="form-group">
                        <label>عنوان لوحة التحذير:</label>
                        <input type="text" name="warnPanelTitle" value="<%= config.warnPanelTitle || 'لوحة تحكم التحذيرات' %>">
                    </div>
                    <div class="form-group">
                        <label>لون الإيمبد:</label>
                        <input type="color" name="warnPanelColor" value="<%= config.warnPanelColor || '#ed4245' %>" style="height:40px; padding:0;">
                    </div>
                </div>
                
                <div class="form-group">
                    <label>وصف الإيمبد (التعليمات):</label>
                    <textarea name="warnPanelDesc" rows="2"><%= config.warnPanelDesc || 'استخدم الأزرار أدناه.' %></textarea>
                </div>
                
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div class="form-group">
                        <label>الحد الأقصى للتحذيرات (ويعاقب بعدها):</label>
                        <input type="number" name="warnMax" value="<%= config.warnMax || 3 %>">
                    </div>
                    <div class="form-group">
                        <label>العقاب التلقائي بعد تجاوز الحد:</label>
                        <select name="warnAction">
                            <option value="timeout" <%= config.warnAction === 'timeout' ? 'selected' : '' %>>Timeout (كتم)</option>
                            <option value="kick" <%= config.warnAction === 'kick' ? 'selected' : '' %>>Kick (طرد)</option>
                            <option value="ban" <%= config.warnAction === 'ban' ? 'selected' : '' %>>Ban (حظر)</option>
                        </select>
                    </div>
                </div>
                
                <div class="builder-box">
                    <h4 style="margin-top:0; color:#ed4245;">📝 الأسباب الجاهزة للتحذير</h4>
                    <div id="warnReasonsContainer"></div>
                    <button type="button" onclick="warnReasons.push('سبب جديد');renderWarnReasons();" style="background:#ed4245; border:none; color:white; padding:10px 20px; border-radius:5px; margin-top:10px; cursor:pointer; font-weight:bold;">➕ إضافة سبب للتحذير</button>
                </div>
            </div>

            <div id="tickets_panel" class="tab-content">
                <h3 class="section-title">🎟️ إعدادات بانر التكتات الخارجي</h3>
                <div class="form-group">
                    <label>الروم التي سيُرسل بها البانر:</label>
                    <select name="panelChannelId">
                        <option value="">-- اختر الروم --</option>
                        <% channels.filter(c=>c.type===0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.panelChannelId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                <div class="form-group">
                    <label>القسم الاحتياطي لفتح التكتات (Category):</label>
                    <select name="defaultCategoryId">
                        <option value="">-- اختر قسم --</option>
                        <% channels.filter(c=>c.type===4).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.defaultCategoryId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                <div class="form-group">
                    <label>عنوان إيمبد القوانين الخارجي:</label>
                    <input type="text" name="ticketEmbedTitle" value="<%= config.ticketEmbedTitle || 'الدعم الفني' %>">
                </div>
                <div class="form-group">
                    <label>وصف الإيمبد الخارجي:</label>
                    <textarea name="ticketEmbedDesc" rows="4"><%= config.ticketEmbedDesc || 'اضغط لفتح تذكرة' %></textarea>
                </div>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div class="form-group">
                        <label>لون الإيمبد:</label>
                        <input type="color" name="ticketEmbedColor" value="<%= config.ticketEmbedColor || '#0099ff' %>" style="height:40px; padding:0;">
                    </div>
                    <div class="form-group">
                        <label>رقم التكت القادم:</label>
                        <input type="number" name="ticketCount" value="<%= config.ticketCount || 0 %>">
                    </div>
                    <div class="form-group">
                        <label>الحد الأقصى للتكتات المفتوحة للعضو:</label>
                        <input type="number" name="maxTicketsPerUser" value="<%= config.maxTicketsPerUser || 1 %>">
                    </div>
                    <div class="form-group">
                        <label>رابط صورة كبيرة للبانر (اختياري):</label>
                        <input type="text" name="ticketEmbedImage" value="<%= config.ticketEmbedImage || '' %>">
                    </div>
                </div>
            </div>

            <div id="tickets_builder" class="tab-content">
                <h3 class="section-title">🎛️ صانع التكتات التفصيلي</h3>
                <div id="buttonsContainer"></div>
                <button type="button" onclick="addNewButton()" style="background:#f2a658; color:white; border:none; padding:15px; width:100%; font-size:18px; font-weight:bold; border-radius:8px; cursor:pointer; margin-top:20px;">➕ إضافة زر تكت جديد بالكامل</button>
            </div>

            <div id="roles" class="tab-content">
                <h3 class="section-title">👨‍⚖️ نظام الرتب الموحد</h3>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    <div class="form-group">
                        <label>👮 رتبة الإدارة الصغرى:</label>
                        <select name="adminRoleId">
                            <option value="">-- غير محدد --</option>
                            <% roles.forEach(r => { %>
                                <option value="<%= r.id %>" <%= config.adminRoleId === r.id ? 'selected' : '' %>><%= r.name %></option>
                            <% }) %>
                        </select>
                    </div>
                    <div class="form-group">
                        <label>👑 رتب الإدارة العليا (متعدد):</label>
                        <select name="highAdminRoles" multiple style="height:100px;">
                            <% roles.forEach(r => { %>
                                <option value="<%= r.id %>" <%= config.highAdminRoles && config.highAdminRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                            <% }) %>
                        </select>
                    </div>
                </div>
                <hr>
                <h3 class="section-title">🕵️‍♂️ تحكم استلام التكت (Claim)</h3>
                <div class="form-group" style="background: #2b2b36; padding: 15px; border-radius: 8px; border-right: 4px solid #5865F2;">
                    <label style="display:inline-block; margin-bottom:0;">إخفاء التكت عن باقي الإدارة بمجرد الاستلام:</label>
                    <input type="checkbox" id="hideToggle" name="hideTicketOnClaim" style="width: 20px; height: 20px; float:left;" <%= config.hideTicketOnClaim ? 'checked' : '' %>>
                </div>
            </div>

            <div id="commands" class="tab-content">
                <h3 class="section-title">⌨️ تخصيص الأوامر والصلاحيات (شاملة النقل والفك)</h3>
                <div style="display:grid; grid-template-columns: 1fr 1fr; gap:20px;">
                    <% 
                    const cmdsList = [
                        {id: 'cmdDone', label: 'تقييم الوسيط (!done)'}, 
                        {id: 'cmdTrade', label: 'نافذة التريد (!trade)'},
                        {id: 'cmdBan', label: 'إعطاء باند (!ban)'}, 
                        {id: 'cmdUnban', label: 'فك الباند (!unban)'},
                        {id: 'cmdTimeout', label: 'إعطاء تايم أوت (!timeout)'}, 
                        {id: 'cmdUntimeout', label: 'فك التايم أوت (!untimeout)'},
                        {id: 'cmdVmove', label: 'سحب عضو لفويسك (!vmove)'}, 
                        {id: 'cmdMove', label: 'نقل عضو لروم محددة (!move)'},
                        {id: 'cmdClear', label: 'مسح الشات (!clear)'}
                    ]; 
                    
                    cmdsList.forEach(cmdObj => { 
                    %>
                        <div class="cmd-box">
                            <label style="color:#5865F2; font-size:16px;">🔥 أمر <%= cmdObj.label %>:</label>
                            <input type="text" name="<%= cmdObj.id %>" value="<%= config[cmdObj.id] %>" style="margin-bottom:15px;">
                            <label style="font-size:14px; color:#a3a6aa;">الرتب المسموح لها:</label>
                            <select name="<%= cmdObj.id %>Roles" multiple style="height:80px;">
                                <% roles.forEach(r => { %>
                                    <option value="<%= r.id %>" <%= config[cmdObj.id+'Roles'] && config[cmdObj.id+'Roles'].includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                                <% }) %>
                            </select>
                        </div>
                    <% }) %>
                </div>
            </div>

            <div id="punishments" class="tab-content">
                <h3 class="section-title">🔨 تحكم إيمبدات العقوبات (باند، تايم أوت، فك)</h3>
                
                <div class="form-group" style="background:#2b2b36; padding:15px; border-radius:8px;">
                    <label style="color:#e74c3c; font-size:18px;">نوع إيمبد العقوبة المُرسل بالشات:</label>
                    <select name="punishmentStyle" onchange="toggleCustomPunishment(this.value)">
                        <option value="basic" <%= config.punishmentStyle === 'basic' ? 'selected' : '' %>>الأساسي (شكل الديسكورد العادي المنسق)</option>
                        <option value="custom" <%= config.punishmentStyle === 'custom' ? 'selected' : '' %>>مخصص (أنت تكتب الرسالة والعنوان)</option>
                    </select>
                </div>

                <div id="customPunishmentBox" style="display: <%= config.punishmentStyle === 'custom' ? 'block' : 'none' %>; background:#1e1e24; padding:20px; border-radius:8px; border:1px solid #e74c3c;">
                    <p style="color:#a3a6aa; margin-bottom: 20px;">
                        المتغيرات المتاحة: <br>
                        <code>[user]</code> = منشن المعاقب | <code>[moderator]</code> = الإداري | <code>[reason]</code> = السبب | <code>[duration]</code> = المدة (للتايم فقط)
                    </p>

                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                        <div class="cmd-box">
                            <h4 style="color:#ed4245; margin-top:0;">إيمبد إعطاء الباند</h4>
                            <div class="form-group"><label>العنوان:</label><input type="text" name="customBanTitle" value="<%= config.customBanTitle || '🔨 تم حظر عضو' %>"></div>
                            <div class="form-group"><label>الوصف:</label><textarea name="customBanDesc" rows="3"><%= config.customBanDesc || 'تم حظر [user] بواسطة [moderator].\nالسبب: [reason]' %></textarea></div>
                        </div>

                        <div class="cmd-box">
                            <h4 style="color:#3ba55d; margin-top:0;">إيمبد فك الباند</h4>
                            <div class="form-group"><label>العنوان:</label><input type="text" name="customUnbanTitle" value="<%= config.customUnbanTitle || '🕊️ تم فك الحظر' %>"></div>
                            <div class="form-group"><label>الوصف:</label><textarea name="customUnbanDesc" rows="3"><%= config.customUnbanDesc || 'تم فك الحظر عن [user] بواسطة [moderator].' %></textarea></div>
                        </div>

                        <div class="cmd-box">
                            <h4 style="color:#f2a658; margin-top:0;">إيمبد التايم أوت</h4>
                            <div class="form-group"><label>العنوان:</label><input type="text" name="customTimeoutTitle" value="<%= config.customTimeoutTitle || '⏳ تم إعطاء تايم أوت' %>"></div>
                            <div class="form-group"><label>الوصف:</label><textarea name="customTimeoutDesc" rows="3"><%= config.customTimeoutDesc || 'تم معاقبة [user] بواسطة [moderator] لمدة [duration] دقيقة.\nالسبب: [reason]' %></textarea></div>
                        </div>

                        <div class="cmd-box">
                            <h4 style="color:#3ba55d; margin-top:0;">إيمبد فك التايم أوت</h4>
                            <div class="form-group"><label>العنوان:</label><input type="text" name="customUntimeoutTitle" value="<%= config.customUntimeoutTitle || '🔊 تم فك التايم أوت' %>"></div>
                            <div class="form-group"><label>الوصف:</label><textarea name="customUntimeoutDesc" rows="3"><%= config.customUntimeoutDesc || 'تم فك التايم أوت عن [user] بواسطة [moderator].' %></textarea></div>
                        </div>
                    </div>
                </div>
            </div>

            <div id="embeds_control" class="tab-content">
                <h3 class="section-title">🎨 تحكم ألوان الإيمبدات الشامل</h3>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    <div class="form-group cmd-box"><label style="color:white;">لون إيمبد إجابات النوافذ (في التكت):</label><input type="color" name="answersEmbedColor" value="<%= config.answersEmbedColor || '#2b2d31' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:white;">لون إيمبد القفل (Close):</label><input type="color" name="closeEmbedColor" value="<%= config.closeEmbedColor || '#2b2d31' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:white;">لون إيمبد طلب التريد (!trade):</label><input type="color" name="tradeEmbedColor" value="<%= config.tradeEmbedColor || '#f2a658' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:white;">لون إيمبد اللوج العادي (Ticket Logs):</label><input type="color" name="logEmbedColor" value="<%= config.logEmbedColor || '#ed4245' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:white;">لون إيمبد الترانسكريبت (Transcript):</label><input type="color" name="transcriptEmbedColor" value="<%= config.transcriptEmbedColor || '#2b2d31' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                </div>
            </div>

            <div id="ratings_system" class="tab-content">
                <h3 class="section-title">⭐ التحكم في رسالة التقييم بالخاص</h3>
                <div class="form-group" style="background:#2b2b36; padding:15px; border-radius:8px;">
                    <label style="color:#5865F2; font-size:18px;">نوع التقييم المُرسل:</label>
                    <select name="ratingStyle" onchange="toggleCustomRating(this.value)">
                        <option value="basic" <%= config.ratingStyle === 'basic' ? 'selected' : '' %>>الأساسي العربي</option>
                        <option value="custom" <%= config.ratingStyle === 'custom' ? 'selected' : '' %>>تقييم يدوي مخصص</option>
                    </select>
                </div>

                <div id="customRatingBox" style="display: <%= config.ratingStyle === 'custom' ? 'block' : 'none' %>; background:#1e1e24; padding:20px; border-radius:8px; border:1px solid #f2a658;">
                    <p style="color:#a3a6aa;">المتغيرات المتاحة: <code>[user]</code> للعميل، <code>[staff]</code> للإداري/الوسيط، <code>[server]</code> لاسم السيرفر.</p>
                    <div class="form-group"><label>عنوان إيمبد التقييم المخصص:</label><input type="text" name="customRatingTitle" value="<%= config.customRatingTitle || 'تقييم فريق العمل' %>"></div>
                    <div class="form-group"><label>وصف رسالة التقييم المخصصة:</label><textarea name="customRatingText" rows="4"><%= config.customRatingText || 'مرحباً [user]، يرجى تقييم خدمة [staff] في سيرفر [server].' %></textarea></div>
                </div>
            </div>

            <button type="submit" class="save-btn" id="mainSaveBtn" onclick="prepareData()">💾 حفظ جميع الإعدادات وتحديث البوت فوراً!</button>
        </form>
    </div>

    <script>
        function openTab(eventObj, tabName) {
            document.querySelectorAll(".tab-content").forEach(c => c.style.display="none");
            document.querySelectorAll(".tab-btn").forEach(b => b.classList.remove("active"));
            document.getElementById(tabName).style.display="block"; 
            eventObj.currentTarget.classList.add("active");
            if (window.innerWidth <= 768) document.getElementById('sidebar').classList.remove('open');
        }
        
        function toggleCustomRating(val) { document.getElementById('customRatingBox').style.display = (val === 'custom') ? 'block' : 'none'; }
        function toggleCustomPunishment(val) { document.getElementById('customPunishmentBox').style.display = (val === 'custom') ? 'block' : 'none'; }

        // صانع الأزرار
        let buttonsDataArray = <%- JSON.stringify(config.customButtons || []) %>;
        const serverRolesArray = <%- JSON.stringify(roles || []) %>;
        const catOptionsHTML = `<option value="">-- اتبع القسم الاحتياطي --</option><%- channels.filter(c=>c.type===4).map(c=>`<option value="${c.id}">${c.name}</option>`).join('') %>`;
        
        function renderButtons() {
            const containerDiv = document.getElementById('buttonsContainer'); 
            containerDiv.innerHTML = '';
            
            for (let bIndex = 0; bIndex < buttonsDataArray.length; bIndex++) {
                const btnObj = buttonsDataArray[bIndex];
                
                let fieldsHTML = ''; 
                if (btnObj.modalFields) {
                    for (let fIndex = 0; fIndex < btnObj.modalFields.length; fIndex++) {
                        const fieldObj = btnObj.modalFields[fIndex];
                        fieldsHTML += `
                        <div style="background:#23232a; padding:10px; margin-top:10px; border-right:3px solid #f2a658;">
                            <label>سؤال (Label) ${fIndex + 1}:</label>
                            <input type="text" value="${fieldObj.label}" onchange="buttonsDataArray[${bIndex}].modalFields[${fIndex}].label=this.value">
                            <button type="button" onclick="buttonsDataArray[${bIndex}].modalFields.splice(${fIndex},1);renderButtons();" style="background:red; color:white; border:none; padding:5px 10px; margin-top:5px; border-radius:3px; cursor:pointer;">حذف السؤال</button>
                        </div>`;
                    }
                }

                let rolesOptionsHTML = '';
                for (let rIndex = 0; rIndex < serverRolesArray.length; rIndex++) {
                    const roleObj = serverRolesArray[rIndex];
                    let isRoleSelected = (btnObj.allowedClaimRoles && btnObj.allowedClaimRoles.includes(roleObj.id)) ? 'selected' : '';
                    rolesOptionsHTML += `<option value="${roleObj.id}" ${isRoleSelected}>${roleObj.name}</option>`;
                }

                if (btnObj.enableRating === undefined) btnObj.enableRating = true;
                
                containerDiv.innerHTML += `
                    <div class="builder-box">
                        <h3 style="color:#5865F2; margin-top:0;">🔘 ${btnObj.label}</h3>
                        <div style="display:grid; grid-template-columns: 1fr 1fr; gap:15px;">
                            <div><label>الـ ID البرمجي:</label><input type="text" value="${btnObj.id}" onchange="buttonsDataArray[${bIndex}].id=this.value"></div>
                            <div><label>اسم الزرار:</label><input type="text" value="${btnObj.label}" onchange="buttonsDataArray[${bIndex}].label=this.value"></div>
                            <div><label>لون الزرار:</label><select onchange="buttonsDataArray[${bIndex}].color=this.value"><option value="Primary" ${btnObj.color=='Primary'?'selected':''}>أزرق</option><option value="Success" ${btnObj.color=='Success'?'selected':''}>أخضر</option><option value="Danger" ${btnObj.color=='Danger'?'selected':''}>أحمر</option><option value="Secondary" ${btnObj.color=='Secondary'?'selected':''}>رمادي</option></select></div>
                            <div><label>القسم المخصص:</label><select onchange="buttonsDataArray[${bIndex}].categoryId=this.value">${catOptionsHTML.replace(`value="${btnObj.categoryId}"`, `value="${btnObj.categoryId}" selected`)}</select></div>
                        </div>

                        <h4 style="color:#3ba55d; margin-top:20px;">🎨 الإيمبد الداخلي</h4>
                        <div style="display:grid; grid-template-columns: 1fr; gap:10px;">
                            <div><label>عنوان الإيمبد:</label><input type="text" value="${btnObj.insideEmbedTitle}" onchange="buttonsDataArray[${bIndex}].insideEmbedTitle=this.value"></div>
                            <div><label>وصف الإيمبد:</label><textarea rows="3" onchange="buttonsDataArray[${bIndex}].insideEmbedDesc=this.value">${btnObj.insideEmbedDesc}</textarea></div>
                            <div><label>لون الإيمبد:</label><input type="color" value="${btnObj.insideEmbedColor}" onchange="buttonsDataArray[${bIndex}].insideEmbedColor=this.value" style="height:40px; padding:0; width:100px;"></div>
                        </div>

                        <h4 style="color:#f2a658; margin-top:20px;">🕵️‍♂️ رتب الاستلام المخصصة</h4>
                        <select multiple style="height:100px; width:100%;" onchange="const selectedOpts = Array.from(this.selectedOptions).map(opt=>opt.value); buttonsDataArray[${bIndex}].allowedClaimRoles = selectedOpts;">${rolesOptionsHTML}</select>

                        <h4 style="color:#9b59b6; margin-top:20px;">🪟 النافذة والتقييم</h4>
                        <div style="margin-bottom:15px; background: #18181b; padding: 15px; border-radius: 8px;">
                            <input type="checkbox" ${btnObj.requireModal?'checked':''} onchange="buttonsDataArray[${bIndex}].requireModal=this.checked;renderButtons();" style="width:18px;height:18px;"> <label style="display:inline;">تفعيل النافذة</label>
                            <span style="margin:0 15px;">|</span>
                            <input type="checkbox" ${btnObj.isMediator?'checked':''} onchange="buttonsDataArray[${bIndex}].isMediator=this.checked;" style="width:18px;height:18px;"> <label style="display:inline;">تكت وساطة</label>
                            <span style="margin:0 15px;">|</span>
                            <input type="checkbox" ${btnObj.enableRating!==false?'checked':''} onchange="buttonsDataArray[${bIndex}].enableRating=this.checked;" style="width:18px;height:18px;"> <label style="display:inline;">إرسال تقييم للإدارة</label>
                        </div>

                        <div style="display:${btnObj.requireModal?'block':'none'}; background:#2f3136; padding:15px; border-radius:5px;">
                            <label>عنوان النافذة (Modal Title):</label>
                            <input type="text" value="${btnObj.modalTitle}" onchange="buttonsDataArray[${bIndex}].modalTitle=this.value">
                            ${fieldsHTML}
                            <button type="button" onclick="if(!buttonsDataArray[${bIndex}].modalFields) buttonsDataArray[${bIndex}].modalFields=[]; buttonsDataArray[${bIndex}].modalFields.push({label:'سؤال جديد',required:true});renderButtons();" style="background:#3ba55d; color:white; border:none; padding:10px; margin-top:10px; border-radius:5px; cursor:pointer;">➕ إضافة سؤال</button>
                        </div>
                        <button type="button" onclick="buttonsDataArray.splice(${bIndex},1);renderButtons();" style="background:#ed4245; color:white; border:none; padding:15px; width:100%; margin-top:20px; font-weight:bold; border-radius:5px; cursor:pointer;">🗑️ حذف الزر</button>
                    </div>`;
            }
        } 
        renderButtons();

        function addNewButton() { 
            buttonsDataArray.push({id: 'btn_' + Date.now(), label: 'زر جديد', color: 'Primary', categoryId: '', insideEmbedTitle: 'تذكرة جديدة', insideEmbedDesc: 'اكتب طلبك...', insideEmbedColor: '#2b2d31', requireModal: false, modalTitle: 'بيانات مطلوبة', modalFields: [], isMediator: false, enableRating: true, allowedClaimRoles: []}); 
            renderButtons(); 
        }

        function prepareData() { 
            document.getElementById('customButtonsData').value = JSON.stringify(buttonsDataArray); 
        }
    </script>
</body>
</html>
