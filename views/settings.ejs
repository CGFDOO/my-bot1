<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إعدادات MNC | لوحة التحكم الشاملة</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Tajawal', sans-serif; background-color: #121212; color: #ffffff; margin: 0; display: flex; height: 100vh; overflow: hidden; }
        .mobile-header { display: none; background-color: #1e1e24; padding: 15px 20px; align-items: center; justify-content: space-between; border-bottom: 2px solid #5865F2; }
        .menu-btn { background: #5865F2; color: white; border: none; padding: 8px 15px; border-radius: 5px; cursor: pointer; }
        .sidebar { width: 260px; background-color: #1e1e24; padding: 20px; display: flex; flex-direction: column; gap: 10px; overflow-y: auto; z-index: 1000; border-left: 1px solid #333; }
        .sidebar h2 { color: #5865F2; font-size: 22px; text-align: center; margin-bottom: 15px; }
        .tab-btn { background: #2b2b36; color: white; border: none; padding: 12px; text-align: right; font-size: 15px; border-radius: 8px; cursor: pointer; transition: 0.3s; font-family: 'Tajawal', sans-serif; }
        .tab-btn:hover { background: #3b3b4f; }
        .tab-btn.active { background: #5865F2; font-weight: bold; }
        .content { flex: 1; padding: 30px; overflow-y: auto; background-color: #18181b; }
        .tab-content { display: none; background: #23232a; padding: 25px; border-radius: 10px; box-shadow: 0 4px 15px rgba(0,0,0,0.3); animation: fadeIn 0.4s; }
        .tab-content.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
        .form-group { margin-bottom: 20px; }
        .form-group label { display: block; margin-bottom: 8px; color: #b9bbbe; font-weight: bold; }
        .form-group input, .form-group select, .form-group textarea { width: 100%; padding: 10px; background: #2f3136; border: 1px solid #202225; color: white; border-radius: 5px; box-sizing: border-box; font-family: 'Tajawal', sans-serif; }
        .save-btn { background: #3ba55d; color: white; padding: 15px; border: none; border-radius: 5px; font-size: 18px; cursor: pointer; width: 100%; font-weight: bold; font-family: 'Tajawal', sans-serif; margin-top: 20px; }
        .save-btn:hover { background: #2d7d46; }
        .builder-box { border: 1px solid #5865F2; padding: 15px; border-radius: 8px; margin-bottom: 15px; background: #1e1e24; }
        .add-btn { background: #5865F2; color: white; padding: 8px 15px; border: none; border-radius: 5px; cursor: pointer; font-family: 'Tajawal', sans-serif; }
        hr { border-color: #333; margin: 20px 0; }
        @media (max-width: 768px) { body { flex-direction: column; overflow: auto; } .mobile-header { display: flex; } .sidebar { position: fixed; right: -300px; top: 0; height: 100%; width: 250px; } .sidebar.open { right: 0; } .content { padding: 15px; } }
    </style>
</head>
<body>

    <div class="mobile-header">
        <h2 style="margin: 0; color: white;">إعدادات MNC</h2>
        <button class="menu-btn" onclick="toggleSidebar()">☰ القائمة</button>
    </div>

    <div class="sidebar" id="sidebar">
        <h2>🛠️ لوحة التحكم</h2>
        <button class="tab-btn active" onclick="openTab(event, 'general')">⚙️ الإعدادات والترحيب</button>
        <button class="tab-btn" onclick="openTab(event, 'tickets_panel')">🎟️ بانر التكتات الخارجي</button>
        <button class="tab-btn" onclick="openTab(event, 'tickets_builder')" style="border: 1px solid #f2a658;">🎛️ صانع الزراير والنوافذ</button>
        <button class="tab-btn" onclick="openTab(event, 'roles')">👨‍⚖️ الرتب والتحكم</button>
        <button class="tab-btn" onclick="openTab(event, 'commands')">⌨️ الأوامر الشاملة</button>
        <button class="tab-btn" onclick="openTab(event, 'logs')">📁 جميع اللوجات</button>
        <button class="tab-btn" onclick="openTab(event, 'embedBuilder')" style="border: 1px solid #5865F2;">🎨 صانع الإيمبد الحر</button>
        <a href="/dashboard" style="color: #ff4d4d; text-align: center; text-decoration: none; margin-top: 10px;">⬅️ العودة</a>
    </div>

    <div class="content">
        <% if (success === 'saved') { %> <div style="background: #3ba55d; padding: 10px; border-radius: 5px; text-align: center; margin-bottom: 20px; font-weight: bold;">✅ تم حفظ التعديلات بنجاح!</div> <% } %>
        
        <form action="/settings/<%= guild.id %>" method="POST" id="mainForm">
            <input type="hidden" name="customButtonsData" id="customButtonsData">
            <input type="hidden" name="autoRespondersData" id="autoRespondersData">

            <div id="general" class="tab-content active">
                <h3>⚙️ الإعدادات العامة</h3>
                <div class="form-group"><label>البريفكس (Prefix):</label><input type="text" name="prefix" value="<%= config.prefix %>"></div>
                <div class="form-group"><label>🛡️ الحماية:</label>
                    <input type="checkbox" name="antiLinks" <%= config.antiLinks ? 'checked' : '' %>> منع الروابط | 
                    <input type="checkbox" name="antiSpam" <%= config.antiSpam ? 'checked' : '' %>> منع السبام
                </div>
                <div class="form-group"><label>رتبة الدخول التلقائية:</label><select name="autoRoleId"><option value="">-- معطل --</option><% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.autoRoleId === r.id ? 'selected' : '' %>><%= r.name %></option><% }) %></select></div>
                
                <hr>
                <h3>🖼️ نظام الترحيب المتطور (Welcome)</h3>
                <div class="form-group"><label>روم الترحيب:</label><select name="welcomeChannelId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c => { %><option value="<%= c.id %>" <%= config.welcomeChannelId === c.id ? 'selected' : '' %>><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>رسالة الترحيب (استخدم [user], [server], [memberCount]):</label><textarea name="welcomeMessage"><%= config.welcomeMessage %></textarea></div>
                <div class="form-group"><label>رابط صورة الخلفية (Background URL):</label><input type="text" name="welcomeBgImage" value="<%= config.welcomeBgImage %>" placeholder="https://..."></div>
                <div class="form-group"><label>لون إطار الصورة (Avatar Border Hex):</label><input type="color" name="welcomeAvatarBorderColor" value="<%= config.welcomeAvatarBorderColor || '#ffffff' %>" style="height: 40px; padding:0;"></div>

                <hr>
                <h3>⚠️ نظام التحذيرات والاقتراحات</h3>
                <div class="form-group"><label>روم اللوج للتحذيرات:</label><select name="warnLogChannelId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c => { %><option value="<%= c.id %>" <%= config.warnLogChannelId === c.id ? 'selected' : '' %>><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>الحد الأقصى للتحذيرات (قبل العقاب):</label><input type="number" name="warnMax" value="<%= config.warnMax %>"></div>
                <div class="form-group"><label>روم الاقتراحات (Suggestions):</label><select name="suggestionChannelId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c => { %><option value="<%= c.id %>" <%= config.suggestionChannelId === c.id ? 'selected' : '' %>><%= c.name %></option><% }) %></select></div>
            </div>

            <div id="tickets_panel" class="tab-content">
                <h3>🎟️ إعدادات بانر التكتات الخارجي (اللي بيظهر للناس)</h3>
                <div class="form-group"><label>الروم التي سيُرسل بها البانر:</label><select name="panelChannelId"><option value="">-- اختر الروم --</option><% channels.filter(c=>c.type===0).forEach(c => { %><option value="<%= c.id %>" <%= config.panelChannelId === c.id ? 'selected' : '' %>><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>عنوان الإيمبد الخارجي:</label><input type="text" name="ticketEmbedTitle" value="<%= config.ticketEmbedTitle %>"></div>
                <div class="form-group"><label>القوانين / الوصف الخارجي:</label><textarea name="ticketEmbedDesc"><%= config.ticketEmbedDesc %></textarea></div>
                <div class="form-group"><label>لون الإيمبد:</label><input type="color" name="ticketEmbedColor" value="<%= config.ticketEmbedColor %>" style="height:40px; padding:0;"></div>
                <div class="form-group"><label>رابط صورة للبانر (اختياري):</label><input type="text" name="ticketEmbedImage" value="<%= config.ticketEmbedImage %>"></div>
                <div class="form-group"><label>الحد الأقصى للتكتات المفتوحة لكل عضو:</label><input type="number" name="maxTicketsPerUser" value="<%= config.maxTicketsPerUser %>"></div>
            </div>

            <div id="tickets_builder" class="tab-content">
                <h3>🎛️ صانع أزرار التكتات وإيمبد الداخل والنوافذ</h3>
                <p style="color:#a3a6aa; font-size:14px;">اصنع عدد لا نهائي من الأزرار. كل زر له إيمبد داخلي بقوانينه الخاصة، ونافذة بأسئلة تحددها أنت.</p>
                
                <div id="buttonsContainer"></div> <button type="button" class="add-btn" onclick="addNewButton()" style="width: 100%; margin-top: 15px; background: #f2a658;">➕ إضافة زر تكت جديد</button>
            </div>

            <div id="roles" class="tab-content">
                <h3>👨‍⚖️ نظام الرتب المفصول</h3>
                <div class="form-group"><label>👮 إدارة صغرى:</label><select name="adminRoleId"><option value="">-- غير محدد --</option><% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.adminRoleId === r.id ? 'selected' : '' %>><%= r.name %></option><% }) %></select></div>
                <div class="form-group"><label>👑 إدارة عليا:</label><select name="highAdminRoles" multiple style="height:80px;"><% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.highAdminRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %></select></div>
                <div class="form-group"><label>🤝 وساطة صغرى:</label><select name="mediatorRoleId"><option value="">-- غير محدد --</option><% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.mediatorRoleId === r.id ? 'selected' : '' %>><%= r.name %></option><% }) %></select></div>
                <div class="form-group"><label>🌟 وساطة عليا:</label><select name="highMediatorRoles" multiple style="height:80px;"><% roles.forEach(r => { %><option value="<%= r.id %>" <%= config.highMediatorRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option><% }) %></select></div>
                
                <hr>
                <div class="form-group" style="background: #2b2b36; padding: 15px; border-radius: 8px;">
                    <label>🕵️‍♂️ إخفاء التكت عن باقي الإدارة عند الاستلام (Claim):</label>
                    <input type="checkbox" id="hideToggle" name="hideTicketOnClaim" style="width: 20px; height: 20px;" <%= config.hideTicketOnClaim ? 'checked' : '' %>>
                </div>
                <div class="form-group" style="background: #2b2b36; padding: 15px; border-radius: 8px;">
                    <label>👀 وضع المراقبة (الإدارة ترى ولا تكتب):</label>
                    <input type="checkbox" id="readOnlyToggle" name="readOnlyStaffOnClaim" style="width: 20px; height: 20px;" <%= config.readOnlyStaffOnClaim ? 'checked' : '' %>>
                </div>
            </div>

            <div id="commands" class="tab-content">
                <h3>⌨️ تخصيص جميع الأوامر</h3>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div class="form-group"><label>تقييم الوسيط:</label><input type="text" name="cmdDone" value="<%= config.cmdDone %>"></div>
                    <div class="form-group"><label>استدعاء وساطة عليا:</label><input type="text" name="cmdReqHigh" value="<%= config.cmdReqHigh %>"></div>
                    <div class="form-group"><label>تفاصيل التريد:</label><input type="text" name="cmdTrade" value="<%= config.cmdTrade %>"></div>
                    <div class="form-group"><label>سحب الإدارة:</label><input type="text" name="cmdCome" value="<%= config.cmdCome %>"></div>
                    <div class="form-group"><label>مسح الشات:</label><input type="text" name="cmdClear" value="<%= config.cmdClear %>"></div>
                    <div class="form-group"><label>قفل الشات:</label><input type="text" name="cmdLock" value="<%= config.cmdLock %>"></div>
                    <div class="form-group"><label>فتح الشات:</label><input type="text" name="cmdUnlock" value="<%= config.cmdUnlock %>"></div>
                    <div class="form-group"><label>سحب فويس:</label><input type="text" name="cmdVmove" value="<%= config.cmdVmove %>"></div>
                    <div class="form-group"><label>باند:</label><input type="text" name="cmdBan" value="<%= config.cmdBan %>"></div>
                    <div class="form-group"><label>تايم أوت:</label><input type="text" name="cmdTimeout" value="<%= config.cmdTimeout %>"></div>
                </div>
            </div>

            <div id="logs" class="tab-content">
                <h3>📁 السجلات بدقة (Logs)</h3>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div class="form-group"><label>ترانسكريبت (حفظ التكت):</label><select name="transcriptChannelId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.transcriptChannelId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                    <div class="form-group"><label>لوج التكتات الأساسي:</label><select name="ticketLogChannelId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.ticketLogChannelId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                    <div class="form-group"><label>تقييم الإدارة (تلقائي):</label><select name="staffRatingChannelId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.staffRatingChannelId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                    <div class="form-group"><label>تقييم الوسطاء:</label><select name="mediatorRatingChannelId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.mediatorRatingChannelId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                    <div class="form-group"><label>لوج إنشاء/حذف الرتب:</label><select name="logRoleCreateDeleteId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logRoleCreateDeleteId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                    <div class="form-group"><label>لوج إعطاء/سحب الرتب:</label><select name="logMemberRoleUpdateId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logMemberRoleUpdateId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                    <div class="form-group"><label>لوج دخول/خروج:</label><select name="logJoinLeaveId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logJoinLeaveId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                    <div class="form-group"><label>لوج حذف الرسائل:</label><select name="logMsgDeleteId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logMsgDeleteId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                    <div class="form-group"><label>لوج تعديل الرسائل:</label><select name="logMsgUpdateId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logMsgUpdateId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                    <div class="form-group"><label>لوج حذف الصور فقط:</label><select name="logImgDeleteId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logImgDeleteId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                    <div class="form-group"><label>لوج غرف الصوت:</label><select name="logVoiceId"><option value="">-- معطل --</option><% channels.filter(c=>c.type===0).forEach(c=>{ %><option value="<%= c.id %>" <%= config.logVoiceId===c.id?'selected':'' %>><%= c.name %></option><% }) %></select></div>
                </div>
            </div>

            <button type="submit" class="save-btn" id="mainSaveBtn" onclick="prepareData()">💾 حفظ جميع الإعدادات الشاملة</button>
        </form>

        <div id="embedBuilder" class="tab-content">
            <h3 style="color: #5865F2;">🎨 صانع الإيمبد الحر للرومات</h3>
            <form action="/settings/<%= guild.id %>/send-embed" method="POST">
                <div class="form-group"><label>الروم:</label><select name="embedChannelId" required><option value="">-- اختر الروم --</option><% channels.filter(c=>c.type===0).forEach(c => { %><option value="<%= c.id %>"><%= c.name %></option><% }) %></select></div>
                <div class="form-group"><label>العنوان:</label><input type="text" name="embedTitle"></div>
                <div class="form-group"><label>الوصف (القوانين):</label><textarea name="embedDesc"></textarea></div>
                <div class="form-group"><label>اللون:</label><input type="color" name="embedColor" value="#5865F2" style="height:40px; padding:0;"></div>
                <div class="form-group"><label>رابط صورة:</label><input type="text" name="embedImage"></div>
                <button type="submit" class="save-btn" style="background: #5865F2;">🚀 إرسال الإيمبد الآن!</button>
            </form>
        </div>
    </div>

    <script>
        // 1. تحكم القوائم
        function toggleSidebar() { document.getElementById("sidebar").classList.toggle("open"); }
        function openTab(evt, tabName) {
            var i, tabcontent, tablinks;
            tabcontent = document.getElementsByClassName("tab-content");
            for (i = 0; i < tabcontent.length; i++) tabcontent[i].style.display = "none";
            tablinks = document.getElementsByClassName("tab-btn");
            for (i = 0; i < tablinks.length; i++) tablinks[i].className = tablinks[i].className.replace(" active", "");
            document.getElementById(tabName).style.display = "block";
            evt.currentTarget.className += " active";
            if (window.innerWidth <= 768) toggleSidebar();
            document.getElementById("mainSaveBtn").style.display = (tabName === 'embedBuilder') ? "none" : "block";
        }

        // 2. التحكم في زراير الكليم
        const hideToggle = document.getElementById('hideToggle');
        const readOnlyToggle = document.getElementById('readOnlyToggle');
        function checkToggles() {
            if (hideToggle.checked) { readOnlyToggle.disabled = true; readOnlyToggle.checked = false; } 
            else { readOnlyToggle.disabled = false; }
        }
        checkToggles();
        hideToggle.addEventListener('change', checkToggles);

        // 3. 🚀 البرمجة الديناميكية لصانع الزراير والنوافذ
        // نجيب الداتا المتسيفة من السيرفر ونحولها لـ JSON عشان نرسمها
        let buttonsData = <%- JSON.stringify(config.customButtons || []) %>;
        const categoriesHTML = `<option value="">-- اختر قسم (Category) --</option>` + 
            `<%- channels.filter(c=>c.type===4).map(c => `<option value="${c.id}">${c.name}</option>`).join('') %>`;

        function renderButtons() {
            const container = document.getElementById('buttonsContainer');
            container.innerHTML = '';
            
            buttonsData.forEach((btn, btnIndex) => {
                let fieldsHTML = '';
                btn.modalFields.forEach((field, fieldIndex) => {
                    fieldsHTML += `
                        <div style="background: #23232a; padding: 10px; margin-top: 10px; border-right: 3px solid #f2a658;">
                            <label>سؤال ${fieldIndex + 1}:</label>
                            <input type="text" value="${field.label}" onchange="updateField(${btnIndex}, ${fieldIndex}, 'label', this.value)" placeholder="مثال: اسمك ايه؟">
                            <button type="button" onclick="removeField(${btnIndex}, ${fieldIndex})" style="background:red; color:white; border:none; padding:5px; margin-top:5px; cursor:pointer;">مسح السؤال</button>
                        </div>
                    `;
                });

                container.innerHTML += `
                    <div class="builder-box">
                        <h4 style="color:#5865F2; margin-top:0;">🔘 زرار (${btn.label || 'جديد'})</h4>
                        
                        <div style="display:grid; grid-template-columns: 1fr 1fr; gap:10px;">
                            <div><label>الـ ID (انجليزي فقط):</label><input type="text" value="${btn.id}" onchange="updateBtn(${btnIndex}, 'id', this.value)"></div>
                            <div><label>اسم الزرار:</label><input type="text" value="${btn.label}" onchange="updateBtn(${btnIndex}, 'label', this.value)"></div>
                            <div><label>لون الزرار:</label>
                                <select onchange="updateBtn(${btnIndex}, 'color', this.value)">
                                    <option value="Primary" ${btn.color==='Primary'?'selected':''}>أزرق (Primary)</option>
                                    <option value="Success" ${btn.color==='Success'?'selected':''}>أخضر (Success)</option>
                                    <option value="Danger" ${btn.color==='Danger'?'selected':''}>أحمر (Danger)</option>
                                    <option value="Secondary" ${btn.color==='Secondary'?'selected':''}>رمادي (Secondary)</option>
                                </select>
                            </div>
                            <div><label>يفتح في أي قسم؟</label>
                                <select onchange="updateBtn(${btnIndex}, 'categoryId', this.value)">
                                    ${categoriesHTML.replace(`value="${btn.categoryId}"`, `value="${btn.categoryId}" selected`)}
                                </select>
                            </div>
                        </div>

                        <hr style="border-color:#444;">
                        
                        <h4 style="color:#3ba55d; margin-bottom:5px;">🎨 الإيمبد الداخلي للتكت (القوانين)</h4>
                        <div><label>عنوان الإيمبد الداخلي:</label><input type="text" value="${btn.insideEmbedTitle}" onchange="updateBtn(${btnIndex}, 'insideEmbedTitle', this.value)"></div>
                        <div style="margin-top:10px;"><label>وصف الإيمبد الداخلي (ضع القوانين هنا):</label><textarea onchange="updateBtn(${btnIndex}, 'insideEmbedDesc', this.value)">${btn.insideEmbedDesc}</textarea></div>
                        <div style="margin-top:10px;"><label>لون الإيمبد الداخلي:</label><input type="color" value="${btn.insideEmbedColor}" onchange="updateBtn(${btnIndex}, 'insideEmbedColor', this.value)" style="height:40px; padding:0;"></div>

                        <hr style="border-color:#444;">
                        
                        <div>
                            <label style="display:inline-block;">🪟 تفعيل النافذة (Modal):</label>
                            <input type="checkbox" ${btn.requireModal?'checked':''} onchange="updateBtn(${btnIndex}, 'requireModal', this.checked)">
                            | <label style="display:inline-block; margin-right:10px;">🤝 زرار وساطة (يستخدم !done):</label>
                            <input type="checkbox" ${btn.isMediator?'checked':''} onchange="updateBtn(${btnIndex}, 'isMediator', this.checked)">
                        </div>
                        
                        <div style="display: ${btn.requireModal ? 'block' : 'none'}; margin-top: 15px; background: #18181b; padding: 15px; border-radius: 5px;">
                            <label>عنوان النافذة:</label>
                            <input type="text" value="${btn.modalTitle}" onchange="updateBtn(${btnIndex}, 'modalTitle', this.value)">
                            ${fieldsHTML}
                            <button type="button" class="add-btn" onclick="addField(${btnIndex})" style="margin-top:10px; background:#3ba55d;">+ إضافة سؤال للنافذة</button>
                        </div>

                        <button type="button" onclick="removeBtn(${btnIndex})" style="background:red; color:white; border:none; padding:10px; width:100%; margin-top:15px; border-radius:5px; cursor:pointer;">🗑️ حذف هذا الزر بالكامل</button>
                    </div>
                `;
            });
        }

        // تحديث البيانات
        function updateBtn(index, key, val) { buttonsData[index][key] = val; if(key === 'requireModal') renderButtons(); }
        function updateField(btnIdx, fieldIdx, key, val) { buttonsData[btnIdx].modalFields[fieldIdx][key] = val; }
        
        // إضافة وحذف
        function addNewButton() {
            buttonsData.push({
                id: 'btn_' + Math.floor(Math.random() * 1000), label: 'زر جديد', color: 'Primary', categoryId: '',
                insideEmbedTitle: 'مرحباً بك', insideEmbedDesc: 'يرجى كتابة طلبك...', insideEmbedColor: '#2b2d31',
                requireModal: false, modalTitle: 'بيانات التكت', modalFields: [], isMediator: false
            });
            renderButtons();
        }
        function removeBtn(index) { buttonsData.splice(index, 1); renderButtons(); }
        function addField(index) { buttonsData[index].modalFields.push({ label: 'سؤال جديد', placeholder: '', required: true }); renderButtons(); }
        function removeField(btnIdx, fieldIdx) { buttonsData[btnIdx].modalFields.splice(fieldIdx, 1); renderButtons(); }

        // أول ما الصفحة تفتح نرسم الزراير القديمة
        renderButtons();

        // 4. قبل ما ندوس حفظ، نحول المصفوفات دي لنصوص عشان السيرفر يفهمها
        function prepareData() {
            document.getElementById('customButtonsData').value = JSON.stringify(buttonsData);
            // لو عايز تضيف ردود تلقائية بنفس الطريقة تقدر تكمل عليها
            document.getElementById('autoRespondersData').value = JSON.stringify([]); 
        }
    </script>
</body>
</html>
