<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>إعدادات الإمبراطورية | MNC System</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
    
    <style>
        body { 
            font-family: 'Tajawal', sans-serif; 
            background-color: #121212; 
            color: #ffffff; 
            margin: 0; 
            display: flex; 
            height: 100vh; 
            overflow: hidden; 
        }
        
        .mobile-header { 
            display: none; 
            background-color: #1e1e24; 
            padding: 15px 20px; 
            align-items: center; 
            justify-content: space-between; 
            border-bottom: 2px solid #5865F2; 
        }
        
        .menu-btn { 
            background: #5865F2; 
            color: white; 
            border: none; 
            padding: 8px 15px; 
            border-radius: 5px; 
            cursor: pointer; 
            font-family: 'Tajawal', sans-serif; 
            font-weight: bold;
        }
        
        .sidebar { 
            width: 320px; 
            background-color: #1e1e24; 
            padding: 20px; 
            display: flex; 
            flex-direction: column; 
            gap: 10px; 
            overflow-y: auto; 
            z-index: 1000; 
            border-left: 1px solid #333; 
        }
        
        .sidebar-header {
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            border-bottom: 1px solid #333; 
            padding-bottom: 10px; 
            margin-bottom: 15px;
        }
        
        .sidebar h2 { 
            color: #5865F2; 
            font-size: 22px; 
            margin: 0; 
        }

        .close-sidebar-btn {
            background: transparent;
            border: none;
            color: #ff4d4d;
            font-size: 20px;
            cursor: pointer;
            display: none;
        }
        
        .tab-btn { 
            background: #2b2b36; 
            color: white; 
            border: none; 
            padding: 15px; 
            text-align: right; 
            font-size: 16px; 
            border-radius: 8px; 
            cursor: pointer; 
            transition: 0.3s; 
            font-family: 'Tajawal', sans-serif; 
        }
        
        .tab-btn:hover { 
            background: #3b3b4f; 
        }
        
        .tab-btn.active { 
            background: #5865F2; 
            font-weight: bold; 
            border-right: 5px solid #ffffff; 
        }
        
        .content { 
            flex: 1; 
            padding: 30px; 
            overflow-y: auto; 
            background-color: #18181b; 
        }
        
        .tab-content { 
            display: none; 
            background: #23232a; 
            padding: 25px; 
            border-radius: 10px; 
            box-shadow: 0 4px 15px rgba(0,0,0,0.3); 
            animation: fadeIn 0.4s; 
        }
        
        .tab-content.active { 
            display: block; 
        }
        
        @keyframes fadeIn { 
            from { opacity: 0; transform: translateY(10px); } 
            to { opacity: 1; transform: translateY(0); } 
        }
        
        .form-group { 
            margin-bottom: 20px; 
        }
        
        .form-group label { 
            display: block; 
            margin-bottom: 8px; 
            color: #b9bbbe; 
            font-weight: bold; 
        }
        
        .form-group input[type="text"], 
        .form-group input[type="number"], 
        .form-group select, 
        .form-group textarea { 
            width: 100%; 
            padding: 12px; 
            background: #2f3136; 
            border: 1px solid #202225; 
            color: white; 
            border-radius: 5px; 
            box-sizing: border-box; 
            font-family: 'Tajawal', sans-serif; 
            font-size: 15px;
        }
        
        .save-btn { 
            background: #3ba55d; 
            color: white; 
            padding: 15px; 
            border: none; 
            border-radius: 5px; 
            font-size: 18px; 
            cursor: pointer; 
            width: 100%; 
            font-weight: bold; 
            font-family: 'Tajawal', sans-serif; 
            margin-top: 20px; 
            transition: 0.3s; 
            box-shadow: 0 4px 10px rgba(59, 165, 93, 0.4);
        }
        
        .save-btn:hover { 
            background: #2d7d46; 
            transform: translateY(-2px);
        }
        
        .builder-box { 
            border: 1px solid #5865F2; 
            padding: 20px; 
            border-radius: 8px; 
            margin-bottom: 15px; 
            background: #1e1e24; 
        }
        
        .cmd-box { 
            background: #1e1e24; 
            border: 1px solid #444; 
            padding: 15px; 
            border-radius: 8px; 
        }
        
        hr { 
            border-color: #333; 
            margin: 20px 0; 
        }
        
        .section-title { 
            color: #f2a658; 
            border-bottom: 2px solid #333; 
            padding-bottom: 10px; 
            margin-top: 30px; 
            margin-bottom: 20px;
        }
        
        @media (max-width: 768px) { 
            body { flex-direction: column; overflow: auto; } 
            .mobile-header { display: flex; } 
            .sidebar { display: none; width: 100%; height: auto;} 
            .sidebar.open { display: flex; } 
            .close-sidebar-btn { display: block; }
            .content { padding: 15px; } 
        }
    </style>
</head>
<body>

    <div class="mobile-header">
        <h2 style="margin: 0; color: white;">MNC Control</h2>
        <button class="menu-btn" onclick="document.getElementById('sidebar').classList.toggle('open')">☰ القائمة المتقدمة</button>
    </div>

    <div class="sidebar" id="sidebar">
        
        <div class="sidebar-header">
            <h2>👑 Control Panel</h2>
            <button class="close-sidebar-btn" onclick="document.getElementById('sidebar').classList.remove('open')">❌</button>
        </div>

        <button class="tab-btn active" onclick="openTab(event, 'general')">⚙️ الإعدادات والترحيب</button>
        <button class="tab-btn" onclick="openTab(event, 'warns_games')">⚠️ التحذيرات والألعاب</button>
        <button class="tab-btn" onclick="openTab(event, 'tickets_panel')">🎟️ التكتات الخارجية (البانر)</button>
        <button class="tab-btn" onclick="openTab(event, 'tickets_builder')" style="border-right: 3px solid #f2a658;">🎛️ صانع التكتات والنوافذ</button>
        <button class="tab-btn" onclick="openTab(event, 'roles')">👨‍⚖️ الرتب واستلام التكت</button>
        
        <button class="tab-btn" onclick="openTab(event, 'trade_approvals')" style="border-right: 3px solid #3ba55d;">⚖️ الموافقات العليا للتريد</button>
        
        <button class="tab-btn" onclick="openTab(event, 'commands')">⌨️ الأوامر الديناميكية للفك</button>
        <button class="tab-btn" onclick="openTab(event, 'punishments')" style="border-right: 3px solid #e74c3c;">🔨 تحكم العقوبات والإيمبدات</button>
        <button class="tab-btn" onclick="openTab(event, 'logs')">📁 جميع سجلات اللوج</button>
        <button class="tab-btn" onclick="openTab(event, 'embeds_control')" style="border-right: 3px solid #ed4245;">🎨 تحكم الإيمبدات والألوان</button>
        <button class="tab-btn" onclick="openTab(event, 'ratings_system')" style="border-right: 3px solid #f1c40f;">⭐ نظام التقييمات الشامل</button>
        <button class="tab-btn" onclick="openTab(event, 'embedBuilder')" style="border-right: 3px solid #9b59b6;">🚀 صانع الإيمبد الحر</button>
        
        <a href="/dashboard" style="color: #ff4d4d; text-align: center; margin-top: auto; padding-top:20px; text-decoration: none; font-weight: bold;">⬅️ العودة للسيرفرات</a>
    </div>

    <div class="content">
        
        <% if (success === 'saved') { %> 
            <div style="background: #3ba55d; padding: 15px; text-align: center; border-radius: 5px; margin-bottom: 20px; font-weight: bold; font-size: 18px; color: white;">
                ✅ تمت مزامنة التعديلات مع الديسكورد بنجاح!
            </div> 
        <% } %>
        
        <form action="/settings/<%= guild.id %>" method="POST" id="mainForm">
            
            <input type="hidden" name="customButtonsData" id="customButtonsData">
            <input type="hidden" name="warnReasonsData" id="warnReasonsData">

            <div id="general" class="tab-content active">
                <h3 class="section-title">⚙️ الإعدادات العامة والحماية</h3>
                <div class="form-group">
                    <label>البريفكس (Prefix):</label>
                    <input type="text" name="prefix" value="<%= config.prefix || '!' %>">
                </div>
                
                <div class="form-group" style="background: #2b2b36; padding: 15px; border-radius: 8px;">
                    <label style="display:inline-block; margin:0; color:#f2a658;">🛡️ الحماية (تفعيل الإيقاف التلقائي):</label><br><br>
                    <input type="checkbox" name="antiLinks" style="width:20px; height:20px;" <%= config.antiLinks ? 'checked' : '' %>> <span style="font-size: 16px;">منع نشر الروابط</span> <br>
                    <input type="checkbox" name="antiSpam" style="width:20px; height:20px; margin-top: 10px;" <%= config.antiSpam ? 'checked' : '' %>> <span style="font-size: 16px;">منع السبام والتكرار</span>
                </div>
                
                <div class="form-group">
                    <label>رتبة الدخول التلقائية (Auto-Role):</label>
                    <select name="autoRoleId">
                        <option value="">-- معطل --</option>
                        <% roles.forEach(r => { %>
                            <option value="<%= r.id %>" <%= config.autoRoleId === r.id ? 'selected' : '' %>><%= r.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <h3 class="section-title">🖼️ نظام الترحيب المتطور (بالصور)</h3>
                
                <div class="form-group">
                    <label>روم الترحيب:</label>
                    <select name="welcomeChannelId">
                        <option value="">-- معطل --</option>
                        <% channels.filter(c=>c.type===0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.welcomeChannelId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div class="form-group">
                    <label>رسالة الترحيب النصية:</label>
                    <textarea name="welcomeMessage" rows="3"><%= config.welcomeMessage || 'حياك الله يا [user] في [server]! أنت العضو رقم [memberCount].' %></textarea>
                </div>
                
                <div class="form-group">
                    <label>رابط صورة الخلفية (رابط مباشر):</label>
                    <input type="text" name="welcomeBgImage" value="<%= config.welcomeBgImage || '' %>">
                </div>
                
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div class="form-group">
                        <label>لون إطار الصورة الشخصية:</label>
                        <input type="color" name="welcomeAvatarBorderColor" value="<%= config.welcomeAvatarBorderColor || '#ffffff' %>" style="height: 40px; padding:0;">
                    </div>
                    <div class="form-group">
                        <label>لون إيمبد الترحيب (إن وُجد):</label>
                        <input type="color" name="welcomeEmbedColor" value="<%= config.welcomeEmbedColor || '#5865F2' %>" style="height: 40px; padding:0;">
                    </div>
                </div>
            </div>

            <div id="warns_games" class="tab-content">
                <h3 class="section-title">⚠️ لوحة التحكم بالتحذيرات</h3>
                <div class="form-group">
                    <label>روم إرسال لوحة التحذيرات:</label>
                    <select name="warnPanelChannelId">
                        <option value="">-- معطل --</option>
                        <% channels.filter(c=>c.type===0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.warnPanelChannelId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div class="form-group">
                    <label>روم سجل التحذيرات (اللوج):</label>
                    <select name="warnLogChannelId">
                        <option value="">-- اتبع اللوج العام --</option>
                        <% channels.filter(c=>c.type===0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.warnLogChannelId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div class="form-group">
                        <label>عنوان لوحة التحذير:</label>
                        <input type="text" name="warnPanelTitle" value="<%= config.warnPanelTitle || 'لوحة تحكم التحذيرات' %>">
                    </div>
                    <div class="form-group">
                        <label>لون الإيمبد:</label>
                        <input type="color" name="warnPanelColor" value="<%= config.warnPanelColor || '#ed4245' %>" style="height:40px; padding:0;">
                    </div>
                </div>
                
                <div class="form-group">
                    <label>وصف الإيمبد (التعليمات):</label>
                    <textarea name="warnPanelDesc" rows="2"><%= config.warnPanelDesc || 'استخدم الأزرار أدناه.' %></textarea>
                </div>
                
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div class="form-group">
                        <label>الحد الأقصى للتحذيرات (ويعاقب بعدها):</label>
                        <input type="number" name="warnMax" value="<%= config.warnMax || 3 %>">
                    </div>
                    <div class="form-group">
                        <label>العقاب التلقائي بعد تجاوز الحد:</label>
                        <select name="warnAction">
                            <option value="timeout" <%= config.warnAction === 'timeout' ? 'selected' : '' %>>Timeout (كتم)</option>
                            <option value="kick" <%= config.warnAction === 'kick' ? 'selected' : '' %>>Kick (طرد)</option>
                            <option value="ban" <%= config.warnAction === 'ban' ? 'selected' : '' %>>Ban (حظر)</option>
                        </select>
                    </div>
                </div>
                
                <div class="builder-box">
                    <h4 style="margin-top:0; color:#ed4245;">📝 الأسباب الجاهزة للتحذير</h4>
                    <div id="warnReasonsContainer"></div>
                    <button type="button" onclick="warnReasons.push('سبب جديد');renderWarnReasons();" style="background:#ed4245; border:none; color:white; padding:10px 20px; border-radius:5px; margin-top:10px; cursor:pointer; font-weight:bold;">➕ إضافة سبب للتحذير</button>
                </div>

                <h3 class="section-title">🎮 الألعاب والمستويات</h3>
                <div style="display:grid; grid-template-columns: 1fr 1fr; gap:15px;">
                    <div class="form-group" style="background:#2b2b36; padding:15px; border-radius:8px;">
                        <label>تفعيل الألعاب:</label>
                        <input type="checkbox" name="gamesEnabled" style="width: 20px; height: 20px;" <%= config.gamesEnabled ? 'checked' : '' %>>
                    </div>
                    <div class="form-group" style="background:#2b2b36; padding:15px; border-radius:8px;">
                        <label>تفعيل نظام المستويات:</label>
                        <input type="checkbox" name="levelingEnabled" style="width: 20px; height: 20px;" <%= config.levelingEnabled ? 'checked' : '' %>>
                    </div>
                </div>
            </div>

            <div id="tickets_panel" class="tab-content">
                <h3 class="section-title">🎟️ إعدادات البانر الخارجي</h3>
                <div class="form-group">
                    <label>روم البانر:</label>
                    <select name="panelChannelId">
                        <option value="">-- اختر الروم --</option>
                        <% channels.filter(c=>c.type===0).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.panelChannelId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div class="form-group">
                    <label>القسم الاحتياطي لفتح التكتات (Category):</label>
                    <select name="defaultCategoryId">
                        <option value="">-- اختر قسم --</option>
                        <% channels.filter(c=>c.type===4).forEach(c => { %>
                            <option value="<%= c.id %>" <%= config.defaultCategoryId === c.id ? 'selected' : '' %>><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                
                <div class="form-group">
                    <label>عنوان إيمبد القوانين الخارجي:</label>
                    <input type="text" name="ticketEmbedTitle" value="<%= config.ticketEmbedTitle || 'الدعم الفني' %>">
                </div>
                
                <div class="form-group">
                    <label>وصف الإيمبد الخارجي:</label>
                    <textarea name="ticketEmbedDesc" rows="4"><%= config.ticketEmbedDesc || 'اضغط لفتح تذكرة' %></textarea>
                </div>
                
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                    <div class="form-group">
                        <label>لون الإيمبد:</label>
                        <input type="color" name="ticketEmbedColor" value="<%= config.ticketEmbedColor || '#0099ff' %>" style="height:40px; padding:0;">
                    </div>
                    <div class="form-group">
                        <label>رقم التكت القادم:</label>
                        <input type="number" name="ticketCount" value="<%= config.ticketCount || 0 %>">
                    </div>
                    <div class="form-group">
                        <label>الحد الأقصى للتكتات المفتوحة للعضو:</label>
                        <input type="number" name="maxTicketsPerUser" value="<%= config.maxTicketsPerUser || 1 %>">
                    </div>
                </div>
            </div>

            <div id="tickets_builder" class="tab-content">
                <h3 class="section-title">🎛️ صانع التكتات التفصيلي</h3>
                <div id="buttonsContainer"></div>
                <button type="button" onclick="addNewButton()" style="background:#f2a658; color:white; border:none; padding:15px; width:100%; font-size:18px; font-weight:bold; border-radius:8px; cursor:pointer; margin-top:20px;">➕ إضافة زر تكت جديد بالكامل</button>
            </div>

            <div id="roles" class="tab-content">
                <h3 class="section-title">👨‍⚖️ نظام الرتب الموحد</h3>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    <div class="form-group">
                        <label>👮 رتبة الإدارة الصغرى:</label>
                        <select name="adminRoleId">
                            <option value="">-- غير محدد --</option>
                            <% roles.forEach(r => { %>
                                <option value="<%= r.id %>" <%= config.adminRoleId === r.id ? 'selected' : '' %>><%= r.name %></option>
                            <% }) %>
                        </select>
                    </div>
                    <div class="form-group">
                        <label>👑 رتب الإدارة العليا (متعدد):</label>
                        <select name="highAdminRoles" multiple style="height:100px;">
                            <% roles.forEach(r => { %>
                                <option value="<%= r.id %>" <%= config.highAdminRoles && config.highAdminRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                            <% }) %>
                        </select>
                    </div>
                    <div class="form-group">
                        <label>🤝 رتبة الوساطة الصغرى:</label>
                        <select name="mediatorRoleId">
                            <option value="">-- غير محدد --</option>
                            <% roles.forEach(r => { %>
                                <option value="<%= r.id %>" <%= config.mediatorRoleId === r.id ? 'selected' : '' %>><%= r.name %></option>
                            <% }) %>
                        </select>
                    </div>
                    <div class="form-group">
                        <label>🌟 رتب الوساطة العليا (متعدد):</label>
                        <select name="highMediatorRoles" multiple style="height:100px;">
                            <% roles.forEach(r => { %>
                                <option value="<%= r.id %>" <%= config.highMediatorRoles && config.highMediatorRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                            <% }) %>
                        </select>
                    </div>
                </div>
                <hr>
                <h3 class="section-title">🕵️‍♂️ تحكم استلام التكت (Claim)</h3>
                <div class="form-group" style="background: #2b2b36; padding: 15px; border-radius: 8px;">
                    <label style="display:inline-block; margin-bottom:0;">إخفاء التكت عن باقي الإدارة بمجرد الاستلام:</label>
                    <input type="checkbox" id="hideToggle" name="hideTicketOnClaim" style="width: 20px; height: 20px; float:left;" <%= config.hideTicketOnClaim ? 'checked' : '' %>>
                </div>
            </div>

            <div id="trade_approvals" class="tab-content">
                <h3 class="section-title">⚖️ إعدادات التريد والموافقات والنداء</h3>
                
                <div class="builder-box" style="border-color:#3ba55d;">
                    <h4 style="color:#3ba55d; margin-top:0;">1. أمر كتابة التريد (!trade)</h4>
                    <p style="color:#a3a6aa; font-size: 14px;">هذا الأمر يظهر زرار يفتح نافذة لكتابة تفاصيل الحساب والسعر، ثم ينزل طلب للموافقة.</p>
                    <div class="form-group">
                        <label>الأمر:</label>
                        <input type="text" name="cmdTrade" value="<%= config.cmdTrade || '!trade' %>">
                    </div>
                    <div class="form-group">
                        <label>الرتب المسموح لها بكتابة الأمر (من يظهر له الزر):</label>
                        <select name="cmdTradeRoles" multiple style="height:100px;">
                            <% roles.forEach(r => { %>
                                <option value="<%= r.id %>" <%= config.cmdTradeRoles && config.cmdTradeRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                            <% }) %>
                        </select>
                    </div>
                </div>

                <div class="builder-box" style="border-color:#f2a658;">
                    <h4 style="color:#f2a658; margin-top:0;">2. صلاحيات الموافقة العليا (Approve / Reject)</h4>
                    <p style="color:#a3a6aa; font-size: 14px;">حدد من يمكنه الضغط على زراير (موافقة✅ / رفض❌) بعد كتابة التريد.</p>
                    <div class="form-group">
                        <label>رتب الموافقة العليا:</label>
                        <select name="tradeApproveRoles" multiple style="height:100px;">
                            <% roles.forEach(r => { %>
                                <option value="<%= r.id %>" <%= config.tradeApproveRoles && config.tradeApproveRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                            <% }) %>
                        </select>
                    </div>
                </div>

                <div class="builder-box" style="border-color:#ed4245;">
                    <h4 style="color:#ed4245; margin-top:0;">3. نظام المنشن والنداء (!req-high)</h4>
                    <p style="color:#a3a6aa; font-size: 14px;">عند نزول طلب التريد، أو عند كتابة أمر النداء، سيقوم البوت بعمل منشن لهذه الرتب ليأتوا فوراً.</p>
                    <div class="form-group">
                        <label>أمر النداء المباشر:</label>
                        <input type="text" name="cmdReqHigh" value="<%= config.cmdReqHigh || '!req-high' %>">
                    </div>
                    <div class="form-group">
                        <label>رتب المنشن (التي سيتم منشنتها):</label>
                        <select name="tradeMentionRoles" multiple style="height:100px;">
                            <% roles.forEach(r => { %>
                                <option value="<%= r.id %>" <%= config.tradeMentionRoles && config.tradeMentionRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                            <% }) %>
                        </select>
                    </div>
                </div>
            </div>

            <div id="commands" class="tab-content">
                <h3 class="section-title">⌨️ الأوامر الديناميكية للغرف والفك</h3>
                <div style="display:grid; grid-template-columns: 1fr 1fr; gap:20px;">
                    <% 
                    const cmdsList = [
                        {id: 'cmdDone', label: 'تقييم الوسيط (!done)'}, 
                        {id: 'cmdUnban', label: 'فك الباند (!unban)'},
                        {id: 'cmdUntimeout', label: 'فك التايم أوت (!untimeout)'},
                        {id: 'cmdVmove', label: 'سحب عضو لفويسك (!vmove)'}, 
                        {id: 'cmdMove', label: 'نقل عضو لروم محددة (!move)'},
                        {id: 'cmdCome', label: 'استدعاء لغرفتك (!come)'}, 
                        {id: 'cmdClear', label: 'مسح الشات (!clear)'}, 
                        {id: 'cmdLock', label: 'قفل الشات (!lock)'}, 
                        {id: 'cmdUnlock', label: 'فتح الشات (!unlock)'}
                    ]; 
                    cmdsList.forEach(cmdObj => { 
                    %>
                        <div class="cmd-box">
                            <label style="color:#5865F2; font-size:16px;">🔥 أمر <%= cmdObj.label %>:</label>
                            <input type="text" name="<%= cmdObj.id %>" value="<%= config[cmdObj.id] %>" style="margin-bottom:15px;">
                            <label style="font-size:14px; color:#a3a6aa;">الرتب المسموح لها:</label>
                            <select name="<%= cmdObj.id %>Roles" multiple style="height:80px;">
                                <% roles.forEach(r => { %>
                                    <option value="<%= r.id %>" <%= config[cmdObj.id+'Roles'] && config[cmdObj.id+'Roles'].includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                                <% }) %>
                            </select>
                        </div>
                    <% }) %>
                </div>
            </div>

            <div id="punishments" class="tab-content">
                <h3 class="section-title">🔨 تحكم إيمبدات العقوبات بالشات</h3>
                
                <div class="form-group" style="background:#2b2b36; padding:15px; border-radius:8px;">
                    <label style="color:#e74c3c; font-size:18px;">نوع الإيمبد المُرسل بالشات (العام):</label>
                    <select name="punishmentStyle" onchange="toggleCustomPunishment(this.value)">
                        <option value="basic" <%= config.punishmentStyle === 'basic' ? 'selected' : '' %>>الأساسي (شكل كبير ومنسق)</option>
                        <option value="custom" <%= config.punishmentStyle === 'custom' ? 'selected' : '' %>>مخصص (أنت تكتب الرسالة)</option>
                    </select>
                </div>

                <div id="customPunishmentBox" style="display: <%= config.punishmentStyle === 'custom' ? 'block' : 'none' %>; background:#1e1e24; padding:20px; border-radius:8px; border:1px solid #e74c3c;">
                    <p style="color:#a3a6aa; margin-bottom: 20px;">
                        المتغيرات المتاحة: <code>[user]</code>, <code>[moderator]</code>, <code>[reason]</code>, <code>[duration]</code>
                    </p>

                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                        
                        <div class="cmd-box">
                            <label style="color:#ed4245;">أمر الباند (!ban):</label>
                            <input type="text" name="cmdBan" value="<%= config.cmdBan || '!ban' %>" style="margin-bottom:10px;">
                            <label>رتب الباند:</label>
                            <select name="cmdBanRoles" multiple style="height:60px; margin-bottom:15px;">
                                <% roles.forEach(r => { %>
                                    <option value="<%= r.id %>" <%= config.cmdBanRoles && config.cmdBanRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                                <% }) %>
                            </select>
                            <h4 style="color:#ed4245; margin-top:0;">إيمبد الإعطاء المخصص</h4>
                            <input type="text" name="customBanTitle" value="<%= config.customBanTitle || '🔨 تم حظر عضو' %>" placeholder="العنوان">
                            <textarea name="customBanDesc" rows="3" style="margin-top:10px;"><%= config.customBanDesc || 'تم حظر [user] بواسطة [moderator].\nالسبب: [reason]' %></textarea>
                        </div>

                        <div class="cmd-box">
                            <label style="color:#f2a658;">أمر التايم أوت (!timeout):</label>
                            <input type="text" name="cmdTimeout" value="<%= config.cmdTimeout || '!timeout' %>" style="margin-bottom:10px;">
                            <label>رتب التايم أوت:</label>
                            <select name="cmdTimeoutRoles" multiple style="height:60px; margin-bottom:15px;">
                                <% roles.forEach(r => { %>
                                    <option value="<%= r.id %>" <%= config.cmdTimeoutRoles && config.cmdTimeoutRoles.includes(r.id) ? 'selected' : '' %>><%= r.name %></option>
                                <% }) %>
                            </select>
                            <h4 style="color:#f2a658; margin-top:0;">إيمبد الإعطاء المخصص</h4>
                            <input type="text" name="customTimeoutTitle" value="<%= config.customTimeoutTitle || '⏳ تم إعطاء تايم أوت' %>" placeholder="العنوان">
                            <textarea name="customTimeoutDesc" rows="3" style="margin-top:10px;"><%= config.customTimeoutDesc || 'تم معاقبة [user] بواسطة [moderator] لمدة [duration].\nالسبب: [reason]' %></textarea>
                        </div>
                    </div>
                </div>
            </div>

            <div id="logs" class="tab-content">
                <h3 class="section-title">📁 السجلات واللوجات الشاملة</h3>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    <% 
                    const logList = [
                        {id: 'transcriptChannelId', label: 'روم الترانسكريبت (الملفات والأزرار)'}, 
                        {id: 'ticketLogChannelId', label: 'لوج حركات التكت (فتح/قفل/حذف)'}, 
                        {id: 'staffRatingChannelId', label: 'لوج تقييمات الإدارة (تلقائي)'}, 
                        {id: 'mediatorRatingChannelId', label: 'لوج تقييمات الوسطاء (!done)'},
                        {id: 'logBanId', label: 'لوج الباند وفك الباند'}, 
                        {id: 'logTimeoutId', label: 'لوج التايم أوت وفكه'},
                        {id: 'logMsgDeleteId', label: 'لوج حذف الرسائل'}, 
                        {id: 'logMsgUpdateId', label: 'لوج تعديل الرسائل'}, 
                        {id: 'logVoiceId', label: 'لوج حركات الفويس والنقل'}, 
                        {id: 'logJoinLeaveId', label: 'لوج الدخول والخروج'},
                        {id: 'logRoleCreateDeleteId', label: 'لوج إنشاء وحذف الرتب'}, 
                        {id: 'logMemberRoleUpdateId', label: 'لوج تحديث رتب الأعضاء'}, 
                        {id: 'logInviteId', label: 'لوج الدعوات'}, 
                        {id: 'logChannelThreadId', label: 'لوج الرومات والثريد'}
                    ]; 
                    
                    logList.forEach(logObj => { 
                    %>
                        <div class="form-group" style="background:#23232a; padding:15px; border-radius:8px; margin-bottom:0;">
                            <label style="color:#f2a658;"><%= logObj.label %>:</label>
                            <select name="<%= logObj.id %>">
                                <option value="">-- معطل --</option>
                                <% channels.filter(c=>c.type===0).forEach(c=>{ %>
                                    <option value="<%= c.id %>" <%= config[logObj.id]===c.id?'selected':'' %>><%= c.name %></option>
                                <% }) %>
                            </select>
                        </div>
                    <% }) %>
                </div>
            </div>

            <div id="embeds_control" class="tab-content">
                <h3 class="section-title">🎨 تحكم ألوان الإيمبدات الشامل</h3>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    <div class="form-group cmd-box"><label style="color:white;">إيمبد إجابات النوافذ (في التكت):</label><input type="color" name="answersEmbedColor" value="<%= config.answersEmbedColor || '#2b2d31' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:white;">إيمبد القفل (Close):</label><input type="color" name="closeEmbedColor" value="<%= config.closeEmbedColor || '#2b2d31' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:white;">إيمبد طلب التريد (!trade):</label><input type="color" name="tradeEmbedColor" value="<%= config.tradeEmbedColor || '#f2a658' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:white;">لوج التكتات الأساسي (Ticket Logs):</label><input type="color" name="logEmbedColor" value="<%= config.logEmbedColor || '#ed4245' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:white;">إيمبد الترانسكريبت (Transcript):</label><input type="color" name="transcriptEmbedColor" value="<%= config.transcriptEmbedColor || '#2b2d31' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    
                    <div class="form-group cmd-box"><label style="color:#ed4245;">إيمبد إعطاء الباند (!ban):</label><input type="color" name="banEmbedColor" value="<%= config.banEmbedColor || '#ed4245' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:#3ba55d;">إيمبد فك الباند (!unban):</label><input type="color" name="unbanEmbedColor" value="<%= config.unbanEmbedColor || '#3ba55d' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:#f2a658;">إيمبد التايم أوت (!timeout):</label><input type="color" name="timeoutEmbedColor" value="<%= config.timeoutEmbedColor || '#f2a658' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                    <div class="form-group cmd-box"><label style="color:#3ba55d;">إيمبد فك التايم (!untimeout):</label><input type="color" name="untimeoutEmbedColor" value="<%= config.untimeoutEmbedColor || '#3ba55d' %>" style="height:50px; padding:0; cursor:pointer;"></div>
                </div>
            </div>

            <div id="ratings_system" class="tab-content">
                <h3 class="section-title">⭐ التحكم في رسالة التقييم بالخاص واللوج</h3>
                <div class="form-group" style="background:#2b2b36; padding:15px; border-radius:8px;">
                    <label style="color:#5865F2; font-size:18px;">نوع التقييم المُرسل:</label>
                    <select name="ratingStyle" onchange="toggleCustomRating(this.value)">
                        <option value="basic" <%= config.ratingStyle === 'basic' ? 'selected' : '' %>>الأساسي العربي</option>
                        <option value="custom" <%= config.ratingStyle === 'custom' ? 'selected' : '' %>>تقييم يدوي مخصص</option>
                    </select>
                </div>

                <div id="customRatingBox" style="display: <%= config.ratingStyle === 'custom' ? 'block' : 'none' %>; background:#1e1e24; padding:20px; border-radius:8px; border:1px solid #f2a658;">
                    <p style="color:#a3a6aa;">المتغيرات المتاحة: <code>[user]</code> للعميل، <code>[staff]</code> للإداري/الوسيط، <code>[server]</code> لاسم السيرفر.</p>
                    
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                        
                        <div class="cmd-box">
                            <h4 style="color:#3ba55d; margin-top:0;">تقييم الدعم الفني والإدارة</h4>
                            <div class="form-group"><label>العنوان:</label><input type="text" name="customRatingTitle" value="<%= config.customRatingTitle || 'تقييم فريق العمل' %>"></div>
                            <div class="form-group"><label>الوصف:</label><textarea name="customRatingText" rows="4"><%= config.customRatingText || 'مرحباً [user]، يرجى تقييم خدمة الإداري [staff].' %></textarea></div>
                            <div class="form-group"><label>لون الإيمبد:</label><input type="color" name="staffRatingColor" value="<%= config.staffRatingColor || '#3ba55d' %>" style="height:40px; padding:0;"></div>
                        </div>

                        <div class="cmd-box">
                            <h4 style="color:#f2a658; margin-top:0;">تقييم الوسطاء (!done)</h4>
                            <div class="form-group"><label>العنوان:</label><input type="text" name="customMedRatingTitle" value="<%= config.customMedRatingTitle || 'تقييم الوساطة' %>"></div>
                            <div class="form-group"><label>الوصف:</label><textarea name="customMedRatingText" rows="4"><%= config.customMedRatingText || 'مرحباً [user]، يرجى تقييم خدمة الوسيط [staff].' %></textarea></div>
                            <div class="form-group"><label>لون الإيمبد:</label><input type="color" name="basicRatingColor" value="<%= config.basicRatingColor || '#f2a658' %>" style="height:40px; padding:0;"></div>
                        </div>
                    </div>
                </div>
                
                <div id="basicRatingColors" style="display: <%= config.ratingStyle === 'basic' ? 'block' : 'none' %>; margin-top: 20px;">
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                        <div class="form-group cmd-box"><label style="color:#3ba55d;">لون تقييم الإدارة (البيسك):</label><input type="color" name="staffRatingColor_basic" value="<%= config.staffRatingColor || '#3ba55d' %>" onchange="document.getElementsByName('staffRatingColor')[0].value = this.value;" style="height:40px; padding:0;"></div>
                        <div class="form-group cmd-box"><label style="color:#f2a658;">لون تقييم الوساطة (البيسك):</label><input type="color" name="basicRatingColor_basic" value="<%= config.basicRatingColor || '#f2a658' %>" onchange="document.getElementsByName('basicRatingColor')[0].value = this.value;" style="height:40px; padding:0;"></div>
                    </div>
                </div>
            </div>

            <button type="submit" class="save-btn" id="mainSaveBtn" onclick="prepareData()">💾 حفظ جميع الإعدادات وتحديث البوت فوراً!</button>
        </form>
        
        <div id="embedBuilder" class="tab-content">
            <h3 class="section-title" style="color: #9b59b6;">🚀 صانع الإيمبد الحر للرومات</h3>
            <form action="/settings/<%= guild.id %>/send-embed" method="POST">
                <div class="form-group">
                    <label>الروم المراد الإرسال إليها:</label>
                    <select name="embedChannelId" required>
                        <option value="">-- اختر الروم --</option>
                        <% channels.filter(c=>c.type===0).forEach(c => { %>
                            <option value="<%= c.id %>"><%= c.name %></option>
                        <% }) %>
                    </select>
                </div>
                <div class="form-group"><label>العنوان:</label><input type="text" name="embedTitle"></div>
                <div class="form-group"><label>الوصف (النص الرئيسي):</label><textarea name="embedDesc" rows="5"></textarea></div>
                <div class="form-group"><label>اللون:</label><input type="color" name="embedColor" value="#5865F2" style="height:40px; padding:0;"></div>
                <div class="form-group"><label>رابط صورة كبيرة (اختياري):</label><input type="text" name="embedImage"></div>
                <div class="form-group"><label>النص السفلي Footer (اختياري):</label><input type="text" name="embedFooter"></div>
                <button type="submit" class="save-btn" style="background: #9b59b6;">ارسال الإيمبد الآن!</button>
            </form>
        </div>
    </div>

    <script>
        function openTab(eventObj, tabName) {
            
            const allContents = document.querySelectorAll(".tab-content");
            for (let i = 0; i < allContents.length; i++) {
                allContents[i].style.display = "none";
            }
            
            const allButtons = document.querySelectorAll(".tab-btn");
            for (let i = 0; i < allButtons.length; i++) {
                allButtons[i].classList.remove("active");
            }
            
            document.getElementById(tabName).style.display = "block"; 
            eventObj.currentTarget.classList.add("active");
            
            if (tabName === 'embedBuilder') {
                document.getElementById("mainSaveBtn").style.display = "none";
            } else {
                document.getElementById("mainSaveBtn").style.display = "block";
            }
            
            if (window.innerWidth <= 768) {
                document.getElementById('sidebar').classList.remove('open');
            }
        }
        
        function toggleCustomRating(val) { 
            const box = document.getElementById('customRatingBox');
            const basicColors = document.getElementById('basicRatingColors');
            if (val === 'custom') { 
                box.style.display = 'block'; 
                basicColors.style.display = 'none';
            } else { 
                box.style.display = 'none'; 
                basicColors.style.display = 'block';
            }
        }
        
        function toggleCustomPunishment(val) { 
            const box = document.getElementById('customPunishmentBox');
            if (val === 'custom') { 
                box.style.display = 'block'; 
            } else { 
                box.style.display = 'none'; 
            }
        }

        const hideToggleCheckbox = document.getElementById('hideToggle'); 
        const readOnlyToggleCheckbox = document.getElementById('readOnlyToggle');
        
        function checkClaimToggles() { 
            if(hideToggleCheckbox.checked) {
                readOnlyToggleCheckbox.disabled = true;
                readOnlyToggleCheckbox.checked = false;
            } else {
                readOnlyToggleCheckbox.disabled = false;
            } 
        }
        checkClaimToggles(); 
        hideToggleCheckbox.addEventListener('change', checkClaimToggles);

        let warnReasonsArray = <%- JSON.stringify(config.warnReasons || []) %>;
        function renderWarnReasons() {
            const containerDiv = document.getElementById('warnReasonsContainer'); 
            containerDiv.innerHTML = '';
            
            for (let i = 0; i < warnReasonsArray.length; i++) {
                const reasonText = warnReasonsArray[i];
                const wrapperDiv = document.createElement('div');
                wrapperDiv.style.display = 'flex';
                wrapperDiv.style.gap = '10px';
                wrapperDiv.style.marginBottom = '10px';
                
                const inputEl = document.createElement('input');
                inputEl.type = 'text';
                inputEl.value = reasonText;
                inputEl.placeholder = 'سبب التحذير...';
                inputEl.onchange = function() { warnReasonsArray[i] = this.value; };
                
                const delBtn = document.createElement('button');
                delBtn.type = 'button';
                delBtn.innerText = '✖';
                delBtn.style.background = 'red';
                delBtn.style.border = 'none';
                delBtn.style.color = 'white';
                delBtn.style.padding = '10px 15px';
                delBtn.style.borderRadius = '5px';
                delBtn.style.cursor = 'pointer';
                delBtn.onclick = function() { warnReasonsArray.splice(i, 1); renderWarnReasons(); };
                
                wrapperDiv.appendChild(inputEl);
                wrapperDiv.appendChild(delBtn);
                containerDiv.appendChild(wrapperDiv);
            }
        } 
        renderWarnReasons();

        let buttonsDataArray = <%- JSON.stringify(config.customButtons || []) %>;
        const serverRolesArray = <%- JSON.stringify(roles || []) %>;
        const catOptionsHTML = `<option value="">-- اتبع القسم الاحتياطي --</option><%- channels.filter(c=>c.type===4).map(c=>`<option value="${c.id}">${c.name}</option>`).join('') %>`;
        
        function renderButtons() {
            const containerDiv = document.getElementById('buttonsContainer'); 
            containerDiv.innerHTML = '';
            
            for (let bIndex = 0; bIndex < buttonsDataArray.length; bIndex++) {
                const btnObj = buttonsDataArray[bIndex];
                
                let fieldsHTML = ''; 
                if (btnObj.modalFields) {
                    for (let fIndex = 0; fIndex < btnObj.modalFields.length; fIndex++) {
                        const fieldObj = btnObj.modalFields[fIndex];
                        fieldsHTML += `
                        <div style="background:#23232a; padding:10px; margin-top:10px; border-right:3px solid #f2a658;">
                            <label>سؤال (Label) ${fIndex + 1}:</label>
                            <input type="text" value="${fieldObj.label}" onchange="buttonsDataArray[${bIndex}].modalFields[${fIndex}].label=this.value">
                            <button type="button" onclick="buttonsDataArray[${bIndex}].modalFields.splice(${fIndex},1);renderButtons();" style="background:red; color:white; border:none; padding:5px 10px; margin-top:5px; border-radius:3px; cursor:pointer;">حذف السؤال</button>
                        </div>`;
                    }
                }

                let rolesOptionsHTML = '';
                for (let rIndex = 0; rIndex < serverRolesArray.length; rIndex++) {
                    const roleObj = serverRolesArray[rIndex];
                    let isRoleSelected = '';
                    if (btnObj.allowedClaimRoles && btnObj.allowedClaimRoles.includes(roleObj.id)) {
                        isRoleSelected = 'selected';
                    }
                    rolesOptionsHTML += `<option value="${roleObj.id}" ${isRoleSelected}>${roleObj.name}</option>`;
                }

                if (btnObj.enableRating === undefined) {
                    btnObj.enableRating = true;
                }
                
                let btnHtmlString = `
                    <div class="builder-box">
                        <h3 style="color:#5865F2; margin-top:0;">🔘 ${btnObj.label}</h3>
                        <div style="display:grid; grid-template-columns: 1fr 1fr; gap:15px;">
                            <div><label>الـ ID البرمجي:</label><input type="text" value="${btnObj.id}" onchange="buttonsDataArray[${bIndex}].id=this.value"></div>
                            <div><label>اسم الزرار:</label><input type="text" value="${btnObj.label}" onchange="buttonsDataArray[${bIndex}].label=this.value"></div>
                            <div><label>لون الزرار:</label><select onchange="buttonsDataArray[${bIndex}].color=this.value"><option value="Primary" ${btnObj.color=='Primary'?'selected':''}>أزرق</option><option value="Success" ${btnObj.color=='Success'?'selected':''}>أخضر</option><option value="Danger" ${btnObj.color=='Danger'?'selected':''}>أحمر</option><option value="Secondary" ${btnObj.color=='Secondary'?'selected':''}>رمادي</option></select></div>
                            <div><label>القسم المخصص:</label><select onchange="buttonsDataArray[${bIndex}].categoryId=this.value">${catOptionsHTML.replace(`value="${btnObj.categoryId}"`, `value="${btnObj.categoryId}" selected`)}</select></div>
                        </div>

                        <h4 style="color:#3ba55d; margin-top:20px;">🎨 الإيمبد الداخلي</h4>
                        <div style="display:grid; grid-template-columns: 1fr; gap:10px;">
                            <div><label>عنوان الإيمبد:</label><input type="text" value="${btnObj.insideEmbedTitle}" onchange="buttonsDataArray[${bIndex}].insideEmbedTitle=this.value"></div>
                            <div><label>وصف الإيمبد:</label><textarea rows="3" onchange="buttonsDataArray[${bIndex}].insideEmbedDesc=this.value">${btnObj.insideEmbedDesc}</textarea></div>
                            <div><label>لون الإيمبد:</label><input type="color" value="${btnObj.insideEmbedColor}" onchange="buttonsDataArray[${bIndex}].insideEmbedColor=this.value" style="height:40px; padding:0; width:100px;"></div>
                        </div>

                        <h4 style="color:#f2a658; margin-top:20px;">🕵️‍♂️ رتب الاستلام المخصصة</h4>
                        <select multiple style="height:100px; width:100%;" onchange="const selectedOpts = Array.from(this.selectedOptions).map(opt=>opt.value); buttonsDataArray[${bIndex}].allowedClaimRoles = selectedOpts;">${rolesOptionsHTML}</select>

                        <h4 style="color:#9b59b6; margin-top:20px;">🪟 النافذة والتقييم</h4>
                        <div style="margin-bottom:15px; background: #18181b; padding: 15px; border-radius: 8px;">
                            <input type="checkbox" ${btnObj.requireModal?'checked':''} onchange="buttonsDataArray[${bIndex}].requireModal=this.checked;renderButtons();" style="width:18px;height:18px;"> <label style="display:inline;">تفعيل النافذة</label>
                            <span style="margin:0 15px;">|</span>
                            <input type="checkbox" ${btnObj.isMediator?'checked':''} onchange="buttonsDataArray[${bIndex}].isMediator=this.checked;" style="width:18px;height:18px;"> <label style="display:inline;">تكت وساطة</label>
                            <span style="margin:0 15px;">|</span>
                            <input type="checkbox" ${btnObj.enableRating!==false?'checked':''} onchange="buttonsDataArray[${bIndex}].enableRating=this.checked;" style="width:18px;height:18px;"> <label style="display:inline;">إرسال تقييم للإدارة</label>
                        </div>

                        <div style="display:${btnObj.requireModal?'block':'none'}; background:#2f3136; padding:15px; border-radius:5px;">
                            <label>عنوان النافذة (Modal Title):</label>
                            <input type="text" value="${btnObj.modalTitle}" onchange="buttonsDataArray[${bIndex}].modalTitle=this.value">
                            ${fieldsHTML}
                            <button type="button" onclick="if(!buttonsDataArray[${bIndex}].modalFields) buttonsDataArray[${bIndex}].modalFields=[]; buttonsDataArray[${bIndex}].modalFields.push({label:'سؤال جديد',required:true});renderButtons();" style="background:#3ba55d; color:white; border:none; padding:10px; margin-top:10px; border-radius:5px; cursor:pointer;">➕ إضافة سؤال</button>
                        </div>
                        <button type="button" onclick="buttonsDataArray.splice(${bIndex},1);renderButtons();" style="background:#ed4245; color:white; border:none; padding:15px; width:100%; margin-top:20px; font-weight:bold; border-radius:5px; cursor:pointer;">🗑️ حذف الزر</button>
                    </div>`;
                
                containerDiv.innerHTML += btnHtmlString;
            }
        } 
        renderButtons();

        function addNewButton() { 
            const newBtnObj = {
                id: 'btn_' + Date.now(), 
                label: 'زر جديد', 
                color: 'Primary', 
                categoryId: '', 
                insideEmbedTitle: 'تذكرة جديدة', 
                insideEmbedDesc: 'اكتب طلبك...', 
                insideEmbedColor: '#2b2d31', 
                requireModal: false, 
                modalTitle: 'بيانات مطلوبة', 
                modalFields: [], 
                isMediator: false, 
                enableRating: true, 
                allowedClaimRoles: []
            };
            buttonsDataArray.push(newBtnObj); 
            renderButtons(); 
        }

        function prepareData() { 
            const stringifiedButtons = JSON.stringify(buttonsDataArray);
            document.getElementById('customButtonsData').value = stringifiedButtons; 
            
            const stringifiedWarns = JSON.stringify(warnReasonsArray);
            document.getElementById('warnReasonsData').value = stringifiedWarns; 
        }
    </script>
</body>
</html>
