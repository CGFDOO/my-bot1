<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MNC System | تسجيل الدخول</title>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Tajawal', sans-serif;
            background-color: #121212;
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            text-align: center;
        }
        .container {
            background: #1e1e24;
            padding: 50px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            border-top: 5px solid #5865F2;
        }
        h1 { margin-bottom: 10px; color: #ffffff; }
        p { color: #a3a6aa; margin-bottom: 30px; }
        .login-btn {
            background-color: #5865F2;
            color: white;
            padding: 15px 30px;
            text-decoration: none;
            border-radius: 8px;
            font-size: 18px;
            font-weight: bold;
            transition: 0.3s;
            display: inline-block;
        }
        .login-btn:hover { background-color: #4752c4; transform: translateY(-3px); }
    </style>
</head>
<body>
    <div class="container">
        <h1>👑 MNC COMMUNITY</h1>
        <p>لوحة التحكم الشاملة لإدارة سيرفرك باحترافية</p>
        <% if (!user) { %>
            <a href="/login" class="login-btn">تسجيل الدخول بواسطة Discord</a>
        <% } else { %>
            <a href="/dashboard" class="login-btn" style="background-color: #3ba55d;">الذهاب إلى لوحة التحكم</a>
        <% } %>
    </div>
</body>
</html>
